{
  "rows_inventoried": 82,
  "verdicts_returned": 82,
  "verdict_counts": {
    "BLOCKED": 22,
    "EXECUTABLE": 15,
    "EXECUTABLE_WITH_CHANGES": 39,
    "WRONG_DESTINATION": 3,
    "DO_NOT_EXECUTE": 3
  },
  "clearances_challenged": 54,
  "clearances_broken": 39,
  "broken_ids": [
    "R02",
    "R08",
    "R16",
    "R17",
    "R20",
    "R25",
    "R26",
    "R27",
    "R28",
    "R29",
    "R30",
    "R31",
    "R32",
    "R33",
    "R35",
    "R36",
    "R37",
    "R38",
    "R39",
    "R40",
    "R41",
    "R46",
    "R48",
    "R50",
    "R54",
    "R56",
    "R57",
    "R58",
    "R60",
    "R64",
    "R67",
    "R68",
    "R71",
    "R72",
    "R74",
    "R80",
    "R81",
    "R82",
    "R78"
  ],
  "raw_verdicts": [
    {
      "id": "R01",
      "source": "__init__.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "Trivially yes as a mechanism (a top-level __init__ curating its own package's public surface), but every name it must export (Fitness, Proposal, SamplerKernel, sample, kernels, compose, schedules, proposals, landscapes) is absent from the repo today.",
      "destination_evidence": "Current src/proteinsmc/__init__.py:1-12 exports only io, models, runner, sampling, scoring, utils. `ls src/proteinsmc/` (worktree) shows no api.py, kernels/, compose.py, schedules.py, proposals/, or landscapes/ anywhere in the tree.",
      "reachability": "Live and load-bearing: __init__.py runs on every `import proteinsmc`. Rewriting it to reference modules that don't exist would break package import entirely, not silently degrade.",
      "confidence": "high",
      "failure_shape": "none \u2014 a phase-ordering gap, not one of B.7's three shapes; 'REWRITE' undersells that this row can only be the last thing done",
      "corrected_action": "Defer to the end of the migration: REWRITE only after api.py, kernels/, compose.py, schedules.py, proposals/, and landscapes/ all exist and are stable (post phase-2, per plan \u00a77)."
    },
    {
      "id": "R02",
      "source": "io.py",
      "verdict": "EXECUTABLE",
      "same_kind_of_thing": "io.py stays io.py in-repo; the only change is delegating one function's git shell-out to bathos, whose git.py is a strict superset of what's being removed.",
      "destination_evidence": "src/proteinsmc/io.py:27-50 get_git_commit_hash (subprocess rev-parse HEAD) is the sole git shell-out in the file, confirmed by direct read; bathos/src/bathos/git.py:8-39 capture_git_state returns GitState(hash, branch, dirty) via the same subprocess idiom, a superset of the removed function.",
      "reachability": "Live: io.py:22 is imported by runner.py:51 (`from proteinsmc.io import create_metadata_file, create_writer_callback`) and create_metadata_file calls get_git_commit_hash on every run_experiment() call (runner.py:253).",
      "confidence": "high",
      "failure_shape": "none",
      "corrected_action": "As written. Note in passing (not blocking): io.py:22's `from proteinsmc.models.sampler_base import SamplerOutput` will eventually need repointing to api.KernelInfo once R09 lands, but that is a separate, later edit."
    },
    {
      "id": "R03",
      "source": "padding.py",
      "verdict": "EXECUTABLE",
      "same_kind_of_thing": "N/A \u2014 DELETE, no destination. Independently re-verified dead: rg for padding-import patterns across this worktree's src/tests/scripts returns zero hits outside the file's own definitions.",
      "destination_evidence": "-",
      "reachability": "Dead: not listed in src/proteinsmc/__init__.py:3 (io, models, runner, sampling, scoring, utils only); zero importers found independently in this worktree.",
      "confidence": "high",
      "failure_shape": "shape 1 (shared word/thing) in the plan's own rationale, not in the verdict \u2014 'xtrax owns this' is wrong: xtrax's bucket.py is host-side NumPy pre-JIT, pads the opposite axis, raises instead of truncating on overflow, and has no masked_mean/masked_sum counterpart (confirmed independently via B.3.9's citations, not re-read by me this pass)",
      "corrected_action": "DELETE as written; strike 'xtrax.tiling.bucket owns Bucket/select_bucket/bucketize' from the rationale \u2014 the delete stands on deadness alone, not on xtrax owning equivalent functionality."
    },
    {
      "id": "R04",
      "source": "runner.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "Yes \u2014 runner.py stays runner.py in-repo.",
      "destination_evidence": "src/proteinsmc/runner.py:303-326, read directly: `final_state = None` (:303), then `if config.sampler_type == \"smc\":` (:304) is the only branch that assigns final_state; SAMPLER_REGISTRY (:54-79) has 6 live entries, all validated at _validate_config (:174-191), so gibbs/mcmc/hmc/nuts/parallel_replica pass validation, execute nothing, and still log 'Run completed successfully.' (:332).",
      "reachability": "Live entry point (run_experiment, called from oed/experiment.py per errata). Confirmed independently: grep across asr/src, asr/scripts, asr/tests for 'proteinsmc.runner' / 'import runner' returns zero hits, supporting 'asr never calls it.'",
      "confidence": "high",
      "failure_shape": "none \u2014 independently reconfirms B.3.18's finding (silent no-op, not an error) rather than refuting it",
      "corrected_action": "Split into two ordered steps as the row already implies but should state explicitly: (a) phase-0 fix of the :303-304 dispatch now \u2014 generalize to all 6 registered branches or raise NotImplementedError for unhandled ones, doable today with no other-phase dependency; (b) later reduction to a `config -> kernel -> drive.sample()` shim, which is BLOCKED on api.py/kernels/drive.py existing (\u00a78 Q2 gate) and must not be attempted before (a) ships."
    },
    {
      "id": "R05",
      "source": "types.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "No \u2014 mislabeled. Current types.py (52 lines, read in full) holds ArrayLike/Scalar/ScalarFloat/NucleotideSequence/ProteinSequence/AminoAcidSequence/CodonSequence/OneHotAminoAcid/EvoSequence/BatchEvoSequence/SequenceType/UUIDArray/Vmapped*/PRNGKey. Of the row's 'keep' list (Population/Relaxed/Scores/LogWeights/Ctx/PRNGKey), only PRNGKey exists today (types.py:51); the other five are net-new names from plan \u00a73.2's code sample, not survivors of a trim.",
      "destination_evidence": "src/proteinsmc/types.py:1-51 (current, read directly) vs plan doc 260813_proteinsmc-ecosystem-partition.md:94-101 (the proposed new content, explicitly labeled '~30 lines, no proxide import' \u2014 i.e. a from-scratch replacement, not the current file shortened).",
      "reachability": "EvoSequence/BatchEvoSequence are live today: models/sampler_base.py:28 (TYPE_CHECKING import), consumed at SamplerState.sequence (:317) and SamplerOutput.sequences (:244). AminoAcidSequence/CodonSequence/OneHotAminoAcid/Vmapped*/SequenceType/UUIDArray/ArrayLike/Scalar/ScalarFloat have no stated fate in this row.",
      "confidence": "high",
      "failure_shape": "shape 1 \u2014 'STAY, trimmed' is a shared word (types.py) treated as a shared thing (the same vocabulary, minus some lines) when the actual proposal swaps in a disjoint new vocabulary",
      "corrected_action": "Relabel as REWRITE, not 'STAY, trimmed'; give an explicit disposition for every current name, not just Protein/NucleotideSequence; sequence after (not concurrently with) the api.py work that actually consumes Population/Relaxed/Scores/LogWeights/Ctx, since nothing in the current tree imports those names yet and everything that imports EvoSequence today would break if it's dropped first."
    },
    {
      "id": "R06",
      "source": "models/__init__.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "A deprecation-shim __init__ re-exporting from new locations is a sound mechanism in principle, but the 'new locations' (api.py, kernels/*, schedules.py) it must re-export from don't exist.",
      "destination_evidence": "Plan doc 260813_proteinsmc-ecosystem-partition.md:528-530, \u00a78 Q2, verbatim: 'Should models/ and utils/ actually be dissolved? ... only after phase 2 lands ... This is the most optional item in the table; decide it explicitly rather than letting it happen.' This is an explicit, unmade decision by the plan's own author, not resolved anywhere else in the document.",
      "reachability": "Live: models/__init__.py is imported by runner.py:14-21. Independently grepped this worktree's tests for 'proteinsmc.models' references: 34 of 54 test files hit, versus the plan's claimed '45 of ~50' \u2014 same ballpark (most test files), not exactly reconciled by my grep pattern.",
      "confidence": "high",
      "failure_shape": "none \u2014 this is Addendum C's residual-risk item 5 (rows built on top of an undecided prerequisite), not one of B.7's three shapes",
      "corrected_action": "Do not execute until (1) \u00a78 Q2 is explicitly decided in the affirmative and (2) api.py/kernels/schedules.py exist to be re-exported from the shim."
    },
    {
      "id": "R07",
      "source": "models/protocols.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "The delete-half is sound independent of api.py's timing: the file's sole reference is dead code. api.py (the MOVE target) doesn't exist yet, but that only affects when the *replacement* contract lands, not whether this file is safe to delete now.",
      "destination_evidence": "models/protocols.py:16-54, read directly: FitnessFn(Protocol).__call__ third arg is `context: dict[str, Array|float|int]|None`. models/fitness.py:17, read directly: `FitnessFn = Callable[[PRNGKeyArray|None, EvoSequence, PyTree|Array|None], Float]` \u2014 third arg is `PyTree|Array|None`. Genuinely different contracts, confirming B.4's caution.",
      "reachability": "Dead: sampling/particle_systems/smc.py:17,23 is the only reference anywhere (confirmed by the errata's search, consistent with `ls tests/models/` having no test_protocols.py), and it is inside `if TYPE_CHECKING:` under `from __future__ import annotations` \u2014 erased at runtime, never executed.",
      "confidence": "high",
      "failure_shape": "shape 1, avoided here by B.4's caution: same-named FitnessFn in two files is not the same contract",
      "corrected_action": "DELETE now, independent of api.py's timeline. When api.py's Fitness/Proposal eventually land, do not alias-replace `protocols.FitnessFn` with `fitness.FitnessFn` under an assumption of equivalence \u2014 repoint each of the (currently zero live) call sites deliberately."
    },
    {
      "id": "R08",
      "source": "models/fitness.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "models/fitness.py genuinely holds the string-keyed FitnessFunction/CombineFunction/FitnessEvaluator indirection the row correctly targets for deletion (confirmed by direct read: fitness.py:26-91). But needs_translation (:85-91) is `jnp.where(jnp.array([f.n_states != n_states for f in ...]), 1, 0)` \u2014 a bare integer comparison importing neither utils/constants.py nor utils/translation.py \u2014 it is not a codon leak.",
      "destination_evidence": "models/fitness.py:1-91, read in full directly, confirms B.2.13 exactly (line numbers match). The actual codon leak the errata identifies is utils/fitness.py's `translate_func: TranslateFuncSignature` (not in this chunk's files; not independently re-read by me this pass, taken from the errata's citation).",
      "reachability": "Live: models/fitness.py is imported by models/gibbs.py:9, models/sampler_base.py:31, and transitively by models/__init__.py -> runner.py.",
      "confidence": "high",
      "failure_shape": "shape 1 \u2014 'needs_translation' (a name) mistaken for the mechanism by which codons leak (a different thing, in a different file)",
      "corrected_action": "Keep the SPLIT action for FitnessFunction/CombineFunction/FitnessEvaluator -> api.py. Strike the needs_translation/codon-leak sentence from this row; move that claim to whichever row covers utils/fitness.py's TranslateFuncSignature (outside this chunk)."
    },
    {
      "id": "R09",
      "source": "models/sampler_base.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "The device-mesh piece is a confirmed genuine duplicate (sampler_base.py:153-185 _initialize_device_mesh, invoked at :151 on every config construction) matching the shape of xtrax.distributed.sharding's job per B.3.8 (xtrax source not independently re-read by me this pass). But the row's primary destination, api.KernelState/KernelInfo, is a code sample in the plan's \u00a73.3, not a file that exists anywhere in the repo.",
      "destination_evidence": "src/proteinsmc/models/sampler_base.py:38-186 (BaseSamplerConfig, SamplerOutput, SamplerState \u2014 all read directly). `ls src/proteinsmc/` shows no api.py. Plan \u00a78 Q2 (line 528-530) flags the whole models/ dissolution as an explicitly undecided, optional item.",
      "reachability": "Live: BaseSamplerConfig is the base class of SMCConfig (models/smc.py:56), ParallelReplicaConfig (models/parallel_replica.py:34), GibbsConfig (models/gibbs.py:19), MCMCConfig (models/mcmc.py:13), and HMCConfig (models/hmc.py:11) \u2014 all confirmed by direct read \u2014 so it cannot be deleted without those five files changing in the same commit, a joint dependency the move table does not flag anywhere.",
      "confidence": "high",
      "failure_shape": "shape 2 (partial) \u2014 the plan states the mesh/config duplication (structural) but the row bundles it with a full contract migration (semantic) that has no destination file yet",
      "corrected_action": "BLOCKED on (1) \u00a78 Q2 being decided, (2) api.py's KernelState/KernelInfo/Fitness actually being authored, and (3) R10-R14's five BaseSamplerConfig subclasses migrating in the same change. The mesh-deletion sub-claim (-> xtrax.distributed.sharding.get_device_mesh) can be executed on its own schedule, independent of the rest."
    },
    {
      "id": "R10",
      "source": "models/smc.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "models/smc.py:56-93 SMCConfig(BaseSamplerConfig) is config/type declaration only \u2014 no run_smc_loop here (that function lives in sampling/particle_systems/smc.py, a separate file the plan's full \u00a74 table also routes to the identical destination kernels/smc.py, confirming the merge is deliberate design, not a naming collision). Destination kernels/smc.py does not exist.",
      "destination_evidence": "models/smc.py:16, read directly: `from .sampler_base import BaseSamplerConfig, SamplerOutputProtocol` \u2014 a direct dependency on R09's undeleted BaseSamplerConfig. `ls src/proteinsmc` confirms no kernels/ directory anywhere in the tree.",
      "reachability": "Live and heavily exercised: SMCConfig backs the only sampler_type runner.py actually executes end-to-end today (runner.py:304-324, per R04's finding).",
      "confidence": "high",
      "failure_shape": "none \u2014 correctly identified merge target, but the row cannot be executed as an isolated single-file move",
      "corrected_action": "BLOCKED on R09 landing (BaseSamplerConfig dissolved into api.py) and on the sibling row for sampling/particle_systems/smc.py (outside this chunk) executing in the same change \u2014 this is a two-file, one-commit merge, not an independent move."
    },
    {
      "id": "R11",
      "source": "models/parallel_replica.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "The rename itself is well-founded once the loop code (a different file, sampling/particle_systems/parallel_replica.py) joins it: plan \u00a76.3 [CONCEDED] (doc lines 395-417) independently confirms migrate() is a genuinely distinct island-model migration algorithm, not shared with aminx/prolix PT, so 'named for what it is' is honest. But this row's actual source, models/parallel_replica.py, is config-only (ParallelReplicaConfig, ExchangeConfig, PRSMCOutput) and destination kernels/ doesn't exist.",
      "destination_evidence": "models/parallel_replica.py:8, read directly: `from .sampler_base import BaseSamplerConfig, SamplerOutputProtocol`. Plan doc lines 395-417 (\u00a76.3) for the algorithm-identity argument.",
      "reachability": "ParallelReplicaConfig is registered in SAMPLER_REGISTRY (runner.py:59-62) and passes validation, but per R04's confirmed finding its run_fn is never actually invoked by run_experiment (only the 'smc' branch executes) \u2014 validated but functionally inert under the current runner.py bug.",
      "confidence": "high",
      "failure_shape": "none",
      "corrected_action": "Same prerequisite as R10: BLOCKED on R09 and on the sibling sampling/particle_systems/parallel_replica.py row executing jointly."
    },
    {
      "id": "R12",
      "source": "models/gibbs.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "models/gibbs.py:19-20 GibbsConfig(BaseSamplerConfig) is a two-line config stub; the Gibbs update logic (make_gibbs_update_fns, initialize_gibbs_state) lives in sampling/gibbs.py, a separate file. Destination kernels/gibbs.py doesn't exist.",
      "destination_evidence": "src/proteinsmc/models/gibbs.py:1-20, read in full: GibbsUpdateFn alias + GibbsConfig(BaseSamplerConfig), no loop code. sampling/gibbs.py:27,38 (initialize_gibbs_state, make_gibbs_update_fns), read directly, holds the actual algorithm \u2014 confirming the config/implementation split is real and consistent with R10/R11.",
      "reachability": "Registered in SAMPLER_REGISTRY (runner.py:63-66) but functionally inert today under the R04 dispatch bug (only 'smc' executes).",
      "confidence": "high",
      "failure_shape": "none",
      "corrected_action": "BLOCKED on R09 landing and on the (not-in-this-chunk) sampling/gibbs.py row executing in the same change."
    },
    {
      "id": "R13",
      "source": "models/mcmc.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "models/mcmc.py:12-23 MCMCConfig(BaseSamplerConfig) is a config stub (adds only step_size); the actual MCMC loop lives in sampling/mcmc.py, a separate file. Destination kernels/mcmc.py doesn't exist.",
      "destination_evidence": "src/proteinsmc/models/mcmc.py:1-23, read in full, confirms config-only content and direct dependency on BaseSamplerConfig (:7).",
      "reachability": "Registered in SAMPLER_REGISTRY (runner.py:67-70) but functionally inert today under the R04 dispatch bug.",
      "confidence": "high",
      "failure_shape": "none",
      "corrected_action": "BLOCKED on R09 landing and joint execution with the sampling/mcmc.py row."
    },
    {
      "id": "R14",
      "source": "models/hmc.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "models/hmc.py:10-24 HMCConfig(BaseSamplerConfig) is a config stub (step_size, num_integration_steps); the loop lives in sampling/hmc.py. The row's added scope, 'gains required relax: boundary,' refers to the Relaxed type from plan \u00a73.2's proposed types.py (R05) \u2014 itself not yet real.",
      "destination_evidence": "src/proteinsmc/models/hmc.py:1-24, read in full, confirms config-only content and BaseSamplerConfig dependency (:7). Plan doc \u00a73.2 code block (lines 94-101) is the only place `Relaxed` is defined, and it does not exist in the current types.py (confirmed by R05's direct read).",
      "reachability": "Registered in SAMPLER_REGISTRY (runner.py:71-74) but functionally inert today under the R04 dispatch bug. Separately, HMC/NUTS have 4 known failing tests in test_initialization_factory.py (per CLAUDE.md) tied to int8->float32 mismatch \u2014 exactly the defect the relax: boundary is meant to fix by construction (per errata B.2.6), so this row is also entangled with the disposition of sampling/initialization_factory.py, not audited in this chunk.",
      "confidence": "high",
      "failure_shape": "none",
      "corrected_action": "BLOCKED on R05 (Relaxed must exist), R09 (BaseSamplerConfig dissolved), and joint execution with the sampling/hmc.py row."
    },
    {
      "id": "R15",
      "source": "models/nuts.py",
      "verdict": "WRONG_DESTINATION",
      "same_kind_of_thing": "models/nuts.py is only NUTSConfig, a frozen dataclass of two scalar fields (step_size, max_num_doublings) extending BaseSamplerConfig (models/nuts.py:10-25); every sibling row in this same chunk that targets kernels/ (R25 particle_systems/smc.py, R26 parallel_replica.py, R27 sampling/gibbs.py, R28 sampling/mcmc.py) moves the LOOP IMPLEMENTATION file, never the models/ config file, so kernels/nuts.py should hold sampling/nuts.py's run_nuts_loop (136 lines, sampling/nuts.py:33-136), not the config.",
      "destination_evidence": "src/proteinsmc/sampling/__init__.py:1-17 imports run_gibbs_loop, run_hmc_loop, run_mcmc_loop, run_nuts_loop from sibling .gibbs/.hmc/.mcmc/.nuts submodules (all loop implementations, mirroring R25-28's kernels/ targets); src/proteinsmc/runner.py:18,76-78 registers NUTSConfig as config_cls and run_nuts_loop (imported from proteinsmc.sampling at runner.py:24-27) as run_fn in SAMPLER_REGISTRY[\"nuts\"], i.e. the config and the loop are two distinct, separately-registered artifacts.",
      "reachability": "Both files are live: models/nuts.py's NUTSConfig is imported at runner.py:18 and registered as SAMPLER_REGISTRY[\"nuts\"][\"config_cls\"] (runner.py:76); sampling/nuts.py's run_nuts_loop is imported at runner.py:27 and registered as SAMPLER_REGISTRY[\"nuts\"][\"run_fn\"] (runner.py:77). This is not a dead-code issue \u2014 it is the plan naming the wrong one of two live, differently-typed files that happen to share the word \"nuts\".",
      "confidence": "high",
      "failure_shape": "shape 1 (shared WORD \"nuts.py\" across models/ and sampling/ treated as the shared THING that should move to kernels/nuts.py)",
      "corrected_action": "MOVE sampling/nuts.py (run_nuts_loop) -> proteinsmc/kernels/nuts.py, matching R25-28's pattern. models/nuts.py (NUTSConfig) is a separate item with its own disposition, not addressed by this row or this chunk, and must not be silently mapped onto the same kernels/nuts.py destination."
    },
    {
      "id": "R16",
      "source": "models/annealing.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "models/annealing.py's 47-line AnnealingConfig (models/annealing.py:13-48) is a config-only shell for utils/annealing.py's 264-line ANNEALING_REGISTRY + get_annealing_function + linear/exponential/cosine/static schedule implementations (utils/annealing.py:1-265); the row's own rationale (\"47L dataclass over a 264L registry\") only makes sense read as a mandate to merge both into one schedules.py, but the row's source field lists only models/annealing.py.",
      "destination_evidence": "utils/annealing.py:16-19 imports AnnealingConfig and AnnealingFn from proteinsmc.models.annealing under TYPE_CHECKING; if only models/annealing.py moves, this import breaks and must be repointed to proteinsmc.schedules regardless, so the natural fix is to move both files together.",
      "reachability": "Both are live on the default runner path: runner.py:33 imports get_annealing_function from proteinsmc.utils.annealing, and runner.py:260 calls it unconditionally inside run_experiment; AnnealingConfig is instantiated in models/smc.py:63, models/parallel_replica.py:30/42, and throughout tests/conftest.py.",
      "confidence": "high",
      "failure_shape": "none \u2014 this is an under-scoped merge (the row's stated source omits the file its own rationale requires), not one of B.7's three catalogued shapes",
      "corrected_action": "MERGE models/annealing.py + utils/annealing.py -> proteinsmc/schedules.py (config and registry/schedule functions together), not the config file alone."
    },
    {
      "id": "R17",
      "source": "models/memory.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "models/memory.py's MemoryConfig/AutoTuningConfig (models/memory.py:8-53) are not a standalone, deletable unit \u2014 MemoryConfig is sampler_base.py's default-constructed field (models/sampler_base.py:60, default_factory=MemoryConfig) present on every BaseSamplerConfig subclass, so deleting the config type without a replacement breaks every sampler config's construction path, not just an isolated memory-tuning feature.",
      "destination_evidence": "src/proteinsmc/models/sampler_base.py:60 `memory_config: MemoryConfig = field(default_factory=MemoryConfig)`; src/proteinsmc/oed/experiment.py:55-56 instantiates `AutoTuningConfig(enable_auto_tuning=False)` and `MemoryConfig(auto_tuning_config=auto_tuning_config)` at runtime inside run_oed_experiment.",
      "reachability": "Confirmed independently (matches B.2.7/B.4): MemoryConfig/AutoTuningConfig are runtime-instantiated in oed/experiment.py:55-56, which is reachable from oed/run.py's `run_oed_experiment` import (oed/run.py:11) \u2014 the OED CLI entry point per CLAUDE.md. utils/memory.py's auto_tune_batch_size is imported live at runner.py:34. Both are on the default execution path since AutoTuningConfig.enable_auto_tuning defaults True (models/memory.py:48).",
      "confidence": "high",
      "failure_shape": "shape 3 (a DELETE is an implicit negative claim \u2014 \"nothing depends on this\" \u2014 asserted from a search that missed oed/experiment.py's runtime construction and sampler_base.py's default_factory wiring)",
      "corrected_action": "REWRITE, not DELETE: retitle per B.4 \u2014 replace with xtrax.tiling.MemoryBudget + a proteinsmc byte-estimator closure only after repointing sampler_base.py:60's default_factory and oed/experiment.py:55-56's construction sites, and resolve jointly with utils/memory.py (auto_tune_batch_size), which is a separate live file this row doesn't mention."
    },
    {
      "id": "R18",
      "source": "models/mutation.py",
      "verdict": "WRONG_DESTINATION",
      "same_kind_of_thing": "No \"api\" module or \"Proposal\" class exists anywhere in proteinsmc (confirmed via full src/ file listing) or in aminx (grepped aminx/src, zero hits for \"Proposal\"). Worse, the row's own justification \u2014 \"11-line Callable alias becomes a real protocol\" \u2014 is already satisfied by a DIFFERENT, pre-existing file: proteinsmc/models/protocols.py:57-83 already defines a canonical `MutationFn(Protocol)` with the full (key, sequence, context) signature that models/mutation.py's row wants to invent.",
      "destination_evidence": "src/proteinsmc/models/protocols.py:57-83 defines `class MutationFn(Protocol)` with `__call__(self, key, sequence, context=None) -> Array`; this is the version actually imported and used by the live SMC/parallel-replica kernels at sampling/particle_systems/smc.py:22,50,80,102,170 and sampling/particle_systems/parallel_replica.py:19,245,275,435.",
      "reachability": "models/mutation.py's 2-arg `MutationFn = Callable[[PRNGKeyArray, EvoSequence], EvoSequence]` (models/mutation.py:11) is referenced only by underscore-prefixed, intentionally-unused parameters: sampling/nuts.py:27,40 (`_mutation_fn: MutationFn | None = None`) and sampling/hmc.py:19,33 (same pattern) \u2014 i.e. it is reachable only in dead parameters, while the load-bearing MutationFn used by the actually-invoked kernels is the different, 3-arg protocols.py Protocol.",
      "confidence": "high",
      "failure_shape": "shape 1 (the name \"MutationFn\" is shared across models/mutation.py and models/protocols.py as though they were the same thing being promoted to a protocol, when protocols.py already independently defines a different-signature protocol of the same name that is the one actually in live use)",
      "corrected_action": "DELETE models/mutation.py after repointing sampling/nuts.py's and sampling/hmc.py's dead `_mutation_fn: MutationFn` type hints to proteinsmc.models.protocols.MutationFn. There is no 'api.Proposal' destination in this repo or in aminx to move it to."
    },
    {
      "id": "R19",
      "source": "models/nk_landscape.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "As a pure consolidation the merge is coherent: models/nk_landscape.py:33 defines NKLandscape (a flax PyTreeNode), utils/nk_landscape.py:21,145-169 generates it (generate_nk_model), and scoring/nk.py:12,20 wraps it as make_nk_score for the fitness registry \u2014 one dependency chain, naturally one file. The blocker is not the merge mechanics but the unresolved ownership question this row's own b_verdict cites (proteinsmc vs trex, \u00a78 Q4/\u00a76.6, R77).",
      "destination_evidence": "src/proteinsmc/oed/experiment.py:1-6 (module docstring: 'helpers to run small, testable OED experiments on NK landscapes') and :99-101 (`FitnessFunction(name=\"nk\", n_states=design.q, kwargs={\"landscape\": landscape, \"n\": design.N, \"k\": design.K})`), fed by oed/nk.py:10-40 `create_landscape_from_design` -> utils.nk_landscape.generate_nk_model.",
      "reachability": "Correction to the cited B.3.7 premise: 'unused by the only live consumer' is true ONLY of NKLandscapeConfig (models/nk_landscape.py:18-30) \u2014 grep finds zero non-test importers, only tests/models/test_nk_landscape.py:20. It is NOT true of NKLandscape/generate_nk_model: oed/run.py:11 imports run_oed_experiment (the CLI entry point per CLAUDE.md's documented `oed/run.py` command), which unconditionally builds the NK-backed FitnessFunction at oed/experiment.py:99-101 \u2014 this is the OED module's primary, wired fitness path, not dead code.",
      "confidence": "medium",
      "failure_shape": "shape 3 (the 'unused by the only live consumer' negative was true for one class in the file \u2014 NKLandscapeConfig \u2014 but was applied to the whole file/module, missing NKLandscape's live use in oed/experiment.py)",
      "corrected_action": "Hold on the proteinsmc-vs-trex ownership decision (\u00a78 Q4/R77) before executing \u2014 resolving it against the code as actually deletable/unused (per the plan's premise) understates the blast radius: NK is not incidental, it is OED's current default fitness landscape."
    },
    {
      "id": "R20",
      "source": "models/translation.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "models/translation.py's TranslateFuncSignature (models/translation.py:11-13) is an 11-line type alias with no implementation; the functions it types \u2014 nucleotide_to_aa, aa_to_nucleotide, string_to_int_sequence \u2014 live in utils/translation.py, not in the row's stated source. scoring/cai.py (Codon Adaptation Index, scoring/cai.py:1) is the existing evidence of a 'codon' domain in this repo, and it imports `nucleotide_to_aa` from proteinsmc.utils.translation (scoring/cai.py:23) \u2014 the sibling module a codon/ package would sit beside.",
      "destination_evidence": "scoring/cai.py:1,23 (codon-domain module importing utils/translation.py's actual functions); runner.py:48,98 and utils/fitness.py:24,44,71 both import TranslateFuncSignature from proteinsmc.models.translation on the live runner path.",
      "reachability": "Both files are live: TranslateFuncSignature at runner.py:48/98 and utils/fitness.py:24/44/71; the underlying translation functions are called directly by runner.py and by scoring/cai.py:23.",
      "confidence": "high",
      "failure_shape": "none \u2014 under-scoped merge, same pattern as R16",
      "corrected_action": "MERGE models/translation.py + utils/translation.py -> proteinsmc/codon/translation.py (type and implementation together), not the type alias alone."
    },
    {
      "id": "R21",
      "source": "models/types.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "models/types.py (models/types.py:1-39) is explicitly a 'legacy redirect' re-exporting proteinsmc.types' names; it is the same kind of thing as any other still-imported shim, not free-standing dead code \u2014 the question is import count, which I independently re-verified.",
      "destination_evidence": "src/proteinsmc/models/__init__.py:22 re-exports from .types; every other importer resolves through `proteinsmc.models.types`, not a direct path.",
      "reachability": "Confirmed live, exceeding B's '15+' count: utils/jax_utils.py:16, utils/initiate.py:14, utils/fitness.py:25, utils/blackjax_utils.py:13, utils/mutation.py:13, utils/jax_uuid.py:13, models/translation.py:9, models/fitness.py:12, models/gibbs.py:11, models/mutation.py:9, models/sampler_base.py:28, scoring/cai.py:20, scoring/esm.py:17, scoring/mpnn.py:36, sampling/initialization_factory.py:29 (15 src-side importers) plus at least 6 test files (test_hmc_loop.py, test_mcmc_loop.py, test_nuts_loop.py, test_fitness.py, test_fitness_advanced.py, test_types.py).",
      "confidence": "high",
      "failure_shape": "shape 3 (DELETE as phase-1 action is a negative claim about importer count, refuted by a fuller grep \u2014 matches B's own finding, independently reconfirmed at a higher count)",
      "corrected_action": "Schedule a codemod repointing all ~20 importers from proteinsmc.models.types to proteinsmc.types, then delete \u2014 not a standalone phase-1 delete."
    },
    {
      "id": "R22",
      "source": "models/lineage.py",
      "verdict": "EXECUTABLE",
      "same_kind_of_thing": "models/lineage.py (models/lineage.py:1) is a single-line module docstring with no code, class, or function \u2014 there is no 'thing' for a destination to be the same kind of as; deletion just removes an empty file.",
      "destination_evidence": "n/a \u2014 DELETE with no destination.",
      "reachability": "Zero importers of `proteinsmc.models.lineage` or `from .lineage` anywhere in src/ or tests/ (grep only returns unrelated hits: io.py's own `read_lineage_data`/`read_lineage_data_range` functions and oed/smc_tree.py's local variables named `lineage_indices`/`lineage`, none of which import this module).",
      "confidence": "high",
      "failure_shape": "none",
      "corrected_action": ""
    },
    {
      "id": "R23",
      "source": "sampling/__init__.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "sampling/__init__.py:1-17 re-exports exactly the six loop implementations (run_gibbs_loop, run_hmc_loop, run_mcmc_loop, run_nuts_loop, run_prsmc_loop, run_smc_loop) that this chunk's MOVE rows are individually relocating into kernels/*.py \u2014 rewriting it to import from the new kernels/ submodule paths is the correct action in principle.",
      "destination_evidence": "src/proteinsmc/sampling/__init__.py:3-8 (`from .gibbs import run_gibbs_loop`, `from .hmc import run_hmc_loop`, `from .mcmc import run_mcmc_loop`, `from .nuts import run_nuts_loop`, `from .particle_systems.parallel_replica import run_prsmc_loop`, `from .particle_systems.smc import run_smc_loop`).",
      "reachability": "Live \u2014 runner.py:24-27 imports run_gibbs_loop/run_hmc_loop/run_mcmc_loop/run_nuts_loop directly from proteinsmc.sampling (this __init__.py), so its export surface is load-bearing, not incidental.",
      "confidence": "medium",
      "failure_shape": "downstream instance of R15's shape-1 error",
      "corrected_action": "Cannot execute cleanly until R15 is corrected: as R15 currently stands, kernels/nuts.py would be populated by NUTSConfig (a config) rather than run_nuts_loop, so the rewritten __init__.py could not do `from .nuts import run_nuts_loop` as its sibling lines require. Also note sampling/hmc.py -> kernels/hmc.py has no row in this chunk and must land consistently for this rewrite to close."
    },
    {
      "id": "R24",
      "source": "sampling/particle_systems/__init__.py",
      "verdict": "EXECUTABLE",
      "same_kind_of_thing": "sampling/particle_systems/__init__.py:1-9 is a pure re-export shim (run_prsmc_loop, run_smc_loop) with no logic of its own \u2014 the same class of file as R22's lineage.py in spirit, just non-empty; deleting it only matters if something imports through it rather than through the submodules directly.",
      "destination_evidence": "n/a \u2014 DELETE.",
      "reachability": "Confirmed zero consumers of the package-level re-export: grep for `from proteinsmc.sampling.particle_systems import` / `from .particle_systems import` returns nothing. Every actual consumer imports the submodules directly: runner.py:29,32 (`.particle_systems.parallel_replica`, `.particle_systems.smc`), sampling/__init__.py:7-8 (same), tests/sampling/particle_systems/test_parallel_replica.py:16,20 and test_smc.py:15 (same).",
      "confidence": "high",
      "failure_shape": "none",
      "corrected_action": ""
    },
    {
      "id": "R25",
      "source": "sampling/particle_systems/smc.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "Corroborates the audited premise: sampling/particle_systems/smc.py:171,190 has a `writer_callback` parameter invoked directly in the loop body, and :87 uses `static_argnames`, matching the rationale's 'hand-rolled chunking + writer_callback' and 'jax.jit(static_argnames=...)' descriptions verbatim.",
      "destination_evidence": "src/proteinsmc/sampling/particle_systems/smc.py:87 (static_argnames), :171 (writer_callback param), :190 (writer_callback(collected_outputs) call site).",
      "reachability": "Live: imported directly at runner.py:32 and sampling/__init__.py:8.",
      "confidence": "medium",
      "failure_shape": "none",
      "corrected_action": "Carry forward B.3.14's qualification verbatim (not independently re-derived here): filter_jit swap is mechanical for existing call sites, but a future prsmc dispatch through config_to_jax would need params explicitly re-staticised."
    },
    {
      "id": "R26",
      "source": "sampling/particle_systems/parallel_replica.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "Corroborates the audited premise: parallel_replica.py defines `migrate` (lines 28 and 50 \u2014 note: two definitions of `migrate` exist in this single file, worth a closer look independent of this row) and uses `lax.cond` at lines 155 and 366, consistent with the rationale's 'fix the lax.cond non-scalar-predicate bug in migrate'.",
      "destination_evidence": "src/proteinsmc/sampling/particle_systems/parallel_replica.py:28,50 (`def migrate(`), :155,366 (`lax.cond(`).",
      "reachability": "Live: run_prsmc_loop imported directly at runner.py:29-31 and sampling/__init__.py:7.",
      "confidence": "medium",
      "failure_shape": "none",
      "corrected_action": "Carry forward B.3.14 (filter_jit) + B.3.17 (migrate() as one of two named collisions) verbatim; flag the two separate `migrate` definitions (lines 28, 50) for a closer look before this MOVE, since B's citation doesn't distinguish which one carries the bug."
    },
    {
      "id": "R27",
      "source": "sampling/gibbs.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "sampling/gibbs.py:106-165's run_gibbs_loop is a live loop implementation, the same category as R25/26/28's kernels/ destinations (models/gibbs.py's GibbsConfig correctly stays out of scope, unlike R15's nuts mistake \u2014 this row picked the right file).",
      "destination_evidence": "src/proteinsmc/sampling/__init__.py:3 (`from .gibbs import run_gibbs_loop`); runner.py registers run_gibbs_loop as SAMPLER_REGISTRY[\"gibbs\"][\"run_fn\"].",
      "reachability": "Live: sampling/gibbs.py:11 imports `jax.experimental.io_callback as jax_io_callback`, used at :151-160 inside the JIT'd loop \u2014 confirming the rationale's 'Drops private io_callback' targets real, currently-present code.",
      "confidence": "high",
      "failure_shape": "none catalogued \u2014 but flagging a verification gap: the rationale's 'consumes PointFitness' names a type with zero hits anywhere in src/ or tests/ (grepped) \u2014 it does not exist yet in this codebase and must be newly designed, not merely carried over by a MOVE.",
      "corrected_action": "Relabel as REWRITE rather than MOVE: `PointFitness` must be created as part of this work (unverified/nonexistent today). Separately, aminx.potts.sampling.gibbs_sweep (aminx/src/aminx/potts/sampling.py:195) is a real but domain-different function (Potts-model EBM sampling vs. this generic FitnessFn-based Gibbs loop) \u2014 a naming collision to watch if kernels/gibbs.py is ever imported alongside aminx.potts, not a blocker for this proteinsmc-internal move."
    },
    {
      "id": "R28",
      "source": "sampling/mcmc.py",
      "verdict": "EXECUTABLE",
      "same_kind_of_thing": "sampling/mcmc.py:23-126's run_mcmc_loop is a live loop implementation, the same category as R25/26/27's kernels/ destinations (models/mcmc.py's MCMCConfig correctly stays out of scope of this row, same correct pattern as R27).",
      "destination_evidence": "src/proteinsmc/sampling/__init__.py:5 (`from .mcmc import run_mcmc_loop`); runner.py registers run_mcmc_loop as SAMPLER_REGISTRY[\"mcmc\"][\"run_fn\"].",
      "reachability": "Live via runner.py/sampling/__init__.py. Both rationale claims independently verified against current source: 'Drops io_callback' \u2014 sampling/mcmc.py:108-117 has a plain post-scan Python for-loop calling `io_callback(single_output)` outside JIT, exactly the pattern to remove; 'adopts _blackjax.make_blackjax_log_prob_fn instead of its inline wrapper' \u2014 utils/blackjax_utils.py:16 already defines `make_blackjax_log_prob_fn`, while sampling/mcmc.py:69-72 currently defines its own inline `logdensity_fn_step` duplicating that logic, so the replacement utility already exists and this is a low-risk swap.",
      "confidence": "high",
      "failure_shape": "none",
      "corrected_action": ""
    },
    {
      "id": "R29",
      "source": "sampling/hmc.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "kernels/ doesn't exist yet, but is this plan's designated home for per-step sampler-loop bodies (same target as R30/R32); hmc.py is a live, registered sampler body, not dead code, so it belongs there in principle once the init logic it depends on is ported.",
      "destination_evidence": "src/proteinsmc/kernels does not exist (ls error, verified). proteinsmc/runner.py's SAMPLER_REGISTRY registers 'hmc': {'run_fn': run_hmc_loop} and validates config.sampler_type against it before dispatch.",
      "reachability": "LIVE. grep of runner.py shows 'hmc' is a real SAMPLER_REGISTRY key, consulted at the config-validation gate, so run_experiment(sampler_type='hmc') executes sampling/hmc.py today.",
      "confidence": "medium",
      "failure_shape": "shape 2 (MOVE label hides a REWRITE: drops io_callback, replaces mutation_fn with an explicit relax:Callable \u2014 a behavior change) and shape 3 (the 'resolves the 4 known test_initialization_factory.py failures' claim rests on evidence narrower than the claim: docs/FAILING_TESTS_ANALYSIS.md:264-269 names the 6 disputed failures as test_initialize_parallel_replica_state, test_mcmc_initialization, test_smc_state_initialization, test_smc_state_with_none_beta, test_prsmc_state_initialization, test_prsmc_additional_fields_values \u2014 none is an HMC/NUTS test. The actual HMC/NUTS init tests in that same file, and the entire TestRunHMCLoop class in tests/sampling/test_hmc_loop.py, are already @pytest.mark.skip, not failing.)",
      "corrected_action": "Retitle MOVE -> REWRITE. Drop the '4 known failures' justification \u2014 verified those named failures concern ParallelReplica/MCMC/SMC/PRSMC state init, not HMC or NUTS. Sequence after R32's SPLIT/REWRITE so hmc.py's init can call the ported blackjax-algorithm dispatch rather than the deleted initialization_factory.py."
    },
    {
      "id": "R30",
      "source": "sampling/nuts.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "Same kernels/ home as R29/R31 for a per-step sampler loop body; independently confirmed nuts.py is a live, currently-callable registry entry, not a placeholder awaiting registration.",
      "destination_evidence": "src/proteinsmc/kernels does not exist. runner.py's SAMPLER_REGISTRY contains a 'nuts' key mapping to run_nuts_loop; validated at the same config.sampler_type gate as 'hmc'.",
      "reachability": "LIVE \u2014 confirmed directly: 'nuts' is present as a SAMPLER_REGISTRY key in runner.py today, so run_experiment(sampler_type='nuts') currently executes this loop despite its docstring calling itself 'a conceptual placeholder.'",
      "confidence": "high",
      "failure_shape": "shape 2 \u2014 the docstring's self-description (a structural/textual signal) is read as the current registration status (a semantic conclusion), when the registry dict (the actual structural fact) says nuts IS registered.",
      "corrected_action": "Destination unchanged. Action text must say DE-REGISTER nuts from SAMPLER_REGISTRY (removing a currently-callable sampler_type \u2014 a behavior change), not 'keep unregistered' as if that were the status quo."
    },
    {
      "id": "R31",
      "source": "sampling/ste.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "Confirmed dead/unregistered demo code today (no reference to 'ste' anywhere in runner.py), using jax.example_libraries.optimizers rather than optax \u2014 REWRITE-not-MOVE is the right call, but the stated justification is partly wrong.",
      "destination_evidence": "src/proteinsmc/kernels does not exist yet, consistent with siblings.",
      "reachability": "DEAD today for the sampler dispatch path \u2014 grep of runner.py finds zero references to ste; only reachable via direct manual import, never through run_experiment.",
      "confidence": "high",
      "failure_shape": "shape 1 \u2014 'no population axis' is a shared-word claim that doesn't match the actual code: ste.py:94 is `jax.vmap(run_optimization_loop, in_axes=(0,0,0))`, a real population axis. The true defect is that the declared STELoopFn type (ste.py:22-24) is unbatched while the implementation already vmaps \u2014 a type/impl mismatch, not an absent axis.",
      "corrected_action": "Same action and destination. Correct the rationale from 'no population axis' to 'declared STELoopFn type is unbatched but run_optimization_loop is already vmapped over axis 0 (ste.py:94) \u2014 an undeclared-axis mismatch,' which is a stronger, not weaker, REWRITE argument."
    },
    {
      "id": "R32",
      "source": "sampling/initialization_factory.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "DELETE with destination '-' is wrong: the file's real substance (a 9-way blackjax-algorithm dispatch and the full PRSMC island-state constructor) is the same kind of thing as what R29-R31's kernels/ destination is meant to hold, and has nowhere else to go.",
      "destination_evidence": "Read in full: 393 lines. _initialize_blackjax_smc_state (:198-249) is a 9-way match over BaseSMC|AnnealedSMC|ParallelReplicaSMC|FromMCMC/AdaptiveTemperedSMC/PartialPosteriors/TemperedSMC/CustomSMC (+2 NotImplementedError branches) \u2014 orthogonal to the sampler_type string switch. _initialize_prsmc_state (:293-393) builds tiled island populations, calls diversify_initial_sequences, vmaps a BaseSMCState construction and per-island fitness evaluation, and builds the beta/mean_fitness/max_fitness/ess/logZ_estimate additional_fields the PRSMC loop reads.",
      "reachability": "LIVE \u2014 runner.py imports and calls this module as part of run_experiment's real dispatch; tests/sampling/test_initialization_factory.py is 1022 lines (confirmed via wc -l).",
      "confidence": "high",
      "failure_shape": "shape 1 \u2014 'a single dispatch on a sampler_type string' treats the ~30-line outer switch as the whole file's identity, when the file's mass is two unrelated dispatches.",
      "corrected_action": "DELETE -> SPLIT/REWRITE. Port _initialize_blackjax_smc_state and _initialize_prsmc_state into the kernels layer first; do not delete until the int8->float32 dtype bug they currently make testable (via the HMC/NUTS init tests) is re-covered by tests in the new location."
    },
    {
      "id": "R33",
      "source": "scoring/__init__.py",
      "verdict": "EXECUTABLE",
      "same_kind_of_thing": "n/a for a bare DELETE with no destination; the file (13 lines, read in full) only re-exports 4 factory names, and its sole proteinsmc-side consumer pattern (utils/fitness.py:11) imports submodules directly, not the re-exported names, so deleting the re-export content changes nothing for that consumer.",
      "destination_evidence": "proteinsmc/scoring/__init__.py read in full \u2014 only `from .cai import ...` style re-exports; proteinsmc/__init__.py:3 `from . import io, models, runner, sampling, scoring, utils` eagerly imports the scoring package, which is what currently forces scoring/mpnn.py's find_spec('prxteinmpnn') gate to run at proteinsmc-package-import time.",
      "reachability": "live at package-import time (eager import chain), but the specific re-exported factory names have zero consumers.",
      "confidence": "high",
      "failure_shape": "none for this claim in isolation.",
      "corrected_action": "No change to this row. Flag as an unassigned cross-row dependency: once R34/R35/R36/R37/R38 physically relocate the five scoring submodules, utils/fitness.py:11's `from proteinsmc.scoring import cai, combine, esm, mpnn, nk` breaks regardless of this row \u2014 that repoint is not assigned to any row in this chunk."
    },
    {
      "id": "R34",
      "source": "scoring/nk.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "The merge target is coherent in principle \u2014 scoring/nk.py (thin factory), utils/nk_landscape.py (generate_nk_model, calculate_nk_fitness_single), and models/nk_landscape.py (NKLandscape PyTreeNode) are a landscape definition plus its own fitness adapter, a natural single-file bundle; '3 files -> 1' is verified accurate.",
      "destination_evidence": "src/proteinsmc/landscapes does not exist (ls error). Confirmed 3 source files: models/nk_landscape.py:33 class NKLandscape(PyTreeNode); utils/nk_landscape.py:140,174 generate_nk_model / calculate_nk_fitness_single; scoring/nk.py:20 make_nk_score wraps both.",
      "reachability": "LIVE \u2014 utils/fitness.py:32 registers 'nk': nk.make_nk_score in FITNESS_FUNCTIONS, consumed by get_fitness_function, called from runner.py's run_experiment.",
      "confidence": "medium",
      "failure_shape": "shape 3 \u2014 this row was not audited in B (its own b_verdict field is blank) and a real consumer was missed: oed/nk.py separately imports models.nk_landscape.NKLandscape and utils.nk_landscape.generate_nk_model directly, independent of scoring/nk.py, and this row's own note says NK ownership is unresolved pending a different chunk's row (R77, \u00a76.6/\u00a78 Q4).",
      "corrected_action": "BLOCKED on the \u00a76.6/\u00a78 Q4 NK-ownership decision named in the row's own annotation (whether oed/nk.py's separate consumption also moves into landscapes/, or the split stands). Once unblocked: also repoint utils/fitness.py:11,32's import/registry entry to the new landscapes location in the same phase \u2014 an unlisted consumer shared with R33/R35/R36/R37/R38."
    },
    {
      "id": "R35",
      "source": "scoring/combine.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "combine.py's two factories are pure closures with no external deps beyond jax.numpy, so relocation alone is trivial \u2014 but the rationale ('first-class sum_of/weighted, not registry-string-selected') proposes changing the calling contract, not just the file's location.",
      "destination_evidence": "proteinsmc/compose.py does not exist (find returned nothing). combine.py (85 lines, read in full): both make_sum_combine/make_weighted_combine are looked up today via COMBINE_FUNCTIONS[combine_config.name] (utils/fitness.py:35-38,87-91), string-keyed off FitnessEvaluator.combine_fn.name (models/fitness.py).",
      "reachability": "LIVE \u2014 get_fitness_function (utils/fitness.py:68) is called from runner.py's run_experiment at two sites and always resolves a combine_fn through this registry.",
      "confidence": "medium",
      "failure_shape": "shape 2 \u2014 'move combine.py' is stated as a file relocation (structural), but the rationale's own text ('not registry-string-selected') is a semantic redesign of FitnessEvaluator's config contract and get_fitness_function's resolution mechanism.",
      "corrected_action": "Retitle MOVE -> REWRITE. Scope must include FitnessEvaluator.combine_fn's config field and utils/fitness.py's COMBINE_FUNCTIONS resolution, or the 'first-class, not registry-string' framing is unimplementable as scoped. Repoint utils/fitness.py:11,36-38's import to compose.py in the same phase \u2014 unlisted consumer shared with R33/R34/R36/R37/R38."
    },
    {
      "id": "R36",
      "source": "scoring/mpnn.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "aminx already hosts a native MPNN-family sequence scorer at aminx/src/aminx/scoring/score.py:57 make_score_fn(model: ModelProtocol, ...), built for aminx's own ModelProtocol-typed checkpoints (Aminx, PrxteinLigandMPNN in aminx/model/). proteinsmc's mpnn.py is the same kind of thing (an MPNN sequence scorer) but currently passes a bare, type-ignore-masked PyTree instead of a ModelProtocol instance (mpnn.py:65,111-112), so it isn't yet type-compatible with the convention it would sit beside.",
      "destination_evidence": "aminx/src/aminx/scoring/score.py:57 (ModelProtocol contract); aminx/src/aminx/types/protocols.py defines ModelProtocol; ls aminx/src/aminx/ confirms no existing fitness/ directory (would be newly created, parallel to existing scoring/ and model/).",
      "reachability": "LIVE in proteinsmc \u2014 registered at utils/fitness.py:30 ('mpnn': mpnn.make_mpnn_score), reachable via get_fitness_function/runner.py; but never exercised by a proteinsmc test \u2014 rg -ln mpnn tests/ finds only tests/utils/test_fitness.py's membership assertion, confirmed directly.",
      "confidence": "high",
      "failure_shape": "none new beyond what the given b_verdict already states; independently reproduced all four fixes: no AF->MPNN conversion anywhere (confirmed no af_to_mpnn/aa_convert import in mpnn.py), wrong model type masked by # type: ignore[arg-type] (confirmed at mpnn.py:111), sequential_decode_order (mpnn.py:53-61) returns the same jnp.arange identity order as same_random's shim, and never test-executed.",
      "corrected_action": "As given: move from asr's fork (9f42237, deferred imports), not canonical HEAD. Insert af_to_mpnn before scoring; retype to ModelProtocol; resolve the sequential/same_random duplication; repoint utils/fitness.py:11,30's import to the new aminx location in the same phase \u2014 unlisted consumer shared with R33/R34/R35/R37/R38."
    },
    {
      "id": "R37",
      "source": "scoring/esm.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "aminx has zero ESM/PLM infrastructure today (find aminx/src -iname '*esm*' -> 0 hits, confirmed) \u2014 esm.py introduces a new kind of thing to aminx (a sequence-only masked LM) rather than joining an architectural sibling; the genuine fit is a shared weight-loading idiom (hf_hub_download -> eqx deserialize) matching aminx/io/weights.py, not shared model architecture.",
      "destination_evidence": "ls aminx/src/aminx/ -> model/ (singular, 14 files incl. mpnn.py, packer.py, decoder.py) \u2014 no models/; no fitness/ directory exists yet.",
      "reachability": "LIVE via utils/fitness.py:31 'esm': esm.make_esm_score -> get_fitness_function -> runner.py.",
      "confidence": "high",
      "failure_shape": "none new beyond the given b_verdict; independently confirmed the coupling to utils/esm.py is real and runtime (esm.py:39-43 calls load_model then eqx.filter_jit at factory time; :62 calls remap_sequences) and that aminx's directory is model/ singular, not models/.",
      "corrected_action": "As given: utils/esm.py -> aminx/model/esmc.py (not aminx/models/), scoring/esm.py -> aminx/fitness/esm.py; port/quarantine the AbstractFromTorch shims; namespace load_model against aminx/io/weights.py:151's existing load_model; fix remap_sequences's alphabet bug first; repoint the 6 asr sites plus utils/fitness.py:11,31 (unlisted, shared with R33-R36/R38) in the same phase."
    },
    {
      "id": "R38",
      "source": "scoring/cai.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "cai.py is meaningless without codon machinery \u2014 confirmed it imports ECOLI_CODON_FREQ_JAX, ECOLI_MAX_FREQS_JAX, PROTEINMPNN_X_INT from utils/constants.py and nucleotide_to_aa from utils/translation.py, all codon-domain \u2014 so bundling under a proteinsmc/codon/ subpackage is coherent, but that subpackage doesn't exist and none of constants.py/translation.py's moves are in this chunk's rows.",
      "destination_evidence": "src/proteinsmc/codon does not exist (ls error). cai.py itself (70 lines, read in full): cai_score at :46-70 reads PROTEINMPNN_X_INT at :65 as the invalid-codon sentinel.",
      "reachability": "LIVE \u2014 utils/fitness.py:29 registers 'cai': cai.make_cai_score, reachable via get_fitness_function/runner.py.",
      "confidence": "medium",
      "failure_shape": "shape 3 \u2014 PROTEINMPNN_X_INT (read at cai.py:65) is exactly the constant this row's own b_verdict flags as touched by B.2.1's finding that it is misnamed/mis-valued (21 vs the real MPNN X-index of 20) and cannot simply be deleted alongside the alphabet the plan drops elsewhere; this row doesn't state whether codon/cai.py inherits the bug or the fix.",
      "corrected_action": "Move is directionally fine but must be sequenced after (not independent of) the B.2.1 alphabet-constant fix, and must repoint utils/fitness.py:11,29's import to proteinsmc.codon.cai in the same phase \u2014 unlisted consumer shared with R33-R37."
    },
    {
      "id": "R39",
      "source": "oed/__init__.py",
      "verdict": "EXECUTABLE",
      "same_kind_of_thing": "n/a \u2014 bare DELETE, no destination needed.",
      "destination_evidence": "oed/__init__.py read in full: a single docstring, no re-exports, no code.",
      "reachability": "trivially live only at package-import time (harmless side effect of making proteinsmc.oed an implicit namespace package).",
      "confidence": "high",
      "failure_shape": "none.",
      "corrected_action": "none."
    },
    {
      "id": "R40",
      "source": "oed/gp.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "asr/src/asr/oed/gp.py's own docstring/module header states it is 'Adapted from proteinsmc/oed/gp.py,' and both define a GPModel-shaped struct.dataclass consumed the same way (RBF-kernel surrogate over OED features) \u2014 confirmed same kind of thing directly, not merely inherited from B.",
      "destination_evidence": "asr/src/asr/oed/gp.py (confirmed present; header at :1 read). grep for X_mean/X_std in asr/oed/gp.py returns 0 hits; proteinsmc/oed/gp.py:26-27,75,136-146 defines and uses X_mean/X_std for input standardization \u2014 confirmed asr's fork dropped it.",
      "reachability": "both live \u2014 asr's fork is asr's own OED loop; proteinsmc's original is consumed by proteinsmc/oed/experiment.py and oed/run.py.",
      "confidence": "high",
      "failure_shape": "none new; independently reproduces the given b_verdict's addition.",
      "corrected_action": "As given: reconcile, don't overwrite. Explicitly restore X_mean/X_std standardization in asr's fork, or record in writing that its ARD length-scales plus manual log10(mutation_rate) subsume it."
    },
    {
      "id": "R41",
      "source": "oed/structs.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "Not the same kind of thing: OEDDesign (proteinsmc/oed/structs.py:20-35, fields N,K,q,population_size,n_generations,mutation_rate,diversification_ratio,branch_length) parameterizes an SMC sampler run, while ASRDesign (asr/oed/structs.py:24-46, fields N,K,q,lambda_val,mutation_rate,n_leaves,mutation_rate_noise_std,branch_length,use_smc,smc_params) parameterizes a phylogenetic ASR experiment \u2014 only 3 field names overlap. OEDPredictedVariables (5 fields) and ASRPredictedVariables (6 mean + 6 variance fields) share zero field names \u2014 both verified by direct read, not inherited from B.",
      "destination_evidence": "asr/src/asr/oed/structs.py, 77 lines, read in full.",
      "reachability": "both live \u2014 OEDDesign consumed by proteinsmc/oed/opt.py:11-15 perturb_design (hardcodes 6 of its exact field names) and oed/gp.py; ASRDesign is asr's own live experiment config.",
      "confidence": "high",
      "failure_shape": "shape 1 \u2014 a shared class name pattern ('Design'/'PredictedVariables', a shared word) is treated as shared structure, when the two pairs are disjoint in almost every field.",
      "corrected_action": "As given: do not merge into asr/oed/structs.py. Corrected destination asr/oed/smc_design.py (separate module); dedup only OEDFeatureMode (confirmed byte-identical enum: proteinsmc :8-17 vs asr :8-14). Also repoint asr/scripts/analyze_oed.py and diagnose_gp.py (inherited from B, not independently re-verified by me \u2014 flagged as such)."
    },
    {
      "id": "R42",
      "source": "oed/opt.py",
      "verdict": "DO_NOT_EXECUTE",
      "same_kind_of_thing": "Not the same kind of thing its own name claims: calculate_fim_determinant is not a Fisher Information Matrix. Verified directly at proteinsmc/oed/opt.py:47-63 \u2014 fim[i,j] = d_info_i * d_info_j for two forward-difference partials of the SAME scalar (base_prediction.information_gain), which is exactly the rank-1 outer product \u2207g\u2207g\u1d40; the determinant of a rank<=1, n=6 matrix is identically 0 for every design.",
      "destination_evidence": "asr/src/asr/oed/ contains __init__.py, experiment.py, gp.py, padding.py, structs.py \u2014 no opt.py, no FIM/acquisition file present (confirmed via ls).",
      "reachability": "LIVE \u2014 proteinsmc/oed/run.py:19,154 calls calculate_fim_determinant inside the real OED candidate-selection loop (`[calculate_fim_determinant(design, surrogate_model, key) for design in candidate_designs]` feeding an argmax); tests/oed/test_opt.py:3-4,28,33 exercises it directly and also imports train_surrogate_model from proteinsmc.oed.run (confirmed by reading the test file).",
      "confidence": "high",
      "failure_shape": "shape 2 \u2014 the structural presence of a function named calculate_fim_determinant, called from the main loop, is read as proof of semantic payload ('real payload, asr lacks it'), when the computation is degenerate by construction (det \u2261 0).",
      "corrected_action": "DO NOT MOVE. Fix (compute an actual FIM, e.g. per-parameter Jacobian/jvp rather than reusing one scalar's two forward differences) or delete. If deleted, train_surrogate_model (proteinsmc/oed/run.py:108-137) must be preserved/relocated too, since tests/oed/test_opt.py:4 imports it from run.py \u2014 deleting opt.py and run.py together orphans that test, exactly as this row's own b_verdict already notes."
    },
    {
      "id": "R43",
      "source": "oed/phase.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "detect_phase_boundaries() is a generic gradient-threshold-crossing detector on (parameter, metric) arrays; asr/oed/ contains no phase-boundary code today, so there is no destination sibling to compare it against \u2014 the 'same kind of thing' claim can only be tested once SP-6.5's K-mu frontier detector is scoped.",
      "destination_evidence": "asr/src/asr/oed/ contains only experiment.py, gp.py, padding.py, structs.py (ls confirmed) \u2014 no phase.py, no phase-boundary code exists there to receive this file.",
      "reachability": "DEAD in proteinsmc: rg for 'oed.phase|detect_phase_boundaries' across src/ finds only the definition itself; the only importer anywhere is tests/oed/test_phase.py.",
      "confidence": "medium",
      "failure_shape": "none \u2014 the row is not miscategorized, it's genuinely blocked on an unmade decision (as B.3.5+B.4 already found)",
      "corrected_action": "Do not move standalone; land phase.py with the SP-6.5 K-mu frontier work once that program defines what 'boundary' means for its own metric, per B's original qualification."
    },
    {
      "id": "R44",
      "source": "oed/run.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "run.py is a CLI outer-loop (argparse + logging) that imports oed.experiment.run_oed_experiment and oed.tracking's checkpoint/manifest functions directly by name \u2014 it is the same kind of thing as an orchestration entry point, but its two direct dependencies (R46, R47) have contested or destructive destinations that must resolve first.",
      "destination_evidence": "src/proteinsmc/oed/run.py:21-26 imports OEDRecordParams, add_oed_to_metadata, save_oed_checkpoint, save_oed_record from proteinsmc.oed.tracking, and :12 imports run_oed_experiment from proteinsmc.oed.experiment \u2014 both of those source modules are the subjects of R47 (DELETE) and R46 (contested MOVE target).",
      "reachability": "Live within proteinsmc as a documented entry point (module has if __name__=='__main__': main()), but not imported anywhere in asr today (rg for proteinsmc.oed.run in asr found nothing) \u2014 asr would need to newly wire it up.",
      "confidence": "high",
      "failure_shape": "3 \u2014 the plan's own text ('unsequenced') already names this, verified by direct read of the two blocking imports",
      "corrected_action": "Sequence after R46 and R47 are resolved; do not move run.py until its tracking.py and experiment.py imports point somewhere real."
    },
    {
      "id": "R45",
      "source": "oed/nk.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "create_landscape_from_design() returns a proteinsmc.models.nk_landscape.NKLandscape \u2014 a flax.struct.PyTreeNode holding an UNflattened (N, q, q, ..., q) fitness_tables array \u2014 whereas asr's live NK landscape (consumed by the already-vendored smc_tree.py, R48) is trex.nk_model's plain dict PyTree with a FLATTENED (N, q^(k+1)) fitness_tables array; these are not interchangeable without a converter, so nk.py is not 'the same kind of thing' as what asr's live landscape consumers expect.",
      "destination_evidence": "vendor/trex/src/trex/nk_model.py:38-41 returns {'interactions','fitness_tables' (shape (n, n_states**(k+1))),'n_states','k'} \u2014 a dict, flattened table. proteinsmc/src/proteinsmc/models/nk_landscape.py:33-40 NKLandscape(PyTreeNode) holds fitness_tables typed 'N q q ... q' \u2014 unflattened, N+1-dim. proteinsmc/src/proteinsmc/oed/nk.py:27 returns the latter via generate_nk_model.",
      "reachability": "Dead in proteinsmc beyond its own test and oed/experiment.py (rg confirms only those two callers); oed/experiment.py's convert_design_to_config path is itself not what asr's own oed/experiment.py uses (asr's experiment.py imports proteinsmc.oed.smc_tree directly, bypassing nk.py entirely).",
      "confidence": "high",
      "failure_shape": "1 \u2014 'NKLandscape' name/type shared between two repos' NK code was read as interchangeable when the actual runtime representations diverge",
      "corrected_action": "Do not move as-is. Resolve \u00a78 Q4 (NK/landscape ownership) first; if moved, it needs a landscape-format adapter, not a straight port, and currently has zero post-move consumers in asr."
    },
    {
      "id": "R46",
      "source": "oed/experiment.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "Both files are named 'experiment.py' under an oed/ directory, but proteinsmc's version drives NK-only SMC runs through runner.run_experiment + oed.tracking's ArrayRecord manifest, while asr/oed/experiment.py drives ASRDesign-based padded/bucketed landscape generation via trex.nk_model and proteinsmc.oed.smc_tree \u2014 different config types (OEDDesign vs ASRDesign), different landscape sources, different I/O; they are not the same file, only the same filename.",
      "destination_evidence": "asr/src/asr/oed/experiment.py:1-31 defines create_padded_landscape/run_asr_experiment_padded using ASRDesign, ASRPredictedVariables, trex.evals.benchmark, trex.nk_model \u2014 no reference to proteinsmc.oed.experiment or OEDDesign anywhere in the file.",
      "reachability": "asr/oed/experiment.py is live and load-bearing: rg found 4 importers \u2014 scripts/oed_exploration.py, scripts/controlled_sweep.py, scripts/grid_search_oed.py, tests/test_oed_integration.py (all `from asr.oed.experiment import run_asr_experiment_padded`).",
      "confidence": "high",
      "failure_shape": "1 \u2014 filename identity ('experiment.py') mistaken for shared identity; overwriting/merging would destroy a 4-importer live file",
      "corrected_action": "MOVE to asr/oed/smc_experiment.py (a new file), never touch/overwrite asr/oed/experiment.py. Also correct the plan_rationale: convert_design_to_config's fitness_evaluator is a required parameter, not an always-NK hard-wire (the NK-only wiring is 5 lines in run_oed_experiment, oed/experiment.py:100-104, not the general converter)."
    },
    {
      "id": "R47",
      "source": "oed/tracking.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "tracking.py is three distinct systems bundled in one file \u2014 a JSONL manifest+summary (save/load_oed_manifest, create_oed_summary), an optimizer checkpoint (save/load_oed_checkpoint), and an ArrayRecord index (get_run_records_range/get_shared_arrayrecord_path) \u2014 only the first has an obvious analog in a run-tracking tool; I did not independently verify bathos has zero checkpoint/arrayrecord support (out of scope for this pass \u2014 B's claim on that point is unverified by me, cited as-is).",
      "destination_evidence": "n/a \u2014 DELETE action, no destination file to read; bathos itself was not inspected in this pass beyond confirming tracking.py's own three-part structure at oed/tracking.py:21-32 (OEDRecordParams has both record_start_idx/record_count [ArrayRecord] and is passed to save_oed_checkpoint separately [optimizer state]).",
      "reachability": "Live: imported by oed/run.py:21-26, oed/experiment.py:23-26, and scripts/view_oed_tracking.py:11 (all confirmed via rg) \u2014 none of which is itself dead.",
      "confidence": "medium",
      "failure_shape": "3 \u2014 'DELETE, bathos is a third tracking system' is a negative claim (no other consumer) that doesn't hold once the three sub-systems are separated",
      "corrected_action": "Do not blanket-delete. At minimum this breaks R44 and R46's live import chains simultaneously with tracking.py's own removal \u2014 sequence after those, and split rather than delete: only the git-provenance/manifest quarter has a stated bathos destination."
    },
    {
      "id": "R48",
      "source": "oed/smc_tree.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "generate_tree_data_smc / evolve_path_smc consume trex's dict-shaped NK landscape PyTree via trex.nk_model.get_fitness and trex.utils.memory.safe_map \u2014 the exact same landscape convention asr's own generator stack (generator_adapters.py, nk_experiment.py) already uses, and the file is byte-identical between proteinsmc's canonical source and asr's vendored copy, so it is already living in the right place under a different label.",
      "destination_evidence": "diff between /home/marielle/projects/proteinsmc/src/proteinsmc/oed/smc_tree.py and /home/marielle/projects/asr/vendor/proteinsmc/src/proteinsmc/oed/smc_tree.py is empty (byte-identical). asr/pyproject.toml:76 'proteinsmc = { workspace = true }' + :108 'members = [..., \"vendor/proteinsmc\", ...]' resolves proteinsmc imports to that vendored copy at runtime.",
      "reachability": "Live and the most-imported proteinsmc module in asr: rg found imports in src/asr/generator_adapters.py:239, src/asr/nk_experiment.py:26, src/asr/oed/experiment.py:31, tests/test_smc_generator_invariants.py:40, scripts/experiments/sp1_5_smc_neutral_diagnostic.py:48.",
      "confidence": "high",
      "failure_shape": "none \u2014 B's correction (the 'unimportable today' premise is false at runtime via the vendored copy) verified directly by diff+grep",
      "corrected_action": "Action is not a fresh MOVE (the content is already there and already consumed) \u2014 it's DEDUPLICATE: point asr's workspace member at the canonical proteinsmc source (or vice versa) instead of the vendored fork, so there's one copy, not two identical ones."
    },
    {
      "id": "R49",
      "source": "utils/__init__.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "n/a \u2014 same-repo rewrite of proteinsmc's own barrel file, not a cross-repo identity question; blocked purely on sequencing.",
      "destination_evidence": "src/proteinsmc/utils/__init__.py:3-53 re-exports names from exactly the six sibling modules under contest in R50-R55 (annealing, constants, fitness, mutation \u2014 all present in the import list read directly).",
      "reachability": "Live \u2014 this is the package's own __init__, imported transitively by every proteinsmc consumer.",
      "confidence": "high",
      "failure_shape": "none \u2014 this is a legitimate dependency-ordering block, not a B.7 shape 1/2/3 error",
      "corrected_action": "Cannot be scoped or written until R50 (wrong destination path), R51 (unresolved aminx-import-cycle mechanism), R52 (prolix absorption refuted), R53 (landscapes/ target doesn't exist), R54 (chunked_map arg-order break), and R55 (mechanically impossible as scoped) are each resolved \u2014 the shim's content is a direct function of those six outcomes."
    },
    {
      "id": "R50",
      "source": "utils/esm.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "Both are from-scratch Equinox (eqx.Module) implementations of a protein sequence model loaded from HuggingFace-hosted weights via eqx.tree_serialise/deserialise_leaves \u2014 the same idiom aminx/io/weights.py already uses (hf_hub_download-based checkpoint loading) \u2014 so it belongs beside aminx's model code, just not at the stated path.",
      "destination_evidence": "aminx has src/aminx/model/ (singular, 12 files: mpnn.py, encoder.py, decoder.py, etc.) \u2014 no models/ (plural) directory exists (ls confirmed). aminx/src/aminx/io/weights.py:151 already defines 'def load_model(' via huggingface_hub.hf_hub_download (:14,:98) \u2014 the exact function name proteinsmc/utils/esm.py:349 also defines, a real collision.",
      "reachability": "Live in proteinsmc via FITNESS_FUNCTIONS['esm'] (scoring/esm.py imports load_model, remap_sequences from it) and live in asr: rg found 2 direct importers \u2014 src/asr/analysis/biophysics.py:7,16 and src/asr/mtt_training_pipeline.py:25,390 (lower than B's cited count of 6, which I could not reproduce with either py or ipynb search \u2014 flagging as a discrepancy, not a refutation of the underlying break).",
      "confidence": "medium",
      "failure_shape": "2 \u2014 'lives in the sampling repo, should live in the model repo' is a correct structural read, but the destination path and name collision are a shared-WORD-as-shared-THING slip (models/ vs model/, load_model vs load_model)",
      "corrected_action": "Destination should be aminx/model/esmc.py (or a new aminx/models/ dir if the project explicitly wants to introduce that split), and load_model must be renamed on move to avoid colliding with aminx/io/weights.py:151. Also state the charter decision explicitly: this widens aminx from structure-conditioned (MPNN family) to also include sequence-only (ESM-family) models."
    },
    {
      "id": "R51",
      "source": "utils/fitness.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "FITNESS_FUNCTIONS is a plain dict of exactly 4 factories (cai, mpnn, esm, nk \u2014 confirmed at fitness.py:28-33, matching the plan's 'those four imports'); scoring/esm.py's import already reads 'from proteinsmc.utils.esm import load_model, remap_sequences' (scoring/esm.py:10) \u2014 so once R50 moves that module to aminx, this file's import of scoring.esm becomes the aminx edge described, confirming the mechanism, but I did not independently verify B's specific claim about entry_points()-based re-import bypassing a CI grep (would require reading the referenced CI script, out of scope here).",
      "destination_evidence": "src/proteinsmc/scoring/fitness.py:11 'from proteinsmc.scoring import cai, combine, esm, mpnn, nk'; scoring/esm.py:10 imports from proteinsmc.utils.esm (the module R50 relocates).",
      "reachability": "Live \u2014 FITNESS_FUNCTIONS is exported via utils/__init__.py and consumed by get_fitness_function, the runner's dispatch path.",
      "confidence": "medium",
      "failure_shape": "3 \u2014 the plan's claim that a CI grep can catch the aminx-dependency edge was not independently confirmed or refuted by me; deferring to B's finding since the mechanism (lazy entry-point resolution re-importing aminx with no source-visible token) is structurally plausible given the confirmed esm.py import chain",
      "corrected_action": "Do not implement the entry-point registry until a lazy-resolution + sys.modules-based cycle test replaces the stated CI grep, per B's G4."
    },
    {
      "id": "R52",
      "source": "utils/annealing.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "proteinsmc's schedules take (current_step, n_steps, beta_min, beta_max) and return one scalar beta for a specific SMC time-step, ascending beta_min\u2192beta_max; prolix's generate_temperature_ladder takes (n_replicas, min_temp, max_temp) and returns a whole array of Kelvin values indexed by replica, ascending Kelvin \u2014 since beta=1/(kT), ascending Kelvin is DESCENDING beta, the opposite direction, over the opposite axis, returning a vector where proteinsmc expects a scalar \u2014 these are not the same kind of thing.",
      "destination_evidence": "prolix/src/prolix/pt/temperature.py:13-43 generate_temperature_ladder(n_replicas, min_temp, max_temp, geometric) returns Float[...,'n_replicas'] via jnp.linspace/jnp.exp over log(min_temp)..log(max_temp) \u2014 replica-indexed, Kelvin-ascending, no @jit decorator. proteinsmc/src/proteinsmc/utils/annealing.py:76-105 linear_schedule(current_step, n_steps, beta_min, beta_max, _context) is @jit-wrapped, step-indexed, beta-ascending, scalar-returning.",
      "reachability": "Both are live: annealing.py's ANNEALING_REGISTRY is used via get_annealing_function in the SMC sampler; prolix's ladder is used in replica_exchange.py:284 for parallel-tempering setup.",
      "confidence": "high",
      "failure_shape": "2 \u2014 a structural resemblance ('both are schedules over a bounded range') was read as semantic equivalence; the actual axis, arity, and physical-quantity direction all differ, verified directly by reading both files",
      "corrected_action": "Proceed with the proteinsmc-internal rename utils/annealing.py \u2192 proteinsmc/schedules.py on its own merits (it doesn't need prolix). Drop 'absorbs prolix's geometric/linear ladders' from the rationale entirely \u2014 remove prolix's ladder from the consolidation list, as B.2.10 already concluded."
    },
    {
      "id": "R53",
      "source": "utils/nk_landscape.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "utils/nk_landscape.py is the live generation/fitness-evaluation engine for proteinsmc.models.nk_landscape.NKLandscape (a flax PyTreeNode), reachable in production via scoring/nk.py's make_nk_score \u2192 FITNESS_FUNCTIONS['nk'] \u2014 genuinely 'the same kind of thing' as a landscapes/ home, but the target directory proteinsmc/landscapes/ does not exist, and the row's scope excludes models/nk_landscape.py (the NKLandscape class definition itself), splitting one logical unit across an unresolved boundary.",
      "destination_evidence": "find src/proteinsmc -maxdepth 1 -type d lists sampling/, scoring/, models/, utils/, oed/ only \u2014 no landscapes/. scoring/nk.py:12 'from proteinsmc.utils.nk_landscape import NKLandscape, calculate_nk_fitness_single' \u2014 a live registry-facing import, not a dead one.",
      "reachability": "Live via the FITNESS_FUNCTIONS registry (scoring/nk.py:12, used by make_nk_score, registered under 'nk' in fitness.py:32) \u2014 contradicts a reading of this row as dead code; it is the oed/nk.py wrapper (R45) that is dead, not this file.",
      "confidence": "high",
      "failure_shape": "3 \u2014 treating this as 'unused by the only live consumer' (per the memory note attributed to B.3.7) conflates it with oed/nk.py's deadness; utils/nk_landscape.py itself is reachable through the scoring registry, a different, live path",
      "corrected_action": "Do not move until \u00a78 Q4 (NK/landscape ownership, shared with R45) resolves where NKLandscape the class lives \u2014 moving only the utils/ half without models/nk_landscape.py splits one cohesive unit across two locations."
    },
    {
      "id": "R54",
      "source": "utils/mutation.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "mutate()'s offset-mod-q kernel (mutation.py:40-44: mask via uniform<rate, offsets=randint(1,q), (seq+offset)%q) is functionally and textually near-identical to trex/ground_truth.py's mutate() (same shape docstring, same offset-mod-q core with minval=1,maxval=n_states) \u2014 this is not a clean split target, it's an unrecorded fork that already exists at the destination side's own dependency (trex).",
      "destination_evidence": "asr/vendor/trex/src/trex/ground_truth.py:21-51 def mutate(key, sequence, n_states, n_mutations) \u2014 docstring 'Apply random mutations to a population of nucleotide sequences... (shape: (n, nuc_len))' and body 'offsets = random.randint(key_offsets, shape=sequence.shape, minval=1, maxval=n_states); proposed_mutations = (sequence + offsets) % n_states' verbatim-matches proteinsmc/utils/mutation.py:40-43.",
      "reachability": "Live: mutate() is called from oed/smc_tree.py:79 (itself live in asr per R48) and chunked_mutation_step wraps it via chunked_map at mutation.py:217-221.",
      "confidence": "high",
      "failure_shape": "1 \u2014 two independently-named 'mutate' functions with the same signature shape were treated as unrelated when one is a fork of the other, verified by direct textual comparison",
      "corrected_action": "Before splitting mutation.py, reconcile with trex/ground_truth.py's mutate() \u2014 decide which is canonical (trex's takes n_mutations count, proteinsmc's takes a continuous rate \u2014 different APIs over the same kernel) rather than porting a third copy into proposals.PointMutation. Also re-verify the chunked_map tuple-argument order at mutation.py:217-221 against xtrax.transforms.safe_map's actual convention before the swap (I did not independently re-check xtrax's side)."
    },
    {
      "id": "R55",
      "source": "utils/constants.py",
      "verdict": "DO_NOT_EXECUTE",
      "same_kind_of_thing": "n/a \u2014 the row is not a valid same-kind-of-thing move at all; it is a destructive split of a single file whose parts are load-bearing for code the plan does not otherwise touch.",
      "destination_evidence": "constants.py:272-276 builds PROTEINMPNN_TO_ESM_AA_MAP_JAX as jnp.full(AMINO_ACIDS_NUM_STATES=20, ESM_UNK_ID, ...) \u2014 a length-20 array \u2014 while PROTEINMPNN_X_INT=21 (constants.py:115) is used elsewhere as a valid sequence value (mutation.py:76, translation.py:73/95, cai.py:65); indexing a length-20 array at 21 is out-of-bounds and JAX's default clamp mode silently returns index 19, which is restypes[19]=='V' (constants.py:30) \u2014 a real silent Valine-clamp, confirmed by direct inspection of both the map construction and the 22-valued domain it's indexed against.",
      "reachability": "PROTEINMPNN_X_INT is read live by 3 non-constants files (cai.py:65, translation.py:73/95, mutation.py:76) and asserted by 3 test files (test_mutation.py, test_translation.py x3, test_cai.py x2) \u2014 all confirmed via rg; cannot be deleted as the plan proposes.",
      "confidence": "high",
      "failure_shape": "1 \u2014 'protein alphabet, delete it, core only needs q:int' treats AA_CHAR_TO_INT_MAP as a removable shared WORD when it is the constructor of 3 other kept tables (CODON_INT_TO_RES_INT_JAX, ECOLI_MAX_FREQS_JAX, RES_TO_CODON_CHAR) and PROTEINMPNN_X_INT is an active sentinel, not incidental alphabet data",
      "corrected_action": "As B.2.1 concluded: do not delete the protein-alphabet section; fix the mislabeled/wrong-length PROTEINMPNN_TO_ESM_AA_MAP_JAX (real bug, independent of the move) and keep PROTEINMPNN_X_INT + AA_CHAR_TO_INT_MAP in place or re-home them together with their 3 dependent tables, not alone."
    },
    {
      "id": "R56",
      "source": "utils/metrics.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "The row's premise (an 'SMC-intrinsic half' worth moving to kernels/diagnostics.py) doesn't match the file: safe_weighted_mean and calculate_logZ_increment are exported via utils/__init__.py but have zero callers outside their own test file (confirmed by rg across src/ and scripts/) \u2014 dead code, not a live SMC-intrinsic path; separately, no ESS function exists anywhere in the file to move. The 'analysis half' (jeffreys_divergence, calculate_barrier_crossing_frequency, calculate_geometric_fitness_mean) is claimed to belong to asr as 'its only consumer', but asr has zero references to any of these three names (rg across all non-vendor .py in asr) \u2014 the only actual caller is proteinsmc's own oed/experiment.py:155-169, which is itself the subject of the contested R46 move, making this a circular justification.",
      "destination_evidence": "asr repo-wide rg for jeffreys_divergence|calculate_barrier_crossing_frequency|calculate_geometric_fitness_mean|outcome_shannon_entropy returns zero hits outside vendor/. kernels/diagnostics.py does not exist as a checked destination (not located in proteinsmc).",
      "reachability": "safe_weighted_mean/calculate_logZ_increment: dead beyond self-test (metrics.py:25-97, tested only in tests/utils/test_metrics.py, no production caller found). The 5 divergence/entropy/barrier functions: live only via oed/experiment.py, whose own destination is contested (R46).",
      "confidence": "high",
      "failure_shape": "3 \u2014 'asr is its only consumer' is a negative asserted from a narrower search than the claim; a repo-wide rg finds no asr usage at all",
      "corrected_action": "As B.2.5/B.4 concluded: this is a three-way split, not two \u2014 DELETE the dead SMC-intrinsic pair (safe_weighted_mean, calculate_logZ_increment) and the ESS claim (function doesn't exist); the 5 live functions travel with wherever oed/experiment.py's logic actually ends up (asr/oed/smc_experiment.py per R46's correction), not to a standalone asr analysis module with no current callers."
    },
    {
      "id": "R57",
      "source": "utils/memory.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "NOT the same: xtrax.tiling.MemoryBudget (xtrax/src/xtrax/tiling/budget.py:34-52) is a static joint-budget planner needing a caller-supplied estimate callable over a GIVEN AxisDecision sequence, propagating estimator exceptions. proteinsmc's auto_tune_batch_size (utils/memory.py:170-232) empirically wall-clock-benchmarks candidate sizes via time.time()+block_until_ready (memory.py:140-145) to SELECT one. Analytic planner vs empirical tuner - confirms B.2.7 REFUTED by direct read.",
      "destination_evidence": "xtrax/src/xtrax/tiling/budget.py:34-52",
      "reachability": "Live: auto_tune_batch_size etc. exported via utils/__init__.py:29-35,71-96.",
      "confidence": "high",
      "corrected_action": "Retitle REWRITE not DELETE-with-xtrax-destination; pair with models/memory.py per B; wall-clock benchmarking has no MemoryBudget equivalent."
    },
    {
      "id": "R58",
      "source": "utils/jax_utils.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "Same top-level kind but not a name swap: xtrax.transforms.safe_map (map.py:9-38) unconditionally raises ValueError on non-divisible n (map.py:31-35); chunked_map also carries a static_args dict merged per-call (jax_utils.py:26,50-58) safe_map has no equivalent for.",
      "destination_evidence": "xtrax/src/xtrax/transforms/map.py:1-38",
      "reachability": "chunked_map live via mutation.py:217, memory.py:137,142, exported utils/__init__.py:31,81. NEW FINDING: oed/smc_tree.py:8-11 imports `trex` (nk_model, utils.memory.safe_map, types.Adjacency, utils.types.EvoSequence) - trex is absent from proteinsmc/pyproject.toml and no trex package exists under /home/marielle/projects/ except vendored inside asr (asr/vendor/trex). No src file imports oed/smc_tree.py (grep confirmed, including entry point oed/run.py) - smc_tree.py is currently unimportable AND unreachable, though under active edit (commits 406f54d, c9c1616 this week). B's flagged 'third breaking site' at smc_tree.py:290 is real in text but not currently live.",
      "confidence": "high",
      "corrected_action": "Keep B's qualified SPLIT/DELETE; reclassify smc_tree.py site as latent-not-live pending the unresolved trex dependency."
    },
    {
      "id": "R59",
      "source": "utils/jax_uuid.py",
      "verdict": "EXECUTABLE",
      "same_kind_of_thing": "n/a (DELETE) - verified byte-identical to jax_utils.py's copy: jax_uuid.py:16-58 == jax_utils.py:63-105.",
      "destination_evidence": "-",
      "reachability": "Dead: zero non-test importers found.",
      "confidence": "high"
    },
    {
      "id": "R60",
      "source": "utils/pmap_utils.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "NOT the same: xtrax.distributed.sharding (sharding.py:9-56) only builds ShardingPolicy to PartitionSpec pytrees, never touches array data; pmap_utils.distribute (pmap_utils.py:19-58) reshapes real arrays, calls jax.pmap, and unshards results. Confirms B.3.8.",
      "destination_evidence": "xtrax/src/xtrax/distributed/sharding.py:1-80 (no pmap/shard_map call); sampler_base.py:164,185 confirmed builds a real jax.sharding.Mesh.",
      "reachability": "distribute is dead: only reference besides its own file is the re-export at utils/__init__.py:52.",
      "confidence": "high",
      "corrected_action": "DELETE stands but strike the xtrax.distributed.sharding pointer; get_device_mesh should replace sampler_base.py's dead mesh code instead (per B); remove utils/__init__.py:52 export too."
    },
    {
      "id": "R61",
      "source": "utils/translation.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "Partially: nucleotide_to_aa/aa_to_nucleotide (translation.py:47-99) are genuine codon logic via CODON_INT_TO_RES_INT_JAX and fit codon/; but string_to_int_sequence (translation.py:23-43) is a generic string-to-int encoder for either protein or nucleotide strings via a plain dict, no codon logic, doesn't belong bundled there.",
      "destination_evidence": "proteinsmc/codon/ does not exist anywhere in the repo (find src -maxdepth 2 -type d shows no codon dir).",
      "reachability": "Live: imported by runner.py:38, scoring/cai.py:22, utils/initiate.py:9, exported via utils/__init__.py:53.",
      "confidence": "medium",
      "corrected_action": "Split string_to_int_sequence out. Blocking cross-row conflict: R70 (same chunk) says proteinsmc 'deletes its protein alphabet entirely' (AA_CHAR_TO_INT_MAP, PROTEINMPNN_X_INT, CODON_INT_TO_RES_INT_JAX move to proxide) but this row's destination module stays resident in proteinsmc and imports exactly those names from .constants (translation.py:10-15), using PROTEINMPNN_X_INT as its validity sentinel (lines 73,95). R61 and R70 cannot both execute as written without codon/translation.py importing cross-repo from proxide (unstated) or R70's 'deletes entirely' being false.",
      "failure_shape": "1"
    },
    {
      "id": "R62",
      "source": "utils/initiate.py",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "Cannot state - neither destination exists. Searched proteinsmc, xtrax, aminx for a 'proposals' module: found none; only hit is an unrelated local variable `proposals` in proteinsmc/sampling/gibbs.py:74-76. codon/init.py also does not exist.",
      "destination_evidence": "grep -rn proposals across src/, xtrax/src, aminx/src found no owning package; grep -rln tile_init across xtrax/aminx returned nothing.",
      "reachability": "Live: generate_template_population imported by sampling/initialization_factory.py:21, exported via utils/__init__.py:30.",
      "confidence": "high",
      "corrected_action": "Name which repo hosts 'proposals' and its contract before scoping this split; unlike R61's codon/, 'proposals.tile_init' names no owning package anywhere in the ecosystem as it stands."
    },
    {
      "id": "R63",
      "source": "utils/serialization.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "n/a (DELETE) - confirmed live, not conditional-only: create_sampler_output_skeleton imported and called at oed/experiment.py:29,123 today.",
      "destination_evidence": "-",
      "reachability": "Live call site oed/experiment.py:29,123; runner.py:266 and oed/tracking.py:339 still write .arrayrecord files this module reads.",
      "confidence": "high",
      "corrected_action": "Sequence per B: delete only after \u00a73.5's msgpack/SamplerKernel.skeleton() switch lands and replaces oed/experiment.py:123; unconditional DELETE would break it today."
    },
    {
      "id": "R64",
      "source": "utils/blackjax_utils.py",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "n/a (same-repo MOVE) - the stated duplication problem is real (mcmc.py:69-72, hmc.py:90-93, nuts.py:65-70,94-97 each hand-roll a logdensity closure), but make_blackjax_log_prob_fn (blackjax_utils.py:39-44) is not yet that adapter: it returns the raw StackedFitness array unmodified (confirmed by its own test asserting shape (3,), test_blackjax_utils.py:46-51), not the scalar all 3 real sites extract via fitness[0]; and it closes over one fixed key at construction instead of accepting a fresh key per call, whereas all 3 loops fold in a new key every step.",
      "destination_evidence": "proteinsmc/kernels/ does not exist (no kernels dir under src/proteinsmc).",
      "reachability": "Dead: imported only by its own test (test_blackjax_utils.py:10); zero src importers.",
      "confidence": "high",
      "corrected_action": "Rewrite before MOVE/ADOPT: return fitness_fn(...)[0] (scalar) not the raw array; accept key per-call instead of closing over one fixed key.",
      "failure_shape": "2"
    },
    {
      "id": "R65",
      "source": "utils/key_management.py",
      "verdict": "EXECUTABLE",
      "same_kind_of_thing": "n/a (DELETE) - confirmed unused: zero non-test importers found.",
      "destination_evidence": "-",
      "reachability": "Dead, confirmed.",
      "confidence": "high"
    },
    {
      "id": "R66",
      "source": "utils/config_unpacker.py",
      "verdict": "EXECUTABLE",
      "same_kind_of_thing": "n/a (DELETE) - confirmed unused: zero non-test importers found.",
      "destination_evidence": "-",
      "reachability": "Dead, confirmed.",
      "confidence": "high"
    },
    {
      "id": "R67",
      "source": "utils/esm.py (ESM unowned collision)",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "aminx/model/ (confirmed singular) already holds eqx.Module neural-net implementations with weight load/save (mpnn.py, ligand_mpnn.py, decoder.py, encoder.py, packer.py). esm.py's ESMC class (esm.py:299-321) is an eqx.Module with its own create_esmc_skeleton/save_model/load_model (lines 340-498) - same shape of artifact.",
      "destination_evidence": "aminx/src/aminx/model/ contains mpnn.py, ligand_mpnn.py, decoder.py, encoder.py, packer.py; no aminx/models/ (plural) exists; grep -rli esm aminx/src returns nothing, confirming zero existing ESM code.",
      "reachability": "Live: load_model/remap_sequences imported by scoring/esm.py:11, called at line 62, exported via scoring/__init__.py:4 (make_esm_score).",
      "confidence": "high",
      "corrected_action": "Path fix: aminx/model/esmc.py not aminx/models/esmc.py. Also: remap_sequences (esm.py:38-60) indexes PROTEINMPNN_TO_ESM_AA_MAP_JAX, documented (project memory) as allocated length 20 while sequences carry 21, clamping unknown residues to index 19 (Valine) - moving as-is relocates a live bug into the repo (via aa_convert.py:28) whose own conversion code exists to prevent it. Fix before/during move."
    },
    {
      "id": "R68",
      "source": "Memory-budget estimation (4 implementations)",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "n/a (multi-repo) - plan's count confirmed understated: grep for estimate_memory_theoretical/bytes_limit/memory_analysis across aminx/prolix/proteinsmc/xtrax/proxide hit 11 distinct files, well beyond '4 implementations', consistent with B.2.7's direction though I did not re-derive B's exact 5x/3x sub-counts.",
      "destination_evidence": "xtrax/src/xtrax/tiling/budget.py + xtrax/src/xtrax/tiling/estimators.py exist as stated protocol owner.",
      "reachability": "proteinsmc/utils/memory.py confirmed live (see R57).",
      "confidence": "medium",
      "corrected_action": "Replace plan's self-contradictory count language per B; treat as broader consolidation than currently scoped."
    },
    {
      "id": "R69",
      "source": "Length bucketing (4-way; LENGTH_BUCKETS vs SEQ_BUCKETS)",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "DELETE of proteinsmc's copy sound on deadness alone, but 'xtrax owns' overstates fit: xtrax.tiling.bucket is explicitly host-side pre-JIT NumPy by its own docstring (bucket.py:1-19, imports only numpy) while proteinsmc's dead padding.py used jax.numpy device-side (padding.py:18) - confirms B.3.9. asr forked its own independent copy (asr/src/asr/oed/padding.py, confirmed to exist) rather than adopting xtrax's.",
      "destination_evidence": "xtrax/src/xtrax/tiling/bucket.py:1-30; asr/src/asr/oed/padding.py exists as live independent fork.",
      "reachability": "proteinsmc/padding.py confirmed dead: only unrelated word-matches for 'padding' found, zero imports of SEQ_BUCKETS/get_seq_bucket.",
      "confidence": "high",
      "corrected_action": "DELETE stands as written. Drop the 'consolidation achieved' framing - asr's live fork means the duplication isn't actually resolved by this delete."
    },
    {
      "id": "R70",
      "source": "AA alphabet / MPNN\u2194AF permutation (4 vocabularies, 3 permutations)",
      "verdict": "WRONG_DESTINATION",
      "same_kind_of_thing": "Not established: proxide has no single canonical af_to_mpnn/mpnn_to_af to converge onto - found TWO independent implementations inside proxide alone (chem/conversion.py:28,33 and io/parsing/mappings.py:34,40), plus aminx's third copy (utils/aa_convert.py:28,33). Extends B.2.17, which corrected the plan's residues.py location to mappings.py but missed proxide's own internal duplication.",
      "destination_evidence": "proxide/src/proxide/chem/conversion.py:28-33 and proxide/src/proxide/io/parsing/mappings.py:34-40 both independently define af_to_mpnn/mpnn_to_af; proxide/chem/residues.py contains neither.",
      "reachability": "Live and hot: aminx/host/runner.py:956-959,1017-1020,1234-1237 and aminx/host/_sampling_helper.py:570,581 call aminx's own af_to_mpnn on an executed sampling path today.",
      "confidence": "high",
      "corrected_action": "BLOCKED pending proxide first consolidating its own two copies before other repos delete in its favor. Cross-reference R61: if this row executes as stated, R61's codon/translation.py (staying in proteinsmc) loses the constants it imports - unresolved ordering conflict between these two rows in the same chunk."
    },
    {
      "id": "R71",
      "source": "proteinsmc/src/proteinsmc/types.py (full file, 51 lines)",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "Only ~5 of proteinsmc's names overlap proxide's (ArrayLike, Scalar, ScalarFloat, ProteinSequence, PRNGKey) \u2014 proxide/src/proxide/core/types.py is dominated by atom/physics/topology types (Coordinates, Charges, Bonds, CmapCoeffs, SystemParams TypedDict, etc., lines 16-141) that proteinsmc has no use for, so KEEP is defensible on substance, but the file is 51 lines not '~30', and the 'unresolved by design... that edge is the one being cut' framing is contradicted by proteinsmc/src/proteinsmc/scoring/mpnn.py:17 already importing `proxide.ops.dataset` today (optionally gated on `find_spec(\"prxteinmpnn\")`, itself a live/reachable optional-dependency pattern, not dead code) \u2014 the proteinsmc\u2192proxide edge is not being cut, it already exists for a different concern.",
      "destination_evidence": "proxide/src/proxide/core/types.py:16-141 (structural/physics types); proteinsmc/src/proteinsmc/scoring/mpnn.py:14-17 (existing proxide import, optionally gated); proteinsmc/src/proteinsmc/scoring/__init__.py:5 (make_mpnn_score exported, so the gated import path is reachable, not dead)",
      "reachability": "types.py is proteinsmc's global type module, imported throughout \u2014 live. mpnn.py's proxide import is optionally gated (`PRXTEINMPNN_AVAILABLE`) but make_mpnn_score is exported at package level (scoring/__init__.py:5) and raises explicitly when unavailable (mpnn.py:80) rather than silently no-op \u2014 this is a legitimate optional-dependency gate, not the dead-behind-a-failing-gate shape Addendum C warns about.",
      "confidence": "medium",
      "corrected_action": "KEEP local aliases (correct on substance \u2014 real overlap is ~5 names, not worth an import of a physics-dominated module), but correct the rationale: proteinsmc/types.py is 51 lines not ~30, and drop 'that edge is the one being cut' \u2014 proteinsmc already imports proxide directly (scoring/mpnn.py:17) for a different purpose, so the type-vocabulary KEEP is an independent, narrower decision, not evidence of a clean boundary cut."
    },
    {
      "id": "R72",
      "source": "proteinsmc/src/proteinsmc/sampling/{mcmc,nuts,hmc,gibbs}.py io_callback params + io.py writer callback",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "xtrax.stages.AxisBoundary (xtrax/src/xtrax/stages/boundaries.py:84-99) bundles an optional fuse/tap/sink per axis specifically to replace hand-threaded io_callback-style side-effect params, and aminx already consumes exactly this mechanism for its own pipeline boundaries (aminx/src/aminx/host/plan.py:487-503, `stage_set.axis_boundaries` + `xtrax.stages.validate_plan_topology`) \u2014 same purpose as proteinsmc's per-run_fn `io_callback: Callable | None` params.",
      "destination_evidence": "xtrax/src/xtrax/stages/boundaries.py:84-99 (AxisBoundary); aminx/src/aminx/host/plan.py:487-503 (live consumer); proteinsmc/src/proteinsmc/sampling/mcmc.py:28, nuts.py:41, hmc.py:34, gibbs.py:99,114 (the 4 io_callback params); proteinsmc/src/proteinsmc/runner.py:315 (single call site)",
      "reachability": "The mechanism (AxisBoundary) is live and reachable in xtrax/aminx. But proteinsmc's own io_callback-bearing run_fns are currently reachable only via direct unit tests (tests/sampling/test_{mcmc,hmc,nuts}_loop.py) \u2014 runner.py:315 calls `run_fn` exactly once in the whole file, gated to `sampler_type==\"smc\"` only (see R80), so mcmc/nuts/hmc/gibbs's io_callback wiring is dead from the polymorphic entry point today.",
      "confidence": "medium",
      "corrected_action": "Consolidate onto xtrax.stages.AxisBoundary, but (1) there is no 'drive.sample()' anywhere in aminx or proteinsmc \u2014 grep confirms zero matches for that name in either repo; the real consolidation site is proteinsmc's per-sampler run_fn signatures (mcmc.py, nuts.py, hmc.py, gibbs.py) plus io.py's writer-callback setup, and (2) this should be sequenced after R80's runner.py dispatch fix, since 4 of the 5 io_callback-bearing run_fns aren't currently reachable from run_experiment at all."
    },
    {
      "id": "R73",
      "source": "proteinsmc/src/proteinsmc/oed/smc_tree.py (334 lines)",
      "verdict": "DO_NOT_EXECUTE",
      "same_kind_of_thing": "Not the same kind of thing: xtrax.tiling.DedupSpec (xtrax/src/xtrax/tiling/dedup.py:47) and its reference prolix/src/prolix/api/ensemble_dedup.py (build_dedup_spec_by_shape, lines 55-72) group inputs by a shape key and compute ONCE per unique key, gathering results back to all members \u2014 a genuine compute-once/gather mechanism. smc_tree.py's generate_tree_data_smc does the opposite: its own docstring (line 184) says it uses SMC 'with redundant parallelism', and evolve_path_smc's docstring (lines 30-32) explains shared RNG keys exist only so that redundantly-recomputed branches reproduce identically \u2014 every leaf path is independently re-run through safe_map (lines 284-298) with no compute-once/gather step anywhere in the file.",
      "destination_evidence": "proteinsmc/src/proteinsmc/oed/smc_tree.py:8 (`from trex import nk_model`, unconditional top-level import), lines 30-32 and 184 (redundant-parallelism docstrings), lines 284-298 (safe_map over every leaf path, no dedup/gather); xtrax/src/xtrax/tiling/dedup.py:47 and prolix/src/prolix/api/ensemble_dedup.py:55-72 (actual dedup mechanism, for contrast)",
      "reachability": "DEAD on two independent grounds: (1) `trex` is not a declared dependency anywhere in proteinsmc/pyproject.toml or uv.lock, and `uv run --no-sync python -c \"import trex\"` fails with `ModuleNotFoundError: No module named 'trex'` in proteinsmc's own environment; (2) smc_tree.py is never imported from oed/__init__.py (empty docstring-only file) or oed/run.py (its imports don't mention smc_tree) or anywhere else in src/ (grep found zero references outside the file itself).",
      "confidence": "high",
      "corrected_action": "Do not move this as a 'dedup-gather mechanism' \u2014 there is no dedup-gather in the source to move; the actual behavior (redundant per-leaf recomputation with shared RNG keys) is a different mechanism entirely. Separately, fix or remove the unconditional `from trex import nk_model` (smc_tree.py:8) before this file can even be imported \u2014 trex is not a proteinsmc dependency. If asr genuinely wants this file's redundant-parallelism logic, that's a plain file move with no DedupSpec mapping; the DedupSpec/dedup-gather framing should be dropped from the row entirely."
    },
    {
      "id": "R74",
      "source": "proteinsmc/src/proteinsmc/io.py:27-50 (get_git_commit_hash), :53 (create_metadata_file); proteinsmc/src/proteinsmc/oed/tracking.py (339 lines)",
      "verdict": "EXECUTABLE",
      "same_kind_of_thing": "bathos/src/bathos/git.py:18 `capture_git_state()` is bathos's own purpose-built git-provenance capture function \u2014 the same kind of thing as proteinsmc's ad hoc `get_git_commit_hash()` subprocess shell-out (io.py:27-50), just centralized with a dedicated module (bathos also has provenance.py, cite.py) rather than duplicated per-project.",
      "destination_evidence": "bathos/src/bathos/git.py:9,18 (GitState class, capture_git_state); proteinsmc/src/proteinsmc/io.py:27-53 (duplicate local implementation); proteinsmc/src/proteinsmc/runner.py:253 (create_metadata_file call site, confirming reachability)",
      "reachability": "io.py:create_metadata_file is called from runner.py:253, the live entry point \u2014 reachable and exercised on every run. oed/tracking.py mixes reachable provenance functions (add_oed_to_metadata:272) with checkpoint/ArrayRecord functions (save_oed_checkpoint:134, load_oed_checkpoint:177, get_next_record_index:108) that are a distinct concern (resumption/manifest state, not git-SHA provenance) \u2014 confirms B.3.10's split is real, not artifactual.",
      "confidence": "high",
      "corrected_action": "None \u2014 row as corrected by B (one library-code site + scripts/ uses; tracking.py's provenance quartet moves, checkpoint/ArrayRecord halves do not) is confirmed by direct reading."
    },
    {
      "id": "R75",
      "source": "xtrax/src/xtrax/loop/ (26 python files, not part of proteinsmc)",
      "verdict": "EXECUTABLE",
      "same_kind_of_thing": "N/A \u2014 this is not a proteinsmc file being moved anywhere; it is xtrax's own internal governance subsystem. 'OUT OF SCOPE' correctly means no move-table action applies to proteinsmc for this row.",
      "destination_evidence": "xtrax/src/xtrax/loop/ confirmed to contain 26 .py files (admission.py, attestation_evidence_gate.py, campaign_approval_gate.py, ...) via direct `find`/`ls`",
      "reachability": "N/A to proteinsmc \u2014 this concerns xtrax's own architecture, not a proteinsmc source file to migrate.",
      "confidence": "high",
      "corrected_action": "None \u2014 correctly scoped as out of scope for a proteinsmc move table."
    },
    {
      "id": "R76",
      "source": "proteinsmc/src/proteinsmc/utils/key_management.py",
      "verdict": "EXECUTABLE",
      "same_kind_of_thing": "N/A (DELETE, not a move) \u2014 confirmed dead: `grep -rn key_management src/proteinsmc` finds only the file's own path, no importers anywhere in src/.",
      "destination_evidence": "proteinsmc/src/proteinsmc/utils/key_management.py exists; proteinsmc/tests/utils/test_key_management.py exists (tested); zero grep hits for `key_management` elsewhere in src/proteinsmc",
      "reachability": "Unreachable from any production code path \u2014 only reachable via its own dedicated test file, confirming 'tested, imported nowhere' as stated in the row.",
      "confidence": "high",
      "corrected_action": "None."
    },
    {
      "id": "R77",
      "source": "proteinsmc/src/proteinsmc/oed/nk.py, utils/nk_landscape.py, models/nk_landscape.py, scoring/nk.py (346 lines total)",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "proteinsmc's NKLandscape (models/nk_landscape.py:33-44) is an immutable `flax.struct.PyTreeNode` with typed array fields (`interactions`, `fitness_tables`); trex's `create_nk_model_landscape` (asr/vendor/trex/src/trex/nk_model.py:17-43) returns a plain Python dict (`{\"interactions\":..., \"fitness_tables\":..., \"n_states\":..., \"k\":...}`) \u2014 a representationally different kind of thing. This is a real blocker, not a hypothetical: asr scripts already key into the dict form (asr/scripts/generate_nk_data.py:51-52 `landscape[\"interactions\"]`), which is illegal attribute access against proteinsmc's frozen dataclass.",
      "destination_evidence": "proteinsmc/src/proteinsmc/models/nk_landscape.py:33-44 (frozen PyTreeNode); asr/vendor/trex/src/trex/nk_model.py:17-43 (dict-returning `create_nk_model_landscape`); asr/scripts/generate_nk_data.py:51-52 (dict-key access in the wild). Note: no top-level `/home/marielle/projects/trex` repo exists \u2014 `trex` is archived (`archive/united_workspace/trex`) and vendored inside asr (`asr/vendor/trex`); `proteinsmc.landscapes` does not exist anywhere (confirmed via `find` for `landscapes` and `nk` files) \u2014 the row's recommended destination path is aspirational, not live.",
      "reachability": "proteinsmc's NK files (oed/nk.py, utils/nk_landscape.py, models/nk_landscape.py, scoring/nk.py) are live and imported from oed/nk.py's create_landscape_from_design. asr's trex usage is live (13+ scripts reference it). Both sides are reachable; the incompatibility is representational, not a dead-code issue.",
      "confidence": "high",
      "corrected_action": "Row's own framing ('DECIDE before phase 3') is correct and my independent read confirms the blocker is real: proteinsmc's frozen-dataclass NKLandscape and trex's mutable dict cannot be unified by renaming/importing alone \u2014 a genuine adapter or one-sided rewrite is needed before 'port smc_tree onto proteinsmc.landscapes.nk' is executable, and proteinsmc.landscapes doesn't exist yet either."
    },
    {
      "id": "R78",
      "source": "proteinsmc's nucleotide/codon files (translation.py, constants.py, cai.py, mutation.py, models/translation.py, sequence_type threading)",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "A naive `grep -rln 'nucleotide|codon' src/proteinsmc` (excluding tests) returns exactly 13 files, matching the plan's undercount and consistent with B.3.15's claim that the real count (19) is missed by surface search. Separately, proxide is not collision-free as claimed: `crates/proxide-core/proto/ccd_chem_comp_types.proto:6` documents a CCD-derived residue-type table explicitly built to 'classify a residue name as a peptide/nucleotide/other component' \u2014 real nucleotide-adjacent content in proxide that a plain grep of proxide's Python sources alone misses (it lives in the Rust/proto layer), corroborating B.3.16's refutation of 'collides with nothing in the ecosystem.'",
      "destination_evidence": "proxide/crates/proxide-core/proto/ccd_chem_comp_types.proto:1-17 (peptide/nucleotide/other classifier, atom-indexed CCD content); direct grep count of 13 nucleotide/codon-bearing files in proteinsmc/src (undercounting confirmed structurally consistent with B.3.15's 19-file claim, though I did not independently enumerate all 19)",
      "reachability": "Not separately re-verified in full \u2014 I did not trace every one of B's claimed 19 files or the asr vendor-copy/trex-mutate() collision claims within this pass's time budget; my spot checks corroborate rather than contradict B.2.15/B.3.15/B.3.16.",
      "confidence": "low",
      "corrected_action": "Defer to B's corrected framing: KEEP-quarantined is still likely right in spirit, but the row's own rationale ('collides with nothing in the ecosystem', '13 files') is confirmed wrong on both counts by my independent spot check \u2014 file count and the collision-free claim both need correction per B.3.15/B.3.16 before this row can be marked EXECUTABLE as written."
    },
    {
      "id": "R79",
      "source": "aminx.registry.SAMPLERS (aminx/src/aminx/registry.py:51)",
      "verdict": "BLOCKED",
      "same_kind_of_thing": "aminx's SamplerFn protocol (aminx/src/aminx/types/protocols.py:69-80) is `(prng_key, bundle, config, stage_set) -> (sequence, logits, decoding_order)` \u2014 a single conditional-decode call over a fixed structural bundle with no generational/population state threaded through, confirming the row's premise that SAMPLERS is 'decode-shaped, not population-shaped' against aminx's own source.",
      "destination_evidence": "aminx/src/aminx/registry.py:51 (SAMPLERS registry); aminx/src/aminx/types/protocols.py:69-80 (SamplerFn protocol signature)",
      "reachability": "SAMPLERS registry and SamplerFn protocol are live, in-use aminx types (registry.py:37-45 register/get methods, consumed elsewhere in aminx's host layer).",
      "confidence": "medium",
      "corrected_action": "The DECODERS rename is technically well-founded against aminx's own SamplerFn signature (confirmed decode-shaped), but per B's G11 it sits downstream of an unresolved architectural question (\u00a71's premise about whether aminx can host an iterative sampler at all) \u2014 do not execute until that upstream boundary question is re-argued/resolved, even though the naming rationale itself checks out."
    },
    {
      "id": "R80",
      "source": "proteinsmc/src/proteinsmc/runner.py:54-79 (SAMPLER_REGISTRY), :303-324 (dispatch)",
      "verdict": "EXECUTABLE",
      "same_kind_of_thing": "N/A (bugfix in place, not a move) \u2014 confirmed directly: SAMPLER_REGISTRY has 6 entries (smc, parallel_replica, gibbs, mcmc, hmc, nuts; runner.py:54-79), but `run_fn(...)` is invoked exactly once in the entire file (runner.py:315), inside `if config.sampler_type == \"smc\":` only. `final_state = None` is set unconditionally at line 303 and never reassigned for the other 5 types.",
      "destination_evidence": "proteinsmc/src/proteinsmc/runner.py:54-79, 303-328",
      "reachability": "The bug is on the sole polymorphic entry point (run_experiment \u2192 runner.py), so it's live and hit by any caller selecting a non-'smc' sampler_type. Confirmed more severe than 'validates then no-ops': the other 5 run_fns are never called at all via this path (only reachable via direct unit tests per-sampler, e.g. tests/sampling/test_mcmc_loop.py), not merely validated-then-dropped.",
      "confidence": "high",
      "corrected_action": "None \u2014 B.3.18's finding confirmed and, if anything, understated: it's not just final_state staying None, the underlying run_gibbs_loop/run_mcmc_loop/run_hmc_loop/run_nuts_loop/run_prsmc_loop functions are never invoked by run_experiment for those 5 registered types."
    },
    {
      "id": "R81",
      "source": "N/A \u2014 new function, does not exist yet in proteinsmc or aminx",
      "verdict": "EXECUTABLE",
      "same_kind_of_thing": "aminx already has exactly the building blocks this new function's signature needs: ConditionalDecodeStep/UnconditionalDecodeStep/StageSet (aminx/src/aminx/types/stages.py:249,330,389) for the 'decoder'/'stages' args, and aminx's SamplerFn protocol (protocols.py:69-80, see R79) already returns a proposal-shaped `(sequence, logits, decoding_order)` tuple \u2014 decoder_proposal would just be a thin wrapper producing a `Proposal` object from what aminx already computes.",
      "destination_evidence": "aminx/src/aminx/types/stages.py:249 (ConditionalDecodeStep), :330 (UnconditionalDecodeStep), :389 (StageSet); aminx/src/aminx/types/protocols.py:69-80 (SamplerFn, proposal-shaped output)",
      "reachability": "N/A (new code) \u2014 grep confirms `decoder_proposal` and a `Proposal` class do not exist anywhere in proteinsmc or aminx today, consistent with the row's 'ADD (new function)' action; no dead-code or reachability concern applies.",
      "confidence": "medium",
      "corrected_action": "None \u2014 the row is honestly framed as new code, and the destination has the exact prerequisite types to build on."
    },
    {
      "id": "R82",
      "source": "proteinsmc/src/proteinsmc/utils/jax_utils.py:22 (chunked_map), utils/pmap_utils.py:19 (distribute), padding.py:25 (SEQ_BUCKETS)",
      "verdict": "EXECUTABLE_WITH_CHANGES",
      "same_kind_of_thing": "Confirmed all three exist as claimed. Read prolix/src/prolix/tiling/xtrax_adapter.py directly (179 lines): it converts prolix's OWN AxisSpec (`prolix.tiling.planner.AxisSpec`) into xtrax's AxisSpec (`xtrax.tiling.plan.AxisSpec`, imported as `XtraxAxisSpec` at line 17) via `prolix_axis_to_xtrax()` (line 34) \u2014 i.e. prolix itself maintains a distinct, domain-specific AxisSpec dialect and adapts it at the xtrax boundary rather than using xtrax's AxisSpec directly. This is direct, independent confirmation that modeling proteinsmc/_xtrax.py on this file would reproduce a third incompatible AxisSpec dialect, not achieve a clean consolidation, exactly as B.3.18 warned.",
      "destination_evidence": "prolix/src/prolix/tiling/xtrax_adapter.py:3-21,34-47 (prolix_axis_to_xtrax conversion function, proving prolix keeps its own AxisSpec); proteinsmc/src/proteinsmc/utils/jax_utils.py:22, pmap_utils.py:19, padding.py:25 (the three duplicated concerns, confirmed present)",
      "reachability": "All three source utilities exist in live proteinsmc source under utils/ and padding.py at repo root of src/proteinsmc; not independently traced for call-site reachability within this pass's budget.",
      "confidence": "medium",
      "corrected_action": "DELETE the three stands (confirmed real, unnecessary triplication of the concern). The 'REWRITE as ~150-line adapter modeled on prolix' framing should carry forward the caveat, confirmed by direct reading, that prolix's own reference file is itself a translation between two incompatible AxisSpec dialects, not a single unified one \u2014 so proteinsmc/_xtrax.py would be a third dialect+adapter pair, not a clean win, and 'xtrax owns the concern' should be dropped per B.3.9."
    }
  ],
  "raw_challenges": [
    {
      "id": "R02",
      "survives": false,
      "downgraded_to": "EXECUTABLE_WITH_CHANGES (not EXECUTABLE)",
      "evidence": "bathos/src/bathos/git.py:15,18,20-39; proteinsmc/src/proteinsmc/io.py:27-50; proteinsmc/tests/io/test_io.py:42-64; proteinsmc/pyproject.toml:10-25; plan 260813_proteinsmc-ecosystem-partition.md:250 vs :435",
      "objection": "The load-bearing sentence \u2014 bathos's git.py is a 'strict superset of what's being removed' \u2014 is false in four independently checkable ways, and it is a shape-1 error: 'captures the git hash' is a shared WORD, not a shared contract. (1) SENTINEL: io.py:27 returns `str | None` and returns None on every failure path (:36, :50); bathos returns `_UNKNOWN = GitState(hash=\"unknown\", ...)` (git.py:15, :39) and can never return None. metadata.json's `git_commit_hash` (io.py:85) therefore flips from `null` to the string \"unknown\" on any machine without git or outside a repo \u2014 a SILENT change to a provenance field, and it contradicts three tests that assert `commit_hash is None` (test_io.py:46, :55, :64). (2) MISSING CAPABILITY, so not a superset: io.py:41-47 passes `timeout=5` and catches `subprocess.TimeoutExpired` (:49); bathos uses bare `subprocess.check_output` with no timeout (git.py:20-36) and does not catch TimeoutExpired (:38) \u2014 a hung git blocks run_experiment forever. tests/io/test_io.py:58-64 exists specifically to pin that timeout behaviour, so this is a contracted capability being dropped, not a refactor. (3) MUTABLE DEFAULT: `def capture_git_state(cwd: Path = Path.cwd())` (git.py:18) binds the CWD at IMPORT time, not call time. io.py's subprocess.run inherits the process CWD at CALL time. Every existing bathos caller passes an explicit cwd (checker.py:65, decorators.py:38, postmortem.py:257, mcp.py:2213) \u2014 proteinsmc would be the FIRST caller to hit this trap, and it would hit it silently. (4) UNDECLARED DEPENDENCY, unverified as runnable per rule C: `bathos` appears nowhere in proteinsmc/pyproject.toml:10-25, and /home/marielle/projects/proteinsmc/.venv/lib/python3.13/site-packages contains only virtualenv bootstrap files \u2014 bathos is neither declared nor installed, so 'delegate to bathos' is not a one-function edit, it adds a new runtime dependency edge (sampling library -> experiment-tracking tool) that the stated separation of concerns does not contain. (5) The plan contradicts itself about the action: :250 says the shell-out goes '\u2192 bathos', :435 says it is 'deleted'. The clearance flattened two incompatible instructions into one. Verified-and-unbroken parts: get_git_commit_hash IS the sole git shell-out in io.py (subprocess imported once at :7, used once at :41); reachability holds \u2014 runner.py:51 imports create_metadata_file and calls it unconditionally at runner.py:253, itself called from oed/experiment.py:27,111. Required changes: pass an explicit cwd; preserve the `str | None` sentinel by mapping GitState.hash==\"unknown\" -> None or accept and document the metadata format change; restore a timeout; add bathos to pyproject; resolve the :250/:435 contradiction before editing."
    },
    {
      "id": "R03",
      "survives": true,
      "objection": "Attack failed \u2014 I could not break the DELETE, but the row's correction is aimed at the wrong repo. I re-ran the deadness negative far wider than the clearance did (which searched only this worktree's src/tests/scripts): rg for `proteinsmc.padding|from .padding|import padding` across ALL of /home/marielle/projects returns only prolix/src/prolix/__init__.py:8-9 (its own unrelated prolix.padding: PaddedSystem/bucket_proteins/pad_protein) and a cryptography import in praxia. rg for every symbol the file defines (pad_population|pad_sequence|create_particle_mask|create_sequence_mask|masked_mean|masked_sum|get_seq_bucket|MAX_POPULATION|SEQ_BUCKETS|MAX_SEQ_LEN) across proteinsmc (main checkout, not just the worktree) and asr returns only padding.py's own definitions plus unrelated files. Zero importers anywhere, including asr/vendor/proteinsmc and .ipynb notebooks. Dead confirmed. TWO corrections to the row's rationale, both shape-1 misattributions: (a) the real prior-art home is aminx, not xtrax \u2014 aminx/src/aminx/tiling/buckets.py:1-11 is a near-verbatim descendant of this file's docstring ('Padding utilities for static-shape JAX compilation in Aminx' vs proteinsmc/src/proteinsmc/padding.py:1 '...in ProteinSMC'), carries LENGTH_BUCKETS (buckets.py:19) against SEQ_BUCKETS (padding.py:25), and DOES have masked_mean (buckets.py:103). The clearance's own failure_shape asserts 'no masked_mean/masked_sum counterpart' at the destination \u2014 true of xtrax (rg finds neither name anywhere in /home/marielle/projects/xtrax) but false of aminx, and the clearance admits it did not re-read. (b) asr/src/asr/oed/padding.py is a LIVE fork of the same code (pad_sequence:59, create_sequence_mask:97, masked_mean:183, masked_sum:196) actually called at asr/src/asr/oed/experiment.py:23,62,136 \u2014 deleting proteinsmc's copy does not touch it (separate file, separate package), but the row should not leave the impression that this functionality is being retired from the ecosystem. The DELETE stands on deadness alone, as the row says."
    },
    {
      "id": "R04",
      "survives": true,
      "objection": "Attack failed on the substance. I read runner.py directly and every cited line is exact: `final_state = None` at :303, `if config.sampler_type == \"smc\":` at :304 is the only branch that assigns it, `if final_state is not None:` at :326 skips the block, and :332 logs 'Run completed successfully.' regardless \u2014 so gibbs/mcmc/hmc/nuts/parallel_replica pass _validate_config (:176-191, all six keys present in SAMPLER_REGISTRY :54-79), execute no sampler, write an empty ArrayRecord (writer opened :268, closed :334), and exit 0. Silent, not loud. Confirmed. I also re-ran the 'asr never calls it' negative WIDER than the clearance (which searched asr/src, asr/scripts, asr/tests only): rg for `proteinsmc.runner|proteinsmc import runner|run_experiment` across the ENTIRE asr tree including .ipynb notebooks and asr/vendor returns no import of proteinsmc.runner; every asr `run_experiment` hit (scripts/pdz_experiments.py:141, scripts/experiments/sp5_1..sp5_4, tests/test_oed_integration.py:43) is asr's own locally-defined function of the same name \u2014 a name collision, correctly excluded. The negative survives. TWO additions the row must carry. (1) SECOND PHYSICAL COPY, and it is the one asr actually loads: asr/pyproject.toml:108 declares `members = [..., \"vendor/proteinsmc\", ...]` and :76 `proteinsmc = { workspace = true }`, with :70-71 describing it as a 'vendored + patched clone'. asr resolves `import proteinsmc` to /home/marielle/projects/asr/vendor/proteinsmc, a separate git checkout. `diff -q` shows its runner.py and io.py are currently byte-identical to this repo's, so the phase-0 fix does NOT reach asr until the vendor is re-bootstrapped (scripts/bootstrap_vendor_proteinsmc.sh) \u2014 state that in step (a) or asr silently keeps the no-op. (2) The phase-0 fix is test-compatible: tests/test_runner.py:38 asserts only that the registry advertises the six names, not that they execute, so raising NotImplementedError for the five unimplemented branches breaks no existing test. The \u00a78 Q2 gate on step (b) is real \u2014 `find src -name api.py`, `-name drive.py`, `-type d -name kernels` all return nothing."
    },
    {
      "id": "R05",
      "survives": true,
      "objection": "Attack failed \u2014 the REWRITE relabel is right and I confirmed it against both sides \u2014 but the corrected_action is materially incomplete in a way that reproduces the failure it names. Confirmed: src/proteinsmc/types.py is 51 lines (row says 52) and holds exactly ArrayLike:10, Scalar:13, ScalarFloat:14, NucleotideSequence:17, ProteinSequence:18, AminoAcidSequence:19, CodonSequence:20, OneHotAminoAcid:21, EvoSequence:23, BatchEvoSequence:24, SequenceType:26, UUIDArray:27, Vmapped:31, VmappedFitnessFunc:39, VmappedTranslation:42, PRNGKey:51. Of the plan's keep-list only PRNGKey exists (:51); Population/Relaxed/Scores/LogWeights/Ctx appear only in the plan's proposed code block (plan:94-101), nowhere in any repo. Disjoint vocabulary, so 'STAY, trimmed' is indeed shape 1. THREE things the row misses. (1) A SECOND types.py, and it is live: src/proteinsmc/models/types.py re-exports 15 of the 16 names from proteinsmc.types (:5-21) with a matching __all__ (:23-39), and models/__init__.py:22-26 pulls EvoSequence/NucleotideSequence/ProteinSequence out of it into the public `proteinsmc.models` namespace. Its docstring says 'Legacy ... now redirected' (models/types.py:1) \u2014 do NOT read that structural signal as deadness (shape 2): models/__init__.py:22 imports it at runtime, and models/fitness.py:12 imports EvoSequence FROM it. Any types.py rewrite has a second blast radius through this shim plus models.__all__. (2) The importer set is 9 modules, not the two the row names: utils/translation.py:20, utils/metrics.py:17, utils/esm.py:26-27, utils/annealing.py:20, sampling/nuts.py:28, sampling/hmc.py:20, sampling/mcmc.py:20, sampling/gibbs.py:21, models/types.py:5 \u2014 and the most-imported names are ArrayLike and ScalarFloat, the two the row explicitly leaves with 'no stated fate'. Any disposition table that starts from Protein/NucleotideSequence is starting from the rare cases. (3) ALPHABET CONVENTION, unstated at the destination \u2014 the exact B.7 remedy this row skipped: the plan's replacement `Population = Int[Array, \"n L\"]  # int8, values in [0, q)` (plan:94) fixes an ALPHABET SIZE but no ALPHABET ORDERING, while the plan's own header (:16-20) records that proteinsmc encodes in AlphaFold order under ProteinMPNN-named constants and that this is a live silent-corruption bug already reaching asr (utils/constants.py:119 AMINO_ACIDS_NUM_STATES = 20, :273 commented 'Size is based on ColabDesign's max AA int'). A cross-repo contract type that pins q but not the ordering hard-codes that bug into the interface. The rewrite must state the ordering."
    },
    {
      "id": "R07",
      "survives": true,
      "objection": "Attack failed on the runtime-deadness claim \u2014 I pushed on exactly the rule-C angle the clearance did not \u2014 but the row's action is incomplete and would leave the tree failing its own documented gates. What I could not break: models/protocols.py:16 FitnessFn / :57 MutationFn / :94 AnnealingFn are referenced from exactly one place, sampling/particle_systems/smc.py:22, inside `if TYPE_CHECKING:` (:17) under `from __future__ import annotations` (:3). The obvious way this 'dead' claim could be wrong is a runtime annotation evaluator \u2014 jaxtyping's beartype hook, typeguard, or typing.get_type_hints \u2014 which would resolve those strings and raise NameError after deletion. rg for `jaxtyped|beartype|typeguard|get_type_hints|install_import_hook` across all of src/proteinsmc plus pyproject.toml and tests/conftest.py returns ZERO hits, so nothing ever evaluates them. Runtime-dead confirmed. The contract difference is also real as stated: protocols.py:51 types the third arg `context: dict[str, Array | float | int] | None` while models/fitness.py:17 types it `PyTree | Array | None` \u2014 same name, different contract, correctly refused as an alias swap. WHAT THE ROW GETS WRONG: it cites 'smc.py:17,23' but the import is at :22 alone, and it treats this as a single-file delete. It is not. Deleting protocols.py orphans NINE annotation sites in smc.py that name those types \u2014 :50, :79, :80, :81, :101, :102, :103, :169, :170, :172 \u2014 which are runtime-inert but NOT tool-inert: pyproject.toml:47 sets `select = [\"ALL\"]` (F821 undefined name) and CLAUDE.md documents `uv run ty check src/proteinsmc` as a gate. So 'DELETE now' must be 'delete protocols.py AND, in the same commit, remove smc.py:22 and repoint or erase its ten annotation sites', or the repo lands lint-broken and type-check-broken. That is a loud failure, not a silent one, which is why this is an amendment rather than a refutation."
    },
    {
      "id": "R08",
      "survives": false,
      "downgraded_to": "BLOCKED (destination api.py does not exist; not EXECUTABLE_WITH_CHANGES)",
      "evidence": "find /home/marielle/projects/proteinsmc/src -name api.py -> no result; src/proteinsmc/models/fitness.py:14-22,26-91; src/proteinsmc/utils/fitness.py:83-84,41-65; src/proteinsmc/runner.py:98-100",
      "objection": "Three attacks land, one of them on the row's own correction. (1) NO DESTINATION, so nothing about this row is executable today. `find src -name api.py` returns nothing; there is no kernels/ directory and no drive.py. R07's row explicitly acknowledged that api.py does not yet exist and scoped itself around it; R08 keeps 'the SPLIT action ... -> api.py' as if the target were there. The B.7 remedy \u2014 read the DESTINATION's source and state what makes it the same kind of thing \u2014 cannot be satisfied at all, because the destination has no source. This row is gated behind \u00a78 Q2 exactly like R04(b), and must say so. (2) SHAPE-3 UNDER-SCOPED REACHABILITY. The row names three importers (models/gibbs.py:9, models/sampler_base.py:31, models/__init__ -> runner.py). The real set is thirteen: scoring/mpnn.py:35, scoring/esm.py:16, scoring/cai.py:19, scoring/combine.py:12, utils/fitness.py:18-22, utils/blackjax_utils.py:12, sampling/initialization_factory.py:27, sampling/nuts.py:26, sampling/hmc.py:18, models/gibbs.py:9, models/sampler_base.py:31,62,88, oed/experiment.py:16,20, models/__init__.py:4,32-35. Crucially, MOST of them import the type aliases at fitness.py:14-22 (StackedFitness, NeedsTranslation, FitnessFn, CombineFn, StackedFitnessFn) \u2014 NOT the three dataclasses the split targets at :26-91. The row assigns those aliases no fate, so executing the split as written leaves an orphan alias module with no owner and thirteen import sites pointing at it. (3) THE needs_translation CORRECTION OVER-CORRECTS. The row is right on the narrow fact \u2014 fitness.py:85-91 is `jnp.where(jnp.array([f.n_states != n_states for f in self.fitness_functions]), 1, 0)`, importing neither utils/constants.py nor utils/translation.py \u2014 but wrong to conclude 'it is not a codon leak' and strike the sentence. Read the consumer: utils/fitness.py:83-84 calls `evaluator_config.needs_translation(n_states)` and converts it to `needs_translation_tuple`, which utils/fitness.py:41-65 (`_create_score_branches` / `make_score_branch`) uses to decide per-scorer whether to call `translate_func(sequence, key_i, _context)` before scoring, with runner.py:98-100 selecting `nucleotide_to_aa` vs `aa_to_nucleotide` from `config.n_states == NUCLEOTIDES_NUM_STATES`. FitnessEvaluator.needs_translation is the DECISION half of the codon coupling; utils/fitness.py holds only the MECHANISM half. So the alphabet/n_states assumption travels WITH FitnessEvaluator into api.py \u2014 into the cross-repo contract \u2014 which is precisely what the struck sentence was warning about. Correct amendment: re-attribute the mechanism to utils/fitness.py:41-65,83-84 and KEEP the warning on this row, because the class being moved carries the decision. Also note FitnessEvaluator is not a pure config object \u2014 get_functions_by_states (:78-83) and needs_translation (:85-91, calls jnp.where) are behaviour, so api.py inherits a jax.numpy dependency and a semantics question ('n_states relative to WHAT') that no destination source currently answers."
    },
    {
      "id": "R16",
      "survives": false,
      "objection": "Two evidenced failures. (1) FALSE REACHABILITY CLAIM: the row says \"AnnealingConfig is instantiated in models/smc.py:63, models/parallel_replica.py:30/42\". All three are field TYPE ANNOTATIONS behind TYPE_CHECKING-guarded imports, not instantiations \u2014 src/proteinsmc/models/smc.py:18 is `if TYPE_CHECKING:` and :19 is `from .annealing import AnnealingConfig`, making :63 `annealing_config: AnnealingConfig | None = None` a runtime-erased annotation; identically src/proteinsmc/models/parallel_replica.py:10 `if TYPE_CHECKING:` / :15 import, making :30 and :42 `meta_annealing_schedule: AnnealingConfig = field(kw_only=True)` annotations. The only construction sites in the tree are tests/conftest.py:103,162,202. Structural signal (a grep hit on the type name) read as a runtime conclusion \u2014 B.7 shape 2. (2) UNRESOLVED NAME COLLISION the row never mentions: `AnnealingFn` exists TWICE and they are not the same thing \u2014 src/proteinsmc/models/annealing.py:10 `AnnealingFn = Callable[[Any, Any], Any]` (the alias utils/annealing.py:18,38 returns) versus src/proteinsmc/models/protocols.py:94 `class AnnealingFn(Protocol)` with `__call__(self, step: Int, context: dict | None = None) -> Float`. The live SMC kernel imports the PROTOCOL one (src/proteinsmc/sampling/particle_systems/smc.py:22) while the object actually passed to it is built by get_annealing_function and has first parameter `current_step`, not `step` (src/proteinsmc/utils/annealing.py:47). Merging models/annealing.py + utils/annealing.py into schedules.py leaves two same-named `AnnealingFn` types, and any codemod repointing that name can silently bind the wrong one. (3) A third import path is unlisted: src/proteinsmc/models/sampler_base.py:29 imports AnnealingConfig from `proteinsmc.utils.annealing`, not models.annealing, working only because utils/annealing.py:16-19 re-exports it under TYPE_CHECKING. The row correctly identified the merge as under-scoped at 2 files; it is under-scoped at 4.",
      "evidence": "src/proteinsmc/models/smc.py:18-19,63; src/proteinsmc/models/parallel_replica.py:10,15,30,42; src/proteinsmc/models/protocols.py:94; src/proteinsmc/models/annealing.py:10; src/proteinsmc/sampling/particle_systems/smc.py:22; src/proteinsmc/utils/annealing.py:47; src/proteinsmc/models/sampler_base.py:29",
      "downgraded_to": "EXECUTABLE_WITH_CHANGES \u2014 but scope is 4 files, not 2: the merge cannot land until the models/protocols.py:94 `AnnealingFn` collision is resolved and models/sampler_base.py:29's re-export path is repointed. The reachability sentence must be restated (annotations, not instantiations)."
    },
    {
      "id": "R17",
      "survives": false,
      "objection": "The named replacement is not the same kind of thing \u2014 B.7 shape 1, in the corrected_action itself. I read xtrax's source: `MemoryBudget` is `(bytes: int, estimate: Callable[[Sequence[AxisDecision]], int])` whose sole effect is to demote axes Vmap\u2192SafeMap inside BatchPlanner until the joint estimate fits (xtrax/src/xtrax/tiling/budget.py:33-47 and its docstring at :1-13; xtrax/src/xtrax/tiling/plan.py:139,314,336-378). Batch size is an INPUT to it, never an output: `AxisSpec.default_batch_size` (xtrax/src/xtrax/tiling/plan.py:37,49) is supplied by the caller and carried through unchanged into `SafeMap(batch_size=spec.default_batch_size)`. proteinsmc's MemoryConfig/AutoTuningConfig do the opposite thing: they parameterise `auto_tune_batch_size` (src/proteinsmc/utils/memory.py:170), which SELECTS a batch size by empirically executing and timing the function over each candidate in `probe_batch_sizes` (utils/memory.py:199-212, calling benchmark_batch_size per candidate) and returns the winning int, fed back into `get_fitness_function(batch_size=...)` (src/proteinsmc/runner.py:131-144). No xtrax API chooses among probe sizes. \"Memory budget/config\" is the shared word; the things are a declarative planner constraint versus a runtime empirical tuner. Second, undeclared dependency: proteinsmc does not depend on xtrax at all \u2014 pyproject.toml:10-25 lists jax, blackjax, dill, flax, chex, jaxtyping, equinox, uuid-utils, safetensors, prxteinmpnn, einops, huggingface-hub, array-record, msgpack-numpy. Third, citation error: auto_tune_batch_size is imported at runner.py:36, not :34 (:34 is `from proteinsmc.utils.constants import NUCLEOTIDES_NUM_STATES`). Fourth, half the type being \"replaced\" is dead: a wider grep of src/ and tests/ shows `enable_batched_computation` and `device_memory_fraction` (models/memory.py:14-15) are read nowhere outside models/memory.py's own validators. The row's core correction (DELETE\u2192REWRITE, repoint sampler_base.py:60 and oed/experiment.py:55-56 first) is right and I confirmed those two sites; the prescribed replacement is not.",
      "evidence": "xtrax/src/xtrax/tiling/budget.py:33-47; xtrax/src/xtrax/tiling/plan.py:49,336-378; src/proteinsmc/utils/memory.py:170,199-212; src/proteinsmc/runner.py:36,131-144; pyproject.toml:10-25; src/proteinsmc/models/memory.py:14-15",
      "downgraded_to": "NOT_EXECUTABLE_AS_SPECIFIED \u2014 the DELETE\u2192REWRITE retitle stands, but the xtrax.tiling.MemoryBudget target must be struck; MemoryBudget cannot express probe-based batch-size selection, and adopting it would add an undeclared cross-repo dependency."
    },
    {
      "id": "R20",
      "survives": false,
      "objection": "The destination argument rests on a shared domain word, and the row is sequenced wrong. (1) The \"sibling module\" sentence is false as stated: scoring/cai.py is a FITNESS/SCORING factory \u2014 `def make_cai_score() -> FitnessFn` at src/proteinsmc/scoring/cai.py:25 \u2014 and the row's own corrected_action leaves it in scoring/. It would NOT sit beside proteinsmc/codon/translation.py. `ls src/proteinsmc/` shows no codon/ directory exists, and no other repo defines one (rg for `codon` across aminx/xtrax/proxide/prolix src returns zero files), so there is no destination source to cite; the evidence offered is the word \"codon\" appearing in a scoring module's title line. B.7 shape 1. (2) Citation error: cai.py imports nucleotide_to_aa at line 22, not :23. (3) The alphabet question B.7's remedy explicitly demands is unanswered and this is the one row where it bites: src/proteinsmc/utils/translation.py:54 documents \"Uses ColabDesign's AA integers\" while the module's actual lookup tables are PROTEINMPNN-named \u2014 `CODON_INT_TO_RES_INT_JAX` and `PROTEINMPNN_X_INT` at utils/translation.py:10-15, used at :72-73 and :95-96 \u2014 and this repo's own memory record says those ProteinMPNN-named constants are AlphaFold-ordered. A move that creates a `codon/` package must state which alphabet the package commits to; the row does not. (4) Ordering dependency neither row states: models/translation.py:9 imports `EvoSequence` from `proteinsmc.models.types` \u2014 precisely the shim R21 schedules for deletion. R20 must land after R21's codemod or it re-cements the shim.",
      "evidence": "src/proteinsmc/scoring/cai.py:22,25; src/proteinsmc/utils/translation.py:10-15,54,72-73; src/proteinsmc/models/translation.py:9",
      "downgraded_to": "EXECUTABLE_WITH_CHANGES \u2014 the merge of type alias + implementation is sound, but the cai.py \"sibling module\" justification must be struck (it is a shared-word argument), the alphabet convention must be decided as part of the move, and the row must be sequenced after R21."
    },
    {
      "id": "R21",
      "survives": true,
      "objection": "I attacked this three ways and it held. (a) Missing-name attack: a `^Name =` grep of proteinsmc/types.py appears to find only 13 of the 15 re-exported names, which would break the codemod \u2014 REFUTED by reading the file: `Vmapped` and `VmappedTranslation` are Protocol CLASSES at src/proteinsmc/types.py:31 and :42, and all 15 names in models/types.py:5-21 resolve (ArrayLike:10, Scalar:13, ScalarFloat:14, NucleotideSequence:17, ProteinSequence:18, AminoAcidSequence:19, CodonSequence:20, OneHotAminoAcid:21, EvoSequence:23, BatchEvoSequence:24, SequenceType:26, UUIDArray:27, Vmapped:31, VmappedTranslation:42, PRNGKey:51). (b) Circular-import attack: REFUTED \u2014 types.py:3-8 imports only __future__, typing, numpy, jaxtyping; nothing from models. (c) Wider re-grep with --no-ignore over src/ tests/ scripts/ docs/ independently reproduces the row's count exactly: 15 src importers and 6 test files. REFINEMENT the row's corrected_action must absorb: \"repoint all ~20 importers\" names one syntactic form, but two others exist and a `from proteinsmc.models.types import X` codemod misses both \u2014 tests/models/test_types.py:10 `from proteinsmc.models import types as proteinsmc_types` (imports the MODULE OBJECT and dereferences `proteinsmc_types.SequenceType` at :30,33, so that file must be repointed or deleted, not pattern-substituted) and src/proteinsmc/models/__init__.py:22 `from .types import (...)` (relative form). SECOND-COPY NOTE: asr consumes proteinsmc as a separate editable checkout (asr/uv.lock:3202 `source = { editable = \"vendor/proteinsmc\" }`, asr/pyproject.toml:108 workspace members), and asr/vendor/proteinsmc/src/proteinsmc/models/types.py carries its own copy with the same ~15 importers \u2014 so this delete does not break asr, but it widens vendor divergence until asr re-vendors.",
      "evidence": "src/proteinsmc/types.py:10-51; src/proteinsmc/models/types.py:5-21; src/proteinsmc/models/__init__.py:22; tests/models/test_types.py:10,30; asr/uv.lock:3202"
    },
    {
      "id": "R22",
      "survives": true,
      "objection": "Attacked and failed. (a) Wider negative re-run: `rg --no-ignore \"lineage\"` over src/ tests/ scripts/ returns only io.py's own `read_lineage_data`/`read_lineage_data_range` and oed/smc_tree.py locals \u2014 no import of the module. (b) Cross-repo re-run: `rg --no-ignore \"models\\.lineage|from \\.lineage|import lineage\"` across proteinsmc, aminx, xtrax, proxide, prolix, asr, bathos, trex returns only `bathos.query.lineage` hits, an unrelated function in a different package. (c) Package-export attack: I read src/proteinsmc/models/__init__.py in full \u2014 26 import lines and a 30-entry `__all__`, and `.lineage` appears in neither. (d) I confirmed the file's content directly: src/proteinsmc/models/lineage.py is one line, `\"\"\"Module for lineage tracking models.\"\"\"`, with no class, function, or assignment. There is genuinely no thing to rehome. Only residual: asr/vendor/proteinsmc/src/proteinsmc/models/lineage.py is a second physical copy (42 bytes) that this delete does not touch \u2014 cosmetic divergence, no breakage.",
      "evidence": "src/proteinsmc/models/lineage.py:1; src/proteinsmc/models/__init__.py:1-57; asr/vendor/proteinsmc/src/proteinsmc/models/lineage.py"
    },
    {
      "id": "R24",
      "survives": true,
      "objection": "I ran three attacks; all three failed on evidence. (a) PACKAGING attack \u2014 my strongest hypothesis was that deleting the `__init__.py` drops the subpackage from the built distribution, because pyproject.toml:34-35 `[tool.setuptools.packages.find] where = [\"src\"]` looked like plain find_packages. REFUTED by setuptools' own source: `setuptools.config.expand.find_packages(*, namespaces=True, ...)` defaults `namespaces` to True and dispatches to `PEP420PackageFinder`, so an `__init__`-less directory is still discovered. (b) IMPORT-SIDE-EFFECT attack \u2014 I hypothesized the delete silently stops `parallel_replica` being imported as a side effect of importing `smc`. REFUTED: src/proteinsmc/sampling/__init__.py:7-8 already imports `.particle_systems.parallel_replica` and `.particle_systems.smc` directly, so both modules load either way; and the intra-package import at particle_systems/parallel_replica.py:24 is absolute, unaffected. (c) STRING-REFERENCE attack \u2014 `rg` over tests/, docs/, scripts/, pyproject.toml finds no `mock.patch` string or config entry naming the package; only submodule-path imports (tests/sampling/particle_systems/test_parallel_replica.py:16,20, test_smc.py:15) and two prose mentions (docs/TESTING_COVERAGE_ASSESSMENT.md:42,69). The row's negative claim survives a wider search. One residual worth recording, not a blocker: the delete silently converts `proteinsmc.sampling.particle_systems` from a regular package to a PEP-420 namespace package (no `__file__`, no `__all__`) \u2014 nothing introspects it today.",
      "evidence": "src/proteinsmc/sampling/particle_systems/__init__.py:1-9; src/proteinsmc/sampling/__init__.py:7-8; pyproject.toml:34-35; setuptools.config.expand.find_packages signature (namespaces=True); src/proteinsmc/sampling/particle_systems/parallel_replica.py:24"
    },
    {
      "id": "R25",
      "survives": false,
      "objection": "The clearance verifies the SOURCE and never touches the destination, and the premise it corroborates is the wrong one. (1) Every citation in the `destination_evidence` field is a source citation (smc.py:87, :171, :190) \u2014 I confirmed all three are accurate (`static_argnames=(` at :87, `writer_callback: Callable` at :171, `writer_callback(collected_outputs)` at :190) \u2014 but there is zero destination evidence, and the destination cannot supply any: `ls src/proteinsmc/` shows no kernels/ directory, and no such directory exists in aminx, xtrax, proxide, or prolix either. The \"why is this the right kind of home\" sentence is simply absent. (2) The row's own corrected_action states its load-bearing qualification is \"not independently re-derived here\" \u2014 an admitted unverified carry-forward, which under this pass's rule is not a clearance. (3) THE PREMISE THAT MATTERS IS BROKEN AND WOULD MOVE UNCHANGED: smc.py:22 annotates `fitness_fn: FitnessFn` imported from `proteinsmc.models.protocols`, whose contract (models/protocols.py:16, `__call__(key, sequence, context) -> Float[Array, ...]`, documented at :33-36 as returning shape `(,)` per sequence or `(pop_size,)`) is NOT what the runner passes: runner.py:97 hands it the output of `get_fitness_function`, typed `StackedFitnessFn` (utils/fitness.py:73) = `Float[Array, \"1+n_fitness_functions\"]` (models/fitness.py:14,19-21). Meanwhile the three files R25-R28 group as \"the same category\" declare that same argument three different ways \u2014 parallel_replica.py:18 and mcmc.py:19 use `StackedFitnessFn` from models.fitness, gibbs.py:22 uses `FitnessFn` routed through utils.fitness, smc.py:22 uses the models.protocols Protocol. \"fitness_fn\" is one shared word over three declared contracts, one of which contradicts runtime. That is exactly B.7 shape 1, and it is the thing a kernels/ package would have to reconcile.",
      "evidence": "src/proteinsmc/sampling/particle_systems/smc.py:22,87,171,190; src/proteinsmc/models/protocols.py:16,33-36; src/proteinsmc/models/fitness.py:14,17,19-21; src/proteinsmc/utils/fitness.py:73; src/proteinsmc/runner.py:97",
      "downgraded_to": "NOT_CLEARED \u2014 no destination evidence exists to cite, the qualification is self-declared unverified, and the kernel's fitness contract (protocol says scalar, runtime supplies a length-1+n array) must be reconciled across all four kernel rows before any of them moves."
    },
    {
      "id": "R26",
      "survives": false,
      "objection": "The clearance introduces its own shape-2 error and conflates the bug site with its caller. (1) The row flags \"two definitions of `migrate` exist in this single file (lines 28 and 50) ... worth a closer look\" and makes resolving it a precondition. I read it: they are the two arms of ONE overload idiom \u2014 src/proteinsmc/sampling/particle_systems/parallel_replica.py:26 `if TYPE_CHECKING:` \u2192 :28 a stub whose body is `...` at :47, then :48 `else:` \u2192 :50 the real definition. Only :50's runs. The identical idiom appears at smc.py:80/83 and gibbs.py:103/105 in the same codebase. Two grep hits for `def migrate(` read as two definitions is precisely a structural signal treated as a semantic conclusion. (2) The two `lax.cond` citations are not both in migrate: :155 is inside migrate (predicate `accept`, already scalarized one line earlier by `accept = jnp.squeeze(accept)` at :153 \u2014 relevant to the alleged non-scalar-predicate bug), whereas :366 is outside it \u2014 migrate's body ends before the `@partial(` at :252, and :366-367 is `lax.cond(do_exchange & (n_islands > 1), lambda: migrate(...))`, a CALL SITE in a later function. Offering both as evidence for \"the lax.cond bug in migrate\" merges the defect with its caller and makes the fix target ambiguous in the opposite direction from the one the row worried about. (3) It inherits R25's fitness-contract divergence: parallel_replica.py:18 declares `StackedFitnessFn` for the same argument the smc kernel it composes with declares as the models.protocols `FitnessFn`, and parallel_replica.py:24 imports `resample` straight out of that kernel.",
      "evidence": "src/proteinsmc/sampling/particle_systems/parallel_replica.py:18,24,26,28,47,48,50,153,155,252,366-367; src/proteinsmc/sampling/particle_systems/smc.py:22,80,83",
      "downgraded_to": "EXECUTABLE_WITH_CHANGES \u2014 with the \"two migrate definitions\" flag struck as a misread of the TYPE_CHECKING/else overload idiom, the bug localized to the runtime definition at :50 and specifically the `lax.cond` at :155 (not :366, which is a call site), and the shared fitness-contract reconciliation from R25 applied."
    },
    {
      "id": "R27",
      "survives": false,
      "objection": "The row's central self-congratulation is contradicted by the function it is moving. It says \"models/gibbs.py's GibbsConfig correctly stays out of scope ... (this row picked the right file)\". But the runtime definition of run_gibbs_loop is `@partial(jit, static_argnames=(\"config\", \"fitness_fn\", \"update_fns\"))` and takes `config: GibbsConfig` as its FIRST parameter (src/proteinsmc/sampling/gibbs.py:105-107), and dereferences it in the loop bound at :164 (`jax.lax.fori_loop(0, config.num_samples, ...)`). It is the only one of the four loops whose signature is a config object rather than unpacked primitives \u2014 contrast run_smc_loop(num_samples, algorithm, resampling_approach, ...) at smc.py:164-174 and run_mcmc_loop(num_samples, initial_state, fitness_fn, mutation_fn, io_callback) at mcmc.py:23-29. Moving sampling/gibbs.py into kernels/ while GibbsConfig stays in models/ produces a \"kernel\" that imports and statically hashes a config dataclass from the layer it was supposed to be decoupled from \u2014 the same defect the row credits itself with avoiding, so its \"same category as R25/26/28\" sentence is a shared-word grouping, not a verified one. Two of the row's other claims DID survive my attack and should be kept: PointFitness genuinely does not exist \u2014 I re-ran `rg --no-ignore \"PointFitness\"` across all eight repos (proteinsmc, aminx, xtrax, proxide, prolix, asr, bathos, trex) with zero hits, so the negative holds under a wider search than the row's; and the aminx collision note is accurate \u2014 `def gibbs_sweep` is at aminx/src/aminx/potts/sampling.py:195 and takes Potts fields/couplings (h: [n q], j: [n n q q], w: [n n], mask), genuinely a different thing from this FitnessFn-driven loop.",
      "evidence": "src/proteinsmc/sampling/gibbs.py:11,105-107,152-161,164; src/proteinsmc/sampling/particle_systems/smc.py:164-174; src/proteinsmc/sampling/mcmc.py:23-29; aminx/src/aminx/potts/sampling.py:195",
      "downgraded_to": "NOT_CLEARED as scoped \u2014 gibbs is the one loop whose signature is config-bound, so models/gibbs.py cannot simply stay behind; the row needs the same joint treatment it says R15's nuts row got wrong, in addition to the REWRITE relabel for the nonexistent PointFitness."
    },
    {
      "id": "R28",
      "survives": false,
      "objection": "The \"low-risk swap\" is a substitution between two functions that return different types, and the replacement never runs. (1) NOT EQUIVALENT: mcmc.py:69-72's `logdensity_fn_step` returns `fitness[0]` \u2014 a scalar. `make_blackjax_log_prob_fn`'s returned closure returns `fitness_fn(key, sequence, None)` WHOLE (src/proteinsmc/utils/blackjax_utils.py:39-44), i.e. a `StackedFitness = Float[Array, \"1+n_fitness_functions\"]` (src/proteinsmc/models/fitness.py:14). Substituting it hands blackjax's rmh kernel (mcmc.py:55,79-84) a length-1+n vector where a scalar logdensity is expected \u2014 and mcmc.py:48-51 shows the file already has to scalarize with `float(initial_fitness[0])` to build RWState. A Metropolis ratio computed on a vector broadcasts rather than raising: this changes sampling silently, not loudly. (2) KEY BINDING DIFFERS: `logdensity_fn_step` closes over `key` freshly split INSIDE the scan body every step (mcmc.py:67), while `make_blackjax_log_prob_fn(fitness_fn, key)` binds one key at construction time (blackjax_utils.py:16-18,41). The natural refactor hoists the factory out of body_fn, making every step score under the same key \u2014 again silent for any stochastic fitness function. (3) ADDENDUM C: the replacement is dead. `rg --no-ignore \"make_blackjax_log_prob_fn|blackjax_utils\"` over src/, tests/, scripts/ returns its definition at blackjax_utils.py:16 and references ONLY in tests/utils/test_blackjax_utils.py (:10,43,73,107,108,136,166). Zero src/ call sites. The row clears a swap onto a function with no live caller. (4) THE ERROR'S ORIGIN is a docstring that contradicts its own body: blackjax_utils.py:22-25 and :32-35 promise `(combined_fitness, {\"components_fitness\": ...})`, but :39-44 returns a bare array with no tuple and no aux dict. \"Duplicating that logic\" was read off the promise, not the implementation \u2014 B.7 shape 2. The row's OTHER claim survives: mcmc.py:108-117 is indeed a post-scan Python for-loop calling `io_callback(single_output)` outside JIT, exactly as described.",
      "evidence": "src/proteinsmc/utils/blackjax_utils.py:16-18,22-25,32-35,39-44; src/proteinsmc/sampling/mcmc.py:48-51,55,67,69-72,79-84,108-117; src/proteinsmc/models/fitness.py:14,19-21; tests/utils/test_blackjax_utils.py:10,43",
      "downgraded_to": "NOT_EXECUTABLE_AS_SPECIFIED \u2014 the io_callback removal stands, but the make_blackjax_log_prob_fn adoption must be struck: the two functions return different types (scalar vs length-1+n array), bind the PRNG key at different times, and the proposed replacement has no live caller anywhere in src/."
    },
    {
      "id": "R29",
      "survives": false,
      "downgraded_to": "NOT_EXECUTABLE_AS_JUSTIFIED (reachability claim false; row rests on a dead dispatch path)",
      "evidence": "/home/marielle/projects/proteinsmc/.claude/worktrees/wt-20260813-170029/src/proteinsmc/runner.py:303-324",
      "objection": "The 'reachability: LIVE' clearance is refuted at its core. runner.py:256 looks up `run_fn = sampler_def['run_fn']`, but runner.py:303-324 reads `final_state = None` then `if config.sampler_type == \"smc\": ... final_state = run_fn(...)`. There is NO other call of `run_fn`. So for sampler_type='hmc', `_validate_config` passes (runner.py:176-191), `initialize_sampler_state` runs (runner.py:276-292), and then run_hmc_loop is NEVER INVOKED \u2014 run_experiment returns with final_state still None. I re-ran the search wider than the deriver did: `rg -n 'run_hmc_loop'` across proteinsmc, asr, aminx, xtrax, proxide, prolix finds call sites ONLY in tests/sampling/test_hmc_loop.py (:72,:113,:150,:183,:222); the cross-repo search returned zero hits. And that entire test class is dead: tests/sampling/test_hmc_loop.py:42 `@pytest.mark.skip(reason=\"Tests need to be updated for new HMC loop implementation.\")` on `class TestRunHMCLoop` (:43). This is exactly the Addendum C failure the pass exists to catch \u2014 'grep of runner.py shows hmc is a real SAMPLER_REGISTRY key' is a STRUCTURAL signal (dict membership) read as a SEMANTIC conclusion (the code runs). It does not run, by any path, in any repo, in any test. Secondary: the row's own failure_shape analysis is correct (I confirmed test_initialization_factory.py's HMC/NUTS init tests at :102,:147,:410,:445 are all @pytest.mark.skip, so the '4 known failures' justification is indeed unsupported), but the row then re-commits the same error one level up by asserting LIVE. Third: even if runner.py did dispatch, it could not \u2014 run_hmc_loop's signature (hmc.py:26-35: num_samples, initial_state, fitness_fn, step_size, num_integration_steps, inverse_mass_matrix, _mutation_fn, io_callback) does not match the positional call shape runner.py:315-324 uses for SMC. Verdict must be restated as: hmc.py is UNREACHABLE code whose move is a cleanup, not a port of a live sampler body."
    },
    {
      "id": "R30",
      "survives": false,
      "downgraded_to": "NOT_EXECUTABLE_AS_JUSTIFIED (corrected_action is inverted)",
      "evidence": "/home/marielle/projects/proteinsmc/.claude/worktrees/wt-20260813-170029/src/proteinsmc/runner.py:303-324",
      "objection": "R30 is refuted harder than R29 because its corrected_action is built entirely on the false reachability claim. It asserts 'confirmed directly: nuts IS present as a SAMPLER_REGISTRY key ... so run_experiment(sampler_type=\"nuts\") currently executes this loop', and prescribes 'DE-REGISTER nuts ... (removing a currently-callable sampler_type \u2014 a behavior change)'. runner.py:303-304 reads `final_state = None` / `if config.sampler_type == \"smc\":` \u2014 the ONLY branch that calls run_fn. `run_nuts_loop` is imported (runner.py:26) and stored (runner.py:77) and never called. sampler_type='nuts' is therefore NOT 'currently callable': it passes validation and silently produces nothing. De-registering it changes only the ValueError message text at runner.py:177-181, not any executable behavior. Wider search confirms: `rg 'run_nuts_loop'` over proteinsmc + asr + aminx + xtrax + proxide + prolix finds it only at runner.py:26,77, sampling/__init__.py:6,14, nuts.py:30,33, and tests/sampling/test_nuts_loop.py \u2014 and that test class is skipped: tests/sampling/test_nuts_loop.py:41 `@pytest.mark.skip(reason=\"Need to have float representation of sequences.\")` on `class TestRunNUTSLoop` (:42). The row congratulates itself for not being fooled by nuts.py:1-7's 'conceptual placeholder' docstring, but the docstring was RIGHT and the registry dict was the misleading signal. The row inverted which of the two signals was semantic. Note also nuts.py:75 `logdensity=float(initial_logdensity)` would raise a ConcretizationTypeError under any traced call, further evidence this path has never executed."
    },
    {
      "id": "R31",
      "survives": false,
      "downgraded_to": "WRONG_DESTINATION (should be DELETE/dedup against aminx, not proteinsmc/kernels/)",
      "evidence": "/home/marielle/projects/aminx/src/aminx/utils/ste.py:1-12,33-40,67-97",
      "objection": "The clearance never asked whether this thing already exists somewhere in the workspace. It does \u2014 aminx has ALREADY absorbed this exact module, and aminx's copy is strictly ahead. aminx/src/aminx/utils/ste.py:1-12 carries a docstring identical to proteinsmc/sampling/ste.py:1-10 down to the sentence 'Unclear if the optimized sequences will be valid proteins, so this is a heuristic approach to allow gradient-based optimization on discrete outputs to assess how well other samplers are navigating model landscapes.' aminx/utils/ste.py:33-40 `straight_through_estimator` is character-for-character identical to proteinsmc/sampling/ste.py:28-35. aminx/utils/ste.py:67-97 `ste_loss` is proteinsmc's inner `get_loss` (ste.py:48-72) with an added `eps` parameter. aminx's version then adds `gumbel_softmax` (aminx/utils/ste.py:43-64) that proteinsmc lacks. Crucially, aminx's is LIVE where proteinsmc's is dead: aminx/src/aminx/sampling/__init__.py:14 `from aminx.utils import ste` and :24 exports it; aminx/src/aminx/inference/optimize_ste.py (325 lines) imports it at :44 and is itself an optax-based optimizer (:23 `import optax`) \u2014 i.e. the very 'REWRITE from jax.example_libraries.optimizers to optax' this row prescribes has ALREADY BEEN DONE in aminx, plus aminx/inference/decode/ste.py (424 lines). Under the stated separation of concerns ('aminx \u2014 running models, and composable means to use them / plug in samplers'), an STE sequence-design optimizer driven by MPNN logits is aminx's domain, and aminx owns it. Sending proteinsmc's stale 95-line copy to a not-yet-existent proteinsmc/kernels/ re-forks a module that was already de-forked. Separately, the row's destination_evidence ('src/proteinsmc/kernels does not exist yet, consistent with siblings') cites the ABSENCE of the destination as its evidence, which cannot satisfy this pass's rule that a row is cleared only by citing the destination's own source. The row's rationale correction (declared STELoopFn at ste.py:22-24 vs the vmap at ste.py:94) is accurate but irrelevant once the destination is wrong."
    },
    {
      "id": "R32",
      "survives": false,
      "downgraded_to": "EXECUTABLE_WITH_CHANGES, but the stated evidence and the blocking condition are both wrong",
      "evidence": "/home/marielle/projects/proteinsmc/.claude/worktrees/wt-20260813-170029/src/proteinsmc/sampling/initialization_factory.py:124-144 and tests/sampling/test_initialization_factory.py:102,147,410,445",
      "objection": "Two independent evidence failures. (1) MISATTRIBUTED CODE. The row's destination_evidence states '_initialize_prsmc_state (:293-393) builds tiled island populations, calls diversify_initial_sequences'. It does neither. Read the file: `jnp.tile` is at initialization_factory.py:132 and `diversify_initial_sequences` is called at :134-139 \u2014 both inside `initialize_sampler_state` (the outer sampler_type switch, :123-144), not inside `_initialize_prsmc_state` (:293-393), which receives already-tiled, already-diversified populations as its `initial_populations` argument. The row's whole verdict rests on 'the ~30-line outer switch is not the file's identity'; in fact the outer switch is ~60 lines and contains the PRSMC tiling/diversification logic the row credits to the helper. (2) THE BLOCKING CONDITION IS VACUOUS \u2014 an Addendum C violation inside the corrected_action itself. The row says 'do not delete until the int8->float32 dtype bug they currently make testable (via the HMC/NUTS init tests) is re-covered by tests in the new location.' Those tests make nothing testable: every HMC/NUTS init test in that file is skipped \u2014 test_initialization_factory.py:102 and :147 (`@pytest.mark.skip(reason=\"HMC initialization test is currently skipped...\")` on `test_initialize_hmc_state` :103 and `test_initialize_nuts_state` :148), and :410 and :445 (same marker on `test_hmc_initialization` :411 and `test_nuts_initialization` :445). The 1022-line file size the row cites as reachability evidence is a STRUCTURAL signal; the HMC/NUTS portion of it never executes. (3) Minor: '_initialize_blackjax_smc_state ... orthogonal to the sampler_type string switch' overstates \u2014 it is reached only via _initialize_smc_state (:277) under the single `sampler_type_upper == \"SMC\"` arm (:112-122); _initialize_prsmc_state does not call it, constructing BaseSMCState directly at :341-347. The DELETE\u2192SPLIT direction survives (initialize_sampler_state IS genuinely live: runner.py:28, called at runner.py:276-292), but every specific justification offered for it is misstated."
    },
    {
      "id": "R33",
      "survives": false,
      "downgraded_to": "EXECUTABLE, but destination_evidence is false and the delete is not behavior-neutral",
      "evidence": "/home/marielle/projects/proteinsmc/.claude/worktrees/wt-20260813-170029/src/proteinsmc/utils/__init__.py:29 and src/proteinsmc/utils/fitness.py:11",
      "objection": "The row's causal claim is wrong. It states that 'proteinsmc/__init__.py:3 ... eagerly imports the scoring package, which is what currently forces scoring/mpnn.py's find_spec(\"prxteinmpnn\") gate to run at proteinsmc-package-import time.' The actual forcing path is a different one the row never traced: proteinsmc/__init__.py:3 `from . import ... utils` \u2192 utils/__init__.py:29 `from .fitness import FITNESS_FUNCTIONS, get_fitness_function` \u2192 utils/fitness.py:11 `from proteinsmc.scoring import cai, combine, esm, mpnn, nk`. That chain imports mpnn.py eagerly whether or not scoring/__init__.py exists. Deleting scoring/__init__.py therefore does NOT close the find_spec gate the row credits it with \u2014 the gate keeps firing via utils. Conversely the delete is NOT the no-op the row implies: today scoring/__init__.py:3-6 eagerly imports cai/esm/mpnn/nk, so anyone doing `import proteinsmc.scoring` alone pulls all four; after deletion `proteinsmc.scoring` becomes an implicit namespace package that imports nothing. That is a silent import-side-effect change, not 'none'. I did re-run the row's negative claim wider than it did \u2014 `rg -e 'from proteinsmc.scoring import' -e 'make_cai_score|make_esm_score|make_mpnn_score|make_nk_score'` across proteinsmc, asr, aminx, xtrax, proxide, prolix, bathos \u2014 and the negative HOLDS: the only package-level import is utils/fitness.py:11 (submodules, not the re-exported names); tests import from submodules (tests/scoring/test_esm.py:12 `from proteinsmc.scoring.esm import make_esm_score`, tests/scoring/test_cai.py:10 `from proteinsmc.scoring.cai import ...`). So the action stands; the reason given for it does not."
    },
    {
      "id": "R35",
      "survives": false,
      "downgraded_to": "BLOCKED \u2014 destination has no source to cite; domain collision with xtrax",
      "evidence": "/home/marielle/projects/xtrax/src/xtrax/composition (directory) vs the row's own destination_evidence 'proteinsmc/compose.py does not exist (find returned nothing)'",
      "objection": "This row cannot satisfy the pass's clearing rule as written. The rule is 'read the DESTINATION's source and state what makes the two things the same kind of thing'; the row's same_kind_of_thing sentence describes only the SOURCE ('combine.py's two factories are pure closures with no external deps beyond jax.numpy') and its destination_evidence affirmatively records that the destination does not exist. There is no destination source to read, so the row is unclearable by construction rather than cleared. Beyond that: the destination NAME collides with a domain another repo already owns. xtrax is declared as 'composable JAX high-level kernel ops' and already ships `xtrax/src/xtrax/composition` (verified by find). Minting a top-level `proteinsmc/compose.py` for what is actually a fitness-SCORE reducer (combine.py:32 `jnp.sum(fitness_scores, axis=0)`; :72 `jnp.tensordot(weights_array, fitness_components, axes=1)`) renames 'combine'\u2192'compose' and thereby claims a composition namespace in the repo whose stated scope is 'sampling kernels' \u2014 a shared-word move, not a shared-thing move. Verified-true parts of the row: COMBINE_FUNCTIONS resolution really is string-keyed (utils/fitness.py:35-38 defines it; :87-91 resolves `combine_config.name`), so 'first-class, not registry-string-selected' really is a redesign of the config contract, and MOVE\u2192REWRITE is right. One over-claim: 'get_fitness_function (utils/fitness.py:68) is called from runner.py's run_experiment at two sites' \u2014 the two call sites are runner.py:105 and :139, both inside the single helper `_setup_fitness_function`, which run_experiment calls once at runner.py:257."
    },
    {
      "id": "R36",
      "survives": false,
      "downgraded_to": "NOT_EXECUTABLE_AS_JUSTIFIED (reachability false; wrong source commit named)",
      "evidence": "/home/marielle/projects/proteinsmc/.claude/worktrees/wt-20260813-170029/pyproject.toml:20 and src/proteinsmc/scoring/mpnn.py:14,80-82",
      "objection": "Two kills. (1) THE ADDENDUM C DEAD-GATE, REPEATED VERBATIM. The row asserts 'reachability: LIVE in proteinsmc \u2014 registered at utils/fitness.py:30'. That is registry membership again, not execution. mpnn.py:14 reads `PRXTEINMPNN_AVAILABLE = importlib.util.find_spec(\"prxteinmpnn\") is not None` and mpnn.py:80-82 raises ImportError when it is False. `prxteinmpnn` is not installable: pyproject.toml:20 lists it as a hard dependency and `uv run python -c ...` in this repo fails to resolve with 'Because prxteinmpnn was not found in the package registry and your project depends on prxteinmpnn ... your project's requirements are unsatisfiable.' The package name is independently attested as dead by the vendor fork's own commit subject, `36257e7 fix(proteinsmc): repoint dead prxteinmpnn imports to aminx (#4027)`. So make_mpnn_score raises on every call, and \u2014 because the whole `if PRXTEINMPNN_AVAILABLE:` block at mpnn.py:16-23 is skipped \u2014 `make_score_sequence`, `random_decoding_order` and `single_decoding_order` are not even bound at module scope. This is the same shape as B's lead gate: a function dead behind a failing find_spec, cleared as LIVE. The row's own note that it is 'never exercised by a proteinsmc test' was the tell it did not follow. (2) THE ROW NAMES A SUPERSEDED SOURCE COMMIT. corrected_action says 'move from asr's fork (9f42237, deferred imports), not canonical HEAD'. The second physical copy at /home/marielle/projects/asr/vendor/proteinsmc is a separate checkout whose HEAD is 36257e7, one commit PAST 9f42237. I diffed the whole vendored tree against the worktree: scoring/mpnn.py is the ONLY differing file, and the vendor version has already replaced `PRXTEINMPNN_AVAILABLE`/`prxteinmpnn.*` with `AMINX_AVAILABLE`/`aminx.scoring.score`/`aminx.utils.decoding_order` and moved the imports into make_mpnn_score's body. Moving 9f42237's content would discard the aminx repointing that is the entire point of the move. Verified-true parts: aminx/src/aminx/scoring/score.py:57-58 `make_score_fn(model: ModelProtocol, ...)` with ModelProtocol imported at :13; aminx/src/aminx/fitness does not exist; the `# type: ignore[arg-type]` mask is real at mpnn.py:111; `af_to_mpnn` really does exist at aminx/src/aminx/utils/aa_convert.py:28 and is not imported by mpnn.py."
    },
    {
      "id": "R37",
      "survives": false,
      "downgraded_to": "NOT_CLEARED \u2014 the row's own evidence states the destination is not the same kind of thing",
      "evidence": "/home/marielle/projects/aminx/src/aminx/ (find -iname '*esm*' \u2192 0 hits) and src/proteinsmc/scoring/esm.py:39-43",
      "objection": "This row refutes itself and is cleared anyway. Its same_kind_of_thing field states: 'aminx has zero ESM/PLM infrastructure today ... esm.py introduces a NEW KIND OF THING to aminx (a sequence-only masked LM) rather than joining an architectural sibling'. I re-ran that check independently \u2014 `find /home/marielle/projects/aminx/src -iname '*esm*'` returns nothing, and aminx/src/aminx/model/ contains only MPNN-family modules (mpnn.py, mpnn_core.py, ligand_mpnn.py, diffusion_mpnn.py, packer.py, encoder.py, decoder.py, features.py, ligand_features.py, ligand_tiling.py, capabilities.py, dropout.py, versions.py) \u2014 so the finding is correct. But the pass's rule is that a row clears ONLY when you can state why the destination is the RIGHT KIND OF HOME. The row instead substitutes a WEAKER relation: 'the genuine fit is a shared weight-loading idiom (hf_hub_download -> eqx deserialize) matching aminx/io/weights.py, not shared model architecture.' A shared download-and-deserialize idiom is a shared MECHANISM, not a shared kind \u2014 by that standard every checkpointed model in the workspace belongs in aminx. Second: 'reachability: LIVE via utils/fitness.py:31' is registry membership again, not execution; esm.py:39-43 calls `load_model(...)` then `eqx.filter_jit` at FACTORY time, so make_esm_score performs a Hugging Face fetch on construction, and nothing in this pass or the row confirms that path has ever run (the repo's declared dependency set does not even resolve \u2014 see R36). Third: the destination `aminx/fitness/esm.py` does not exist (verified), so again there is no destination source to cite. Verified-true parts: the utils/esm.py coupling is real and runtime (esm.py:11 `from proteinsmc.utils.esm import load_model, remap_sequences`; :62 calls remap_sequences); the `load_model` namespace collision is real (aminx/src/aminx/io/weights.py:151 `def load_model(`); aminx's directory really is `model/` singular; the AbstractFromTorch shim is real at utils/esm.py:63; and the alphabet coupling is real (utils/esm.py:33 imports PROTEINMPNN_TO_ESM_AA_MAP_JAX, tying this move to the same mis-valued constant family as R38)."
    },
    {
      "id": "R38",
      "survives": false,
      "downgraded_to": "WRONG_DESTINATION_RATIONALE \u2014 the bundling argument is itself a shared-word move",
      "evidence": "/home/marielle/projects/proteinsmc/.claude/worktrees/wt-20260813-170029/src/proteinsmc/utils/constants.py:10,33-37,113-117 and src/proteinsmc/scoring/cai.py:13,49,65",
      "objection": "The row's same_kind_of_thing sentence is the exact error class the pass exists to catch. It claims cai.py's imports are 'ECOLI_CODON_FREQ_JAX, ECOLI_MAX_FREQS_JAX, PROTEINMPNN_X_INT from utils/constants.py and nucleotide_to_aa from utils/translation.py, ALL CODON-DOMAIN \u2014 so bundling under a proteinsmc/codon/ subpackage is coherent.' PROTEINMPNN_X_INT is not codon-domain. Read constants.py: `restypes` is an AMINO-ACID alphabet list (:10), `restype_num = len(restypes)  # := 20.` (:33), `unk_restype_index = restype_num` (:34), `restypes_with_x = [*restypes, \"X\"]` (:36) \u2014 i.e. X sits at index 20 \u2014 and yet `PROTEINMPNN_X_INT = 21` (:115), with `STOP_INT` and `UNKNOWN_AA_INT` aliased to it (:116-117). So the row folded an amino-acid alphabet sentinel into 'codon machinery' because cai.py happens to consume it at :65 (`valid_codon_mask = aa_seq != PROTEINMPNN_X_INT`) \u2014 a shared-USE relation dressed as a shared-DOMAIN relation. That matters concretely: creating proteinsmc/codon/ and moving cai.py drags an off-by-one amino-acid constant into a codon subpackage, giving the wrong constant a new and more authoritative-looking home. cai.py's own docstring contradicts its constant's name a third way \u2014 cai.py:49 'aa_seq uses ColabDesign's AA integers' while the constant is named PROTEINMPNN and the alphabet is AlphaFold-ordered (constants.py:113 `AA_CHAR_TO_INT_MAP = restype_order`). Three names, one array, none of them agreeing. The row's own failure_shape correctly flags the 21-vs-20 problem and its corrected_action correctly sequences after the B.2.1 fix; but it does not follow that flag through to the destination rationale, which is the thing being cleared. Also, as with R29/R31/R35: `src/proteinsmc/codon` does not exist, so no destination source is citable."
    },
    {
      "id": "R39",
      "survives": false,
      "downgraded_to": "EXECUTABLE_WITH_CHANGES \u2014 verify packaging before deleting; reachability sentence is false",
      "evidence": "/home/marielle/projects/proteinsmc/.claude/worktrees/wt-20260813-170029/src/proteinsmc/__init__.py:3 and pyproject.toml:1-3,34-35",
      "objection": "I expected this bare DELETE to survive and it does not, on two counts. (1) THE REACHABILITY SENTENCE IS FALSE. The row says the file is 'trivially live only at package-import time (harmless side effect of making proteinsmc.oed an implicit namespace package)'. proteinsmc/__init__.py:3 is `from . import io, models, runner, sampling, scoring, utils` \u2014 `oed` is NOT in that list. So oed/__init__.py does not run at proteinsmc package-import time at all; it runs only on a direct `proteinsmc.oed.*` import, of which the live examples are cross-repo: /home/marielle/projects/asr/scripts/analyze_oed.py:11-12 and /home/marielle/projects/asr/scripts/diagnose_gp.py:4-5 both do `from proteinsmc.oed.gp import fit_gp_model, design_to_features` / `from proteinsmc.oed.structs import OEDDesign`. (2) AN UNVERIFIED PACKAGING DEPENDENCY THE ROW RATES 'confidence: high, failure_shape: none'. pyproject.toml:1-3 sets `build-backend = \"setuptools.build_meta\"` and pyproject.toml:34-35 configures `[tool.setuptools.packages.find] where = [\"src\"]` with NO explicit `namespaces` key. Whether deleting `oed/__init__.py` keeps `proteinsmc.oed` in a built wheel therefore depends entirely on setuptools' `namespaces` default for pyproject-based find. I could NOT verify that default \u2014 UNVERIFIED, because both venvs are empty (`/home/marielle/projects/proteinsmc/.venv/bin/python -c \"import importlib.util\"` reports jax, blackjax, equinox all absent) and `uv run` cannot build an env at all (pyproject.toml:20's `prxteinmpnn` is unresolvable). If the default excludes namespace packages, this delete silently drops proteinsmc.oed from the distribution and breaks the two asr scripts above at import \u2014 the 'changes behaviour silently rather than loudly' case. The row's content claim is accurate (oed/__init__.py really is one docstring line, no code), but 'failure_shape: none' is not earned."
    },
    {
      "id": "R40",
      "survives": false,
      "downgraded_to": "NOT_CLEARED \u2014 same_kind_of_thing rests on a docstring + a shared class name; the two GPModels are not interchangeable",
      "evidence": "/home/marielle/projects/asr/src/asr/oed/gp.py:13,29-34,208 vs /home/marielle/projects/proteinsmc/.claude/worktrees/wt-20260813-170029/src/proteinsmc/oed/gp.py:7,25-31,187",
      "objection": "The clearing sentence is a shape-1/shape-2 compound. Its stated evidence is (a) asr's module header 'Adapted from proteinsmc/oed/gp.py' \u2014 a DOCSTRING, the canonical structural-signal-read-as-semantic-conclusion \u2014 and (b) 'both define a GPModel-shaped struct.dataclass' \u2014 a SHARED CLASS NAME. Reading both sources refutes the conclusion. FIELD SETS ARE NOT COMPATIBLE IN EITHER DIRECTION: proteinsmc GPModel (gp.py:25-31) is X_train, y_train, X_mean, X_std, noise, length_scale: Float = 1.0 (a SCALAR), signal_variance; asr GPModel (asr gp.py:29-34) is X_train, y_train, **mask**, noise, length_scale: Array (an ARD VECTOR, `struct.field(default_factory=lambda: jnp.array(1.0))`), signal_variance. asr requires a `mask` field proteinsmc has no concept of (its docstring at asr gp.py:22 says 'Mask of shape (MAX_SAMPLES,), 1.0 for valid data, 0.0 for padding' \u2014 a static-shape padding design proteinsmc does not share); proteinsmc requires X_mean/X_std asr lacks. Neither can construct the other. THE PUBLIC API DIVERGED TOO: proteinsmc's `design_to_features(design: OEDDesign, mode: OEDFeatureMode = OEDFeatureMode.ALL)` (gp.py:187) vs asr's `design_to_features(design: ASRDesign)` (asr gp.py:208), with mode handling split out into a separate `transform_features(features, mode)` (asr gp.py:316); asr additionally has optimize_gp_hyperparameters (:108), features_to_design (:228), score_candidates_batch (:277) that proteinsmc lacks. DECISIVELY, R40 CONTRADICTS R41: asr's gp.py is typed end-to-end against ASRDesign (asr gp.py:13, :208, :228) and proteinsmc's against OEDDesign (gp.py:7, :187), and R41's own accepted conclusion is that those two Design types are disjoint and must NOT be merged. A gp.py 'reconciliation' across two disjoint design types is not a merge, it is the same fork R41 rules must stay separate. FINALLY, AN UNLISTED CONSUMER THE REACHABILITY SENTENCE DENIES BY OMISSION: the row says 'asr's fork is asr's own OED loop; proteinsmc's original is consumed by proteinsmc/oed/experiment.py and oed/run.py'. Wrong \u2014 proteinsmc's ORIGINAL gp.py is imported from inside asr, at asr/scripts/analyze_oed.py:11 and asr/scripts/diagnose_gp.py:4 (`from proteinsmc.oed.gp import fit_gp_model, design_to_features`). Any change to proteinsmc's gp.py breaks those two. The row's one verified-true addition stands: `rg 'X_mean|X_std'` over asr/oed/gp.py really does return 0 hits, versus proteinsmc gp.py:26-27,75,145-146."
    },
    {
      "id": "R41",
      "survives": false,
      "downgraded_to": "CONCLUSION SURVIVES, EVIDENCE DOES NOT \u2014 four of its cited facts are wrong",
      "evidence": "/home/marielle/projects/asr/src/asr/oed/structs.py:8-12,16-21,37-46 vs /home/marielle/projects/proteinsmc/.claude/worktrees/wt-20260813-170029/src/proteinsmc/oed/structs.py:13-17,24-31 and src/proteinsmc/oed/opt.py:11-15,44",
      "objection": "The verdict (do not merge into asr/oed/structs.py) is right, and my reading strengthens it \u2014 but four cited facts are wrong, and one destination risk was missed. (1) FIELD OVERLAP IS UNDERCOUNTED. The row says 'only 3 field names overlap'. Reading both: OEDDesign (proteinsmc structs.py:24-31) is N, K, q, population_size, n_generations, mutation_rate, diversification_ratio, branch_length; ASRDesign (asr structs.py:37-46) is N, K, q, lambda_val, mutation_rate, n_leaves, mutation_rate_noise_std, branch_length, use_smc, smc_params. The overlap is FIVE \u2014 N, K, q, mutation_rate, branch_length. (2) AND ONE OF THOSE FIVE HAS A TYPE CONFLICT THE ROW MISSED: proteinsmc's `branch_length: Float = 1.0` (structs.py:31) versus asr's `branch_length: Int` (asr structs.py:44). A merge would have silently reconciled a float to an int. (3) 'byte-identical enum' IS NOT BYTE-IDENTICAL AND THE LINE RANGE IS WRONG. The row cites 'proteinsmc :8-17 vs asr :8-14'. proteinsmc's OEDFeatureMode is at structs.py:13-17 (not :8-17); asr's is at asr/structs.py:8-12 (not :8-14); and the bodies differ in indentation \u2014 proteinsmc uses 2-space (its ruff config sets indent-width=2), asr uses 4-space. They are semantically equivalent (both `ALL = auto()`, `EFFECTIVE_ONLY = auto()`), so the dedup is still safe, but 'byte-identical' is false and was not checked. (4) perturb_design HARDCODES NOTHING. The row says 'OEDDesign consumed by proteinsmc/oed/opt.py:11-15 perturb_design (hardcodes 6 of its exact field names)'. opt.py:11-15 is fully generic \u2014 `val = getattr(design, param_name)` at :13, `design.replace(**{param_name: new_val})` at :15. The six hardcoded names live at opt.py:44 (`param_names = [\"N\", \"K\", \"q\", \"population_size\", \"mutation_rate\", \"diversification_ratio\"]`) inside calculate_fim_determinant, alongside `n_params = 6` at :39. (5) MISSED DESTINATION COLLISION: the corrected destination `asr/oed/smc_design.py` names a concept asr already models \u2014 asr/oed/structs.py:16-21 defines `SMCHyperparams` (pop_size, selection_intensity, inference_batch_size) and ASRDesign already carries `use_smc: bool = False` and `smc_params: SMCHyperparams` (asr structs.py:45-46). Dropping an OEDDesign into a file called smc_design.py creates a second, competing SMC-design struct beside the one asr already has. On the row's one self-flagged unverified item I can confirm it in its favour: asr/scripts/analyze_oed.py and asr/scripts/diagnose_gp.py both exist and both import proteinsmc.oed.structs (analyze_oed.py:12, diagnose_gp.py:5), so that repoint is genuinely required."
    },
    {
      "id": "R46",
      "survives": false,
      "evidence": "/home/marielle/projects/proteinsmc/src/proteinsmc/oed/run.py:12",
      "objection": "The clearance never establishes that asr is the right KIND of home \u2014 it only establishes that asr's own experiment.py must not be overwritten. Three breaks. (1) The corrected action orphans the source's only production caller and says nothing about it: proteinsmc/oed/run.py:12 `from proteinsmc.oed.experiment import run_oed_experiment`, called at run.py:215 and :248, and run.py is proteinsmc's own documented OED entry point (proteinsmc/CLAUDE.md line 'uv run python src/proteinsmc/oed/run.py'). Moving experiment.py to asr forces proteinsmc -> asr, inverting the stated charter (asr consumes proteinsmc's sampling kernels; asr/pyproject.toml:16 lists proteinsmc as a dependency of asr, not the reverse). (2) The destination has zero demand for the thing being moved: a repo-wide rg for `proteinsmc.oed.experiment` / `run_oed_experiment` / `convert_design_to_config` outside archive/ returns ONLY proteinsmc's own run.py and proteinsmc/tests/oed/test_experiment_integration.py:10 \u2014 no asr hit at all. The clearance's 'reachability' section cites 4 importers of asr/oed/experiment.py, a DIFFERENT file; that is a structural signal about the destination's neighbour offered where a justification for the move belongs (shape 2). (3) asr already forked, not imported, this stack: asr/src/asr/oed/gp.py:3 says 'Adapted from proteinsmc/oed/gp.py' while asr/scripts/analyze_oed.py:11-12 and asr/scripts/diagnose_gp.py:4-5 still import proteinsmc.oed.gp/structs \u2014 so asr/oed/ would end up holding an ASRDesign experiment.py, an adapted gp.py, AND an OEDDesign smc_experiment.py, i.e. two parallel OED stacks in one package. Separately, all 8 of the source's imports are proteinsmc-internal (proteinsmc.io:13, .models:14-20, .oed.nk:21, .oed.structs:22, .oed.tracking:23, .runner:27, .utils.metrics:28, .utils.serialization:29), so the move relocates a proteinsmc-runner driver into the consumer repo. The row's non-overwrite correction and the convert_design_to_config correction are both right (verified at oed/experiment.py:35-41 and :100-104); the MOVE target is not.",
      "downgraded_to": "REFUTED \u2014 destination not justified; corrected action silently orphans proteinsmc/oed/run.py:12 and inverts the asr->proteinsmc dependency"
    },
    {
      "id": "R48",
      "survives": false,
      "evidence": "/home/marielle/projects/asr/pyproject.toml:70",
      "objection": "The byte-identity is real but was read too narrowly, and the corrected DEDUPLICATE action is not executable. (1) A whole-package `diff -rq proteinsmc/src/proteinsmc asr/vendor/proteinsmc/src/proteinsmc` shows the tree is NOT a clean copy: scoring/mpnn.py differs, and the diff is substantive \u2014 canonical line 14 `PRXTEINMPNN_AVAILABLE = importlib.util.find_spec(\"prxteinmpnn\")` with module-scope `from prxteinmpnn...` imports, vs the vendored `AMINX_AVAILABLE = importlib.util.find_spec(\"aminx\")` with those imports deliberately deferred into make_mpnn_score()'s body, carrying a comment citing 'asr backlog #3713' and a jax>=0.11 dm-haiku import break. One-file identity was generalised to package identity (shape 2). (2) The vendor is not a stale mirror to be collapsed \u2014 asr/vendor/proteinsmc/.git/HEAD reads `ref: refs/heads/fix/asr-3713-lazy-mpnn-import`, a local branch off the pinned rev in scripts/bootstrap_vendor_proteinsmc.sh:11 (REV=8154daacaef3da4bd4408263360fc0877ebb674c). 'Point asr's workspace member at the canonical source' discards that branch. (3) It is affirmatively blocked by dependency resolution, which asr's own pyproject documents at lines 70-76: the vendor exists BECAUSE 'proteinsmc's own pyproject still depends on prxteinmpnn by its pre-rename name' and the bootstrap script rewrites that line to aminx. Canonical proteinsmc/pyproject.toml:19 still declares `\"prxteinmpnn\"`, and asr's env has no such distribution (asr/.venv/lib/python3.13/site-packages has aminx-0.1.0a24.dist-info, no prxtein*). The .pth confirms today's resolution (`__editable__.proteinsmc-0.1.0.pth` -> /home/marielle/projects/asr/vendor/proteinsmc/src), so repointing it breaks `uv sync`. The row's refutation of the 'unimportable today' premise stands; the deduplication it prescribes does not.",
      "downgraded_to": "NOT EXECUTABLE AS CORRECTED \u2014 dedup blocked by the documented prxteinmpnn->aminx vendor patch and a live local branch; package is not identical (scoring/mpnn.py)"
    },
    {
      "id": "R50",
      "survives": false,
      "evidence": "/home/marielle/projects/proteinsmc/src/proteinsmc/utils/constants.py:113",
      "objection": "The 'same kind of thing' sentence answers only 'from-scratch Equinox + HF weights' and never answers the alphabet question Addendum B.7's remedy explicitly requires \u2014 and that is where this row breaks, in exactly the shape-1 way. esm.py's live entry point remap_sequences (esm.py:38-58) indexes `PROTEINMPNN_TO_ESM_AA_MAP_JAX`, imported at esm.py:28-32. That map is built at constants.py:272-283 from `AA_CHAR_TO_INT_MAP`, which constants.py:113 defines as `restype_order` \u2014 and restypes at constants.py:10-31 is A,R,N,D,C,Q,E,G,H,I,L,K,M,F,P,S,T,W,Y,V, i.e. AlphaFold order (the loop body at constants.py:278-287 even calls its keys 'ColabDesign character'). So a map NAMED ProteinMPNN->ESM is actually AF->ESM. aminx is precisely the repo where that word is load-bearing and enforced: aminx/src/aminx/utils/aa_convert.py:16-17 defines MPNN_ALPHABET='ACDEFGHIKLMNPQRSTVWYX' and AF_ALPHABET='ARNDCQEGHILKMFPSTWYVX' as distinct orders with explicit permutations af_to_mpnn/mpnn_to_af at :28-35, pinned by tests/ebm/test_alphabet_boundary_ebm.py. Dropping an AF-ordered map called PROTEINMPNN_TO_ESM into that namespace mis-remaps every residue with no exception raised \u2014 a silent behaviour change, and the corrected action does not mention it. Second unaddressed break: the module cannot travel alone. It needs ESM_BOS_ID/ESM_EOS_ID/PROTEINMPNN_TO_ESM_AA_MAP_JAX from proteinsmc/utils/constants.py, and asr already imports those constants directly (asr/src/asr/mtt_training_pipeline.py:30) alongside the model (:25) \u2014 so the move either splits a coupled pair across repos or forces aminx -> proteinsmc.utils.constants, against the existing proteinsmc -> prxteinmpnn/aminx direction (proteinsmc/pyproject.toml:19). The path (model/ vs models/) and load_model collision (aminx/io/weights.py:151, HF_REPO_ID 'maraxen/aminx' + zstd .eqx.zst registry vs esm.py:349's 'maraxen/esmc_models' + plain .eqx) are correctly caught, but they are the smaller half.",
      "downgraded_to": "REFUTED \u2014 needs the alphabet/constants decision resolved first; as written the move converts a latent naming bug into live silent corruption in the one repo that enforces MPNN vs AF ordering"
    },
    {
      "id": "R52",
      "survives": true,
      "evidence": "/home/marielle/projects/prolix/src/prolix/pt/temperature.py:13",
      "objection": "Attack failed \u2014 I read both destinations independently and the refutation holds. prolix/src/prolix/pt/temperature.py:13-43 `generate_temperature_ladder(n_replicas, min_temp, max_temp, geometric=True) -> Float[ArrayLike, 'n_replicas']` is replica-indexed, returns a vector, docstring at :20-21 says 'Minimum/Maximum temperature in Kelvin', and is consumed for PT setup at prolix/src/prolix/pt/replica_exchange.py:284. proteinsmc/src/proteinsmc/utils/annealing.py:76-105 linear_schedule(current_step, n_steps, beta_min, beta_max, _context) is step-indexed, jit-wrapped via @partial(jit, static_argnames=('_context',)) at :75, clipped to [beta_min, beta_max] at :105, and returns a scalar; same for exponential (:122-166), cosine (:182-217), static (:233-264). Different axis, arity, and physical quantity \u2014 confirmed. Two corrections the row still needs, neither fatal: (a) its reachability sentence overstates \u2014 get_annealing_function IS wired at runner.py:33 and :259-261, but no production config ever supplies a non-None AnnealingConfig; the only construction sites are tests (tests/conftest.py:103,202; tests/models/test_annealing.py:20; tests/models/test_parallel_replica*.py) while the one src caller sets annealing_config=None (oed/experiment.py:73), so the schedules are reached only from test fixtures; (b) the rename's blast radius omits proteinsmc/src/proteinsmc/models/sampler_base.py:29 `from proteinsmc.utils.annealing import AnnealingConfig` \u2014 a name utils/annealing.py never defines (it only re-imports it under TYPE_CHECKING at annealing.py:16-19 from proteinsmc.models.annealing), so that line must be retargeted, and note proteinsmc/models/annealing.py already owns the 'annealing' module name inside the same package.",
      "downgraded_to": ""
    },
    {
      "id": "R54",
      "survives": false,
      "evidence": "/home/marielle/projects/asr/vendor/trex/src/trex/ground_truth.py:39",
      "objection": "The 'unrecorded fork / verbatim-matches' premise is false, and the row commits shape 2 while accusing the plan of shape 1: it quotes the two lines that match and skips the lines that define the semantics. proteinsmc/src/proteinsmc/utils/mutation.py:41-42 builds the mask as `mutation_mask = random.uniform(key_mutate, shape=sequence.shape) < mutation_rate` \u2014 independent per-element Bernoulli at a continuous rate over the FULL sequence shape. trex's mutate builds it at ground_truth.py:39-47 as `mutation_mask = jnp.zeros(sequence.shape[0], dtype=bool)` filled by `random.choice(key_mutate, sequence.shape[0], shape=(n_mutations,), replace=False)` \u2014 a fixed COUNT of positions drawn without replacement over axis 0 only, giving a rank-1 mask broadcast against the sequence. Only the two downstream lines (`offsets = random.randint(..., minval=1, maxval=n_states)` and `(sequence + offsets) % n_states`) coincide, and those are the generic offset-mod-q trick, not a fork fingerprint. These are two different mutation models, not 'the same kernel with different APIs', so there is no 'which is canonical' reconciliation to perform and no fork to record. Two further problems: the cited destination is the only source-form trex on this box \u2014 /home/marielle/projects/trex does not exist; the copies are asr/vendor/trex/src (itself a pinned+patched vendored clone per asr/pyproject.toml:79-81), asr/vendor/trex/build/lib, and phyllo/.venv site-packages \u2014 so 'the destination side's own dependency' is a vendored fork, not an upstream. And the item the row flagged as unverified resolves AGAINST a drop-in swap: xtrax/src/xtrax/transforms/map.py:8 `safe_map[T](fn: Callable[[T], T], xs: T, batch_size)` applies fn to the pytree as a SINGLE argument (:30 `jax.vmap(fn)(xs)`, :40 `jax.lax.map(fn, xs, ...)`) and hard-raises when n % batch_size != 0 (:33-37), whereas proteinsmc's chunked_map unpacks tuples positionally (jax_utils.py:55-58 `if isinstance(x, tuple): return func(*x, **kwargs)`) and supports static_args \u2014 which is exactly what mutation.py:217-221 relies on by passing `data=(mutation_keys, population)` to a two-arg fn.",
      "downgraded_to": "REFUTED \u2014 the fork/'same kernel' premise is factually wrong; the two mutate() functions implement different mutation models"
    },
    {
      "id": "R56",
      "survives": false,
      "evidence": "/home/marielle/projects/proteinsmc/scripts/run_oed_deserialization_manual.py:88",
      "objection": "The row's central negative is asserted from a search narrower than the claim \u2014 the very failure it charges the plan with. It states 'the only actual caller is proteinsmc's own oed/experiment.py:155-169' and that this was 'confirmed by rg across src/ and scripts/'. It was not: proteinsmc/scripts/run_oed_deserialization_manual.py imports the module at :10 and calls metrics.jeffreys_divergence at :88, metrics.calculate_barrier_crossing_frequency at :103, metrics.shannon_entropy at :107 and metrics.jensen_shannon_divergence at :117. That is a second live proteinsmc-side caller, and it breaks the corrected action directly: the functions cannot simply 'travel with wherever oed/experiment.py's logic ends up', because a proteinsmc script would be left importing them from a module that moved to another repo. The row also miscounts the unit \u2014 it speaks of '5 live functions' where metrics.py exposes eight (calculate_position_entropy:100, shannon_entropy:116, kl_divergence:139, jeffreys_divergence:175, jensen_shannon_divergence:191, calculate_barrier_crossing_frequency:208, outcome_shannon_entropy:233, calculate_geometric_fitness_mean:251), with jeffreys/jensen_shannon both delegating to kl_divergence, so no subset moves cleanly. Two of its checks do survive my wider search and should be kept: safe_weighted_mean (metrics.py:25) and calculate_logZ_increment (metrics.py:65) genuinely have no caller outside tests/utils/test_metrics.py anywhere under /home/marielle/projects excluding archive/ \u2014 though 'DELETE' still understates that both are public API re-exported at utils/__init__.py:41-43 and listed in __all__ at :79,95 \u2014 and there is indeed no ESS function in the file and no proteinsmc/src/proteinsmc/kernels/ directory (ls shows io.py, models, oed, padding.py, runner.py, sampling, scoring, types.py, utils only). Finally, a destination collision it did not check: asr already has src/asr/metrics.py ('JAX-native metrics for sequence analysis', sequence_entropy at :10), so a hypothetical asr analysis home is occupied by a same-named module that is not this thing.",
      "downgraded_to": "REFUTED \u2014 'only caller' negative is too narrow (missed proteinsmc/scripts/run_oed_deserialization_manual.py:88,103,107,117), which invalidates the prescribed travel-with-experiment.py action"
    },
    {
      "id": "R57",
      "survives": false,
      "objection": "The 'analytic planner vs empirical tuner' sentence is correct for auto_tune_batch_size vs MemoryBudget \u2014 I read both and confirm it. But the row is scoped to the FILE utils/memory.py, and its negative ('strike the xtrax destination', 'wall-clock benchmarking has no MemoryBudget equivalent', REWRITE-not-move) was asserted from reading exactly one file in the destination package. xtrax/src/xtrax/tiling/estimators.py \u2014 same package, and named in this same agent's R68 as co-owner \u2014 contains device_memory_budget (estimators.py:28-57), which reads `dev.memory_stats()` and `stats['bytes_limit']` (estimators.py:51-57): the identical device query proteinsmc's get_device_memory_mb performs at memory.py:59-61. Its module docstring (estimators.py:1-8) states its purpose is to hook budgeting into 'JAX/XLA's own memory accounting instead of hand-rolled byte math' \u2014 which is precisely estimate_memory_usage (memory.py:49-52: population*length*dtype*3.0). So two of the six functions in this file DO have a destination in xtrax; only the wall-clock benchmark loop (memory.py:139-147) does not. The corrected_action over-generalises from the one function examined to the whole file. Second defect: reachability is cited as 'exported via utils/__init__.py:29-35,71-96' \u2014 an __all__ list is a structural signal, not execution (B.7 shape 2). The actual call site is runner.py:36,131, gated at runner.py:112 on config.memory_config.auto_tuning_config.enable_auto_tuning (default True, models/memory.py:48). The row applied a 'confirmed live, not conditional-only' standard in R63 and an export-list standard here. Third: the swap changes behaviour silently in one direction \u2014 get_device_memory_mb swallows AttributeError/IndexError/KeyError and returns 8192 MB (memory.py:69-70) where device_memory_budget raises RuntimeError with 'no silent fallback' (estimators.py:52-56).",
      "evidence": "xtrax/src/xtrax/tiling/estimators.py:1-8,28-57 vs proteinsmc/src/proteinsmc/utils/memory.py:33-52,55-70; src/proteinsmc/runner.py:112,131",
      "downgraded_to": "EXECUTABLE_WITH_CHANGES (revised): split the file \u2014 get_device_memory_mb + estimate_memory_usage DO have an xtrax destination (tiling/estimators.py); only benchmark_batch_size/auto_tune_batch_size are REWRITE-with-no-destination"
    },
    {
      "id": "R58",
      "survives": false,
      "objection": "Two independent breaks, both B.7 shape 3 (negative from a search narrower than the claim).\n\n(1) The call-site enumeration is incomplete. The row lists 'mutation.py:217, memory.py:137,142' as the live chunked_map sites. There is a fourth: pmap_utils.py:51 calls `jax_utils.chunked_map(core_fn, data, batch_size, static_args=static_args)` in module-qualified form (import at pmap_utils.py:16 is `from proteinsmc.utils import jax_utils`), so a grep keyed on `from .jax_utils import chunked_map` or on the symbol in an import line misses it. It sits inside the pmapped worker (pmap_utils.py:49-51), i.e. under jax.pmap \u2014 the one context where safe_map's vmap-vs-lax.map branch (map.py:29-30) would change device memory behaviour silently rather than loudly.\n\n(2) 'smc_tree.py is currently unimportable AND unreachable' is true only of the standalone proteinsmc checkout, and the row's own evidence contains the refutation it did not follow. asr declares `[tool.uv.workspace] members = [\"aminx\", \"vendor/prolix\", \"vendor/proteinsmc\", \"vendor/trex\"]` (asr/pyproject.toml:108) with `proteinsmc = { workspace = true }` (asr/pyproject.toml:76) and `trex = { workspace = true }` (asr/pyproject.toml:81) \u2014 so inside asr, trex resolves and vendor/proteinsmc imports it fine. asr then imports smc_tree at three sites: asr/src/asr/nk_experiment.py:26, asr/src/asr/oed/experiment.py:31, asr/src/asr/generator_adapters.py:239 (`from proteinsmc.oed.smc_tree import generate_tree_data_smc`). asr/vendor/proteinsmc/src/proteinsmc is byte-identical to the repo copy except scoring/mpnn.py (verified by recursive diff), so smc_tree.py:83 and :144 are live chunked_map sites in the ecosystem's actual consumer. 'Latent-not-live' inverts the truth: smc_tree is dead in the repo that owns it and live in the repo that vendors it.\n\nMinor: the row repeats B's 'third breaking site at smc_tree.py:290'. chunked_map is at smc_tree.py:83 and :144; :284 is the trex safe_map call. And tests do import it (tests/oed/test_smc_tree.py:9), so in the standalone repo that test errors at collection today rather than being merely latent.",
      "evidence": "src/proteinsmc/utils/pmap_utils.py:16,49-51; /home/marielle/projects/asr/pyproject.toml:76,81,108; /home/marielle/projects/asr/src/asr/nk_experiment.py:26; /home/marielle/projects/asr/src/asr/oed/experiment.py:31; /home/marielle/projects/asr/src/asr/generator_adapters.py:239",
      "downgraded_to": "REFUTED as stated \u2014 reclassify smc_tree as LIVE-VIA-ASR (not latent), and add pmap_utils.py:51 as a fourth chunked_map site"
    },
    {
      "id": "R59",
      "survives": true,
      "objection": "Attack failed. I diffed the two ranges byte-for-byte (jax_uuid.py:16-58 vs jax_utils.py:63-105) \u2014 identical, no whitespace or docstring drift. The deadness negative also holds when widened: grepping the module name AND both symbol names (generate_jax_uuid, generate_jax_hash) across src/, tests/, scripts/ finds no importer of proteinsmc.utils.jax_uuid anywhere; the only consumers of those two names are tests/utils/test_jax_utils.py:7-8, which import from jax_utils, not jax_uuid. utils/__init__.py never touches the module \u2014 it imports only chunked_map from jax_utils (utils/__init__.py:31) and neither UUID symbol appears in __all__ (lines 55-99). One caveat that does not break the row: a second physical copy exists at asr/vendor/proteinsmc/src/proteinsmc/utils/jax_uuid.py (the vendored tree is identical to the repo except scoring/mpnn.py), so the delete must be applied there too or the vendored clone silently diverges.",
      "evidence": "src/proteinsmc/utils/jax_uuid.py:16-58 == src/proteinsmc/utils/jax_utils.py:63-105 (diff: identical); src/proteinsmc/utils/__init__.py:31,55-99",
      "downgraded_to": ""
    },
    {
      "id": "R60",
      "survives": false,
      "objection": "The primary claim survives and I strengthened it: xtrax.distributed genuinely never touches array data. My search was wider than the row's \u2014 grepping `jax.pmap|shard_map|device_put_sharded|pmap(` across ALL of xtrax/src returns zero hits, not just across sharding.py. So 'strike the xtrax.distributed.sharding pointer' is right.\n\nBut the row breaks on its own destination_evidence and corrected_action.\n\n(a) The evidence cites 'sharding.py:1-80'. The file is 174 lines. Line 80 is three lines before `def get_device_mesh` (sharding.py:83) \u2014 the row bounded its read to end immediately before the very function it then recommends.\n\n(b) 'get_device_mesh should replace sampler_base.py's dead mesh code' calls live code dead. sampler_base.py:147-151 is `def __post_init__` ending in `object.__setattr__(self, \"mesh\", self._initialize_device_mesh())` \u2014 it executes on every BaseSamplerConfig construction, and tests assert its result (tests/models/test_sampler_base_advanced.py:40-44 read config.mesh and config.mesh.axis_names). The row's own R63 demanded 'confirmed live, not conditional-only'; here unconditional live code was called dead.\n\n(c) The proposed replacement is not equivalent and would break the default path. sampler_base._initialize_device_mesh handles device_mesh_shape=None by autodetecting (sampler_base.py:159-164: `Mesh(devices, axis_names=(\"devices\",))`), and device_mesh_shape defaults to None (sampler_base.py:48). xtrax's get_device_mesh(shape, axis_names) has no None path at all and raises ValueError whenever math.prod(shape) != len(jax.devices()) (sharding.py:99-107). Swapping it in makes the default config raise.",
      "evidence": "xtrax/src/xtrax/distributed/sharding.py:83-112 (file is 174 lines); proteinsmc/src/proteinsmc/models/sampler_base.py:48,147-151,159-164; tests/models/test_sampler_base_advanced.py:40-44",
      "downgraded_to": "EXECUTABLE_WITH_CHANGES (revised): DELETE pmap_utils + remove utils/__init__.py:52,85 stands; drop the get_device_mesh/sampler_base recommendation \u2014 that code is live and xtrax's version lacks the autodetect path it depends on"
    },
    {
      "id": "R63",
      "survives": true,
      "objection": "I tried to break the 'live, sequence the delete' conclusion and instead found more support for it. Two additions, neither contradicting the row: (1) a second live consumer the row missed \u2014 tests/test_end_to_end_io.py:14,59 also imports and calls create_sampler_output_skeleton, so an unconditional delete breaks a test as well as oed/experiment.py:123; (2) the .arrayrecord writers cited are real \u2014 runner.py:266 builds `data_{run_uuid}.arrayrecord` and oed/tracking.py:339 returns `oed_data.arrayrecord`. One caveat worth flagging on the sequencing: the gate the row defers to does not exist in code. `SamplerKernel` appears nowhere in src/ or tests/ \u2014 grepping both returns only the string 'msgpack' in two comments (sampler_base.py:196,260). SamplerKernel.skeleton() exists only as plan text (.praxia/docs/plans/260813_proteinsmc-ecosystem-partition.md:131,135,211,338). So 'delete after \u00a73.5 lands' is correct but is a dependency on unwritten code, not on a landed switch, and should be stated that way.",
      "evidence": "tests/test_end_to_end_io.py:14,59; src/proteinsmc/runner.py:266; src/proteinsmc/oed/tracking.py:339; grep SamplerKernel over src/ tests/ \u2192 0 hits",
      "downgraded_to": ""
    },
    {
      "id": "R64",
      "survives": false,
      "objection": "This row cannot be cleared under the pass's own rule, because there is no destination source to read. destination_evidence states 'proteinsmc/kernels/ does not exist' \u2014 I confirm: src/proteinsmc contains __init__.py, io.py, models/, oed/, padding.py, runner.py, sampling/, scoring/, types.py, utils/ and no kernels/. The rule is that a row is cleared only when you can state, citing the destination's own source, why it is the right kind of home. A directory with no source cannot supply that sentence, and the row supplies none \u2014 it reasons entirely from the source side.\n\nWorse, the name collides with something that already exists. All three consumers the row cites live in src/proteinsmc/sampling/ (sampling/mcmc.py:69-72 `logdensity_fn_step` returning fitness[0]; sampling/hmc.py:90-93 the same; sampling/nuts.py), and the repo owner's own separation-of-concerns line for this repo is 'proteinsmc \u2014 sampling kernels'. So 'kernels/' names what sampling/ already is, and nothing in the row establishes it as a different kind of home rather than a second name for the same one.\n\nThe rewrite spec is also incomplete. Beyond the two defects the row lists (returns the raw array; closes over a fixed key), the docstring at blackjax_utils.py:22-35 promises a tuple `(combined_fitness, {\"components_fitness\": ...})` for blackjax's log_density_fn, which the body at :39-44 never returns \u2014 so the docstring contradicts both the current code and the proposed rewrite, and the aux-data contract needs deciding before any move. Deadness itself I confirm: widening to the bare symbol across src/, tests/, scripts/ finds only tests/utils/test_blackjax_utils.py.",
      "evidence": "src/proteinsmc/utils/blackjax_utils.py:22-35 vs 39-44; src/proteinsmc/sampling/ (gibbs.py, hmc.py, initialization_factory.py, mcmc.py, nuts.py, particle_systems/, ste.py); sampling/mcmc.py:69-72; sampling/hmc.py:90-93",
      "downgraded_to": "REFUTED \u2014 no destination source exists to clear against, and 'kernels/' collides in name with the existing sampling/ package that already holds all three consumers"
    },
    {
      "id": "R65",
      "survives": true,
      "objection": "Attack failed. I widened the search from the module name to the two symbols the module actually defines \u2014 split_key_for_sampler (key_management.py:17) and split_key_batched (key_management.py:55), the only two `def`s in the file \u2014 across src/, tests/, and scripts/. Every hit outside the module itself is in tests/utils/test_key_management.py (import at :9). utils/__init__.py never imports the module (its import block is lines 3-53; key_management is absent) and neither symbol appears in __all__ (lines 55-99), so there is no re-export path either. Same second-copy caveat as R59: asr/vendor/proteinsmc/src/proteinsmc/utils/key_management.py exists and must be deleted in lockstep.",
      "evidence": "src/proteinsmc/utils/key_management.py:17,55; src/proteinsmc/utils/__init__.py:3-53,55-99; tests/utils/test_key_management.py:9",
      "downgraded_to": ""
    },
    {
      "id": "R66",
      "survives": true,
      "objection": "Attack failed. config_unpacker.py defines exactly one public symbol, `with_config` (config_unpacker.py:17) \u2014 the only `def`/`class` in the file. Widening to that symbol across src/, tests/, scripts/ yields one hit outside the module: tests/utils/test_config_unpacker.py:10. utils/__init__.py neither imports it nor lists it in __all__. One direction-of-dependency note that supports rather than undermines the delete: config_unpacker.py:12 imports BaseSamplerConfig from proteinsmc.models, so removing it removes an edge into models/ rather than creating a dangling one. Same asr/vendor/proteinsmc second-copy caveat.",
      "evidence": "src/proteinsmc/utils/config_unpacker.py:12,17; tests/utils/test_config_unpacker.py:10; src/proteinsmc/utils/__init__.py:3-53,55-99",
      "downgraded_to": ""
    },
    {
      "id": "R67",
      "survives": false,
      "objection": "The 'same kind of thing' sentence rests on two structural signals \u2014 'is an eqx.Module' and 'has functions named save_model/load_model' \u2014 promoted to the conclusion that aminx/model/ is the right home. The destination's own source says otherwise. aminx/src/aminx/model/__init__.py:1-12 declares a closed public contract by name: 'Model classes: Aminx, PrxteinLigandMPNN, DiffusionAminx, Packer / Encoder/decoder layers / Feature extraction: ProteinFeatures / Capability introspection: ModelCapabilities'. aminx/src/aminx/model/capabilities.py:1 reads 'Static capability flags for MPNN models.' ESMC (proteinsmc/utils/esm.py:299-321) is composed of SparseEmbedding + TransformerStack + Sequential and satisfies none of that contract \u2014 no Encoder, no ProteinFeatures, no ModelCapabilities entry, different weight provenance (huggingface_hub, esm.py:16) versus aminx's zstd + eqx.tree_deserialise_leaves path (aminx/io/weights.py:145-149). 'eqx.Module with weights' describes half of every JAX model library.\n\nSecond: the destination already has a thing by that name that is not this thing. aminx/io/weights.py:151 defines `load_model`, documented at :165 as 'Load a fully instantiated Aminx-family model with weights', inferring skeleton topology from an MPNN checkpoint_id. proteinsmc's esm.py:349 defines a different `load_model(model_name, key) -> ESMC`. The row's only correction was the models/\u2192model/ path; it does not mention the collision.\n\nThird: the reachability negative is narrower than the claim. Beyond scoring/esm.py:11, the module is imported cross-repo at asr/src/asr/mtt_training_pipeline.py:25 and asr/src/asr/analysis/biophysics.py:7 (both `from proteinsmc.utils.esm import load_model as ..., remap_sequences`), and its companion constants at asr/src/asr/mtt_training_pipeline.py:30. Three external consumers; the row names one.\n\nWhat DOES hold: the negative 'zero existing ESM code in aminx' survives a wider search than the row's (case-insensitive, all file types, whole of aminx/src \u2192 zero hits). And the alphabet bug \u2014 which the row sourced to project memory rather than to code \u2014 I verified against source and it is real and understated: constants.py:272-276 allocates PROTEINMPNN_TO_ESM_AA_MAP_JAX at AMINO_ACIDS_NUM_STATES = 20 (:119) while UNKNOWN_AA_INT = PROTEINMPNN_X_INT = 21 (:115-117); the compensating 'X' branches at :290-298 are BOTH dead, because AA_CHAR_TO_INT_MAP = restype_order (:113) is the 20-letter map built from restypes (:10-32) and has no 'X' key. So an out-of-range residue gathers out of bounds at esm.py:52 and clamps to index 19 = 'V'.",
      "evidence": "aminx/src/aminx/model/__init__.py:1-12; aminx/src/aminx/model/capabilities.py:1; aminx/src/aminx/io/weights.py:151,165; proteinsmc/src/proteinsmc/utils/esm.py:299-321,349; proteinsmc/src/proteinsmc/utils/constants.py:113,115-119,272-276,290-298; asr/src/asr/mtt_training_pipeline.py:25,30; asr/src/asr/analysis/biophysics.py:7",
      "downgraded_to": "REFUTED as a clearance \u2014 aminx/model/ is contractually MPNN-family by its own __init__ and capabilities docstrings, load_model collides with aminx/io/weights.py:151, and two asr consumers are unaccounted for"
    },
    {
      "id": "R68",
      "survives": false,
      "objection": "Self-declared medium confidence, self-declared as not having re-derived the counts it is correcting \u2014 and its destination_evidence is contradicted by the same agent's neighbouring row. R68 names 'xtrax/src/xtrax/tiling/budget.py + xtrax/src/xtrax/tiling/estimators.py ... as stated protocol owner', but R57 asserts there is no xtrax equivalent for proteinsmc's device-memory read \u2014 which estimators.py:28-57 performs (dev.memory_stats(), stats['bytes_limit']). The two rows cannot both be right, and the resolution is that estimators.py was named, not read. A row cleared on a file that was cited rather than opened is exactly the failure this pass exists to catch.\n\nSeparately, '11 distinct files' is a grep hit-count over three identifier strings offered as a count of implementations. A file containing the string `bytes_limit` is not thereby a memory-budget implementation \u2014 estimators.py itself contains it as the key it reads from JAX. The row supplies no per-file classification and explicitly declines to re-derive the 5x/3x sub-counts it is replacing, so its only concrete contribution ('the count is understated') rests on unverified structural evidence. Under 'default to refuted when uncertain', this does not clear.",
      "evidence": "xtrax/src/xtrax/tiling/estimators.py:28-57 (read, and contradicts R57's negative); R68's own confidence:'medium' plus 'I did not re-derive B's exact 5x/3x sub-counts'",
      "downgraded_to": "NOT_CLEARED \u2014 needs a per-file classification of the 11 hits (implementation vs. consumer vs. string match) before any consolidation scope is stated"
    },
    {
      "id": "R69",
      "survives": true,
      "objection": "Attack failed, and the deadness claim got stronger under a wider search. The row checked only SEQ_BUCKETS and get_seq_bucket. padding.py exports ten public names \u2014 MAX_POPULATION (:21), MAX_SEQ_LEN (:22), SEQ_BUCKETS (:25), get_seq_bucket (:28), pad_population (:47), pad_sequence (:68), create_particle_mask (:87), create_sequence_mask (:103), masked_mean (:117), masked_sum (:131). Grepping all of them across src/, tests/, and scripts/ returns zero references outside the file itself, and no module ever imports proteinsmc.padding. Fully dead, wider than claimed.\n\nThe destination read also holds: xtrax/src/xtrax/tiling/bucket.py:1-20 states in its own docstring that both select_bucket and bucketize 'run on the host, before the JIT boundary ... deliberate: JAX has no dynamic shapes inside jit, and padding the variable-length input on-device (e.g. jnp.pad of a length-L array) is itself shape-specialized and would recompile per length', and imports only numpy (bucket.py:28) \u2014 so 'xtrax owns this' genuinely does overstate the fit against a jnp-based device-side module.\n\nTwo refinements that do not change the verdict: the jnp import is padding.py:17, not :18 (:18 is jaxtyping); and asr/src/asr/oed/padding.py is not quite the same artifact \u2014 it buckets NK-model parameters N_BUCKETS/K_BUCKETS/Q_BUCKETS (asr padding.py:21-23), a different axis set from proteinsmc's sequence-length-only SEQ_BUCKETS, so 'independent fork' slightly overstates the sameness. The row's conclusion (DELETE stands; drop the 'consolidation achieved' framing) is unaffected either way.",
      "evidence": "src/proteinsmc/padding.py:17,21-25,28,47,68,87,103,117,131 (zero external refs to any); xtrax/src/xtrax/tiling/bucket.py:1-20,28; /home/marielle/projects/asr/src/asr/oed/padding.py:17,21-23",
      "downgraded_to": ""
    },
    {
      "id": "R71",
      "survives": false,
      "downgraded_to": "EXECUTABLE_WITH_CHANGES (KEEP holds on substance; the mandated rationale-correction is refuted)",
      "evidence": "proteinsmc/src/proteinsmc/scoring/mpnn.py:14; proteinsmc/pyproject.toml:20; proteinsmc/.venv/lib/python3.13",
      "objection": "The clearance's whole correction rests on 'proteinsmc already imports proxide directly (scoring/mpnn.py:17) ... this is a legitimate optional-dependency gate, not the dead-behind-a-failing-gate shape Addendum C warns about.' That is the exact Addendum C shape, asserted without executing anything. mpnn.py:14 gates on `importlib.util.find_spec(\"prxteinmpnn\")`; `prxteinmpnn` is a HARD dependency (pyproject.toml:20, inside `dependencies`, not an extra) that does not resolve \u2014 `uv run` fails with 'Because prxteinmpnn was not found in the package registry and your project depends on prxteinmpnn, we can conclude that your project's requirements are unsatisfiable' \u2014 and is not installed: .venv/lib/python3.13 contains only `_virtualenv.pth`/`_virtualenv.py`, no site-packages entries at all. So PRXTEINMPNN_AVAILABLE is False and `from proxide.ops.dataset import create_protein_dataset` (mpnn.py:17) never executes; `create_protein_dataset` at mpnn.py:83 is unreachable behind the `raise ImportError` at mpnn.py:79-81. Second, independent defect: the guard tests package A (prxteinmpnn) while the guarded block imports package B (proxide) \u2014 proxide appears nowhere in proteinsmc/pyproject.toml:10-24 and proxide's own pyproject declares no prxteinmpnn, so even with prxteinmpnn present this line ImportErrors. Only surviving part: the ~5-name overlap is real (proxide/src/proxide/core/types.py:10,13,14,37,104 vs proteinsmc/src/proteinsmc/types.py:10,13,14,18,51), proxide's file is 140 lines and atom/physics-dominated, and proteinsmc/types.py is indeed 51 lines. Also unmentioned by the clearance: there are TWO type modules \u2014 proteinsmc/src/proteinsmc/models/types.py:1 is a 'Legacy type definitions ... now redirected to proteinsmc.types' shim, and 16 src files import through it rather than the file the row names."
    },
    {
      "id": "R72",
      "survives": false,
      "downgraded_to": "REFUTED",
      "objection": "Three independent breaks. (1) STRUCTURAL-READ-AS-SEMANTIC: AxisBoundary has zero instantiations in any non-test source. xtrax only defines it (xtrax/src/xtrax/stages/boundaries.py:84); `rg 'AxisBoundary\\('` over xtrax/src returns only that class definition, and over aminx returns 13 hits, ALL under aminx/tests/ (tests/types/test_boundaries.py:47,58,73; tests/host/test_plan_topology_validator.py:47,62,77,91,105; etc.). Every `StageSet(...)` construction in aminx/src (inference/optimize_ste.py:311; inference/decode/ste.py:211,300,418; inference/logits.py:504) omits axis_boundaries, so it is always the empty-dict default (aminx/src/aminx/types/stages.py:472), and the validator call the clearance cites (aminx/src/aminx/host/plan.py:503) therefore iterates an empty mapping on every production path. aminx's own source says so in words: aminx/src/aminx/sampling/mbr_consensus.py:6 'does not populate StageSet.axis_boundaries.' 'aminx already consumes exactly this mechanism' is a field declaration + a no-op validator call read as a live consumer. (2) SHARED WORD: the four `io_callback` params are not one mechanism. mcmc.py imports no io_callback at all (mcmc.py:1-13) and invokes it as a plain host callable in a Python `for i in range(num_samples)` loop AFTER `jax.lax.scan` returns (mcmc.py:105, 107-117), whereas nuts.py:127, hmc.py:123 and gibbs.py:152 wrap it in `jax_io_callback` inside the traced body. xtrax's Sink protocol docstring mandates 'Implementations must use io_callback(ordered=self.ordered) internally' (boundaries.py:76), so consolidating mcmc onto AxisBoundary moves its writes from post-hoc host code into the trace \u2014 a SILENT semantics/ordering change, not a loud one. (3) proteinsmc declares no xtrax dependency (pyproject.toml:10-24) and `rg xtrax proteinsmc/src` returns zero hits, so this row creates a new undeclared cross-repo edge that the clearance never mentions.",
      "evidence": "aminx/src/aminx/sampling/mbr_consensus.py:6; aminx/src/aminx/types/stages.py:472; proteinsmc/src/proteinsmc/sampling/mcmc.py:105-117"
    },
    {
      "id": "R74",
      "survives": false,
      "downgraded_to": "EXECUTABLE_WITH_CHANGES",
      "objection": "The destination is genuinely live (bathos/src/bathos/git.py:18 has 7 real call sites: checker.py:65, runner.py:410, decorators.py:38, postmortem.py:257, cli.py:969, mcp.py:2213) \u2014 that part holds. But the clearance fails on three counts. (a) SHARED WORD covering only a quarter of the row's source: the same_kind_of_thing sentence argues only get_git_commit_hash \u2194 capture_git_state, yet the row also moves tracking.py's 'provenance quartet'. `add_oed_to_metadata` (proteinsmc/src/proteinsmc/oed/tracking.py:272-301) does no git work whatsoever \u2014 it json-loads metadata.json and writes metadata['oed_design'], ['oed_phase'], ['oed_iteration'] (lines 296-298). It is experiment-design bookkeeping; 'provenance' is the shared word, and destination_evidence names no bathos module for it or for save_oed_record/load_oed_manifest/create_oed_summary. (b) SILENT behaviour change: proteinsmc/src/proteinsmc/io.py:85 writes `\"git_commit_hash\": get_git_commit_hash()`, which is JSON `null` when git is unavailable (io.py:33-36, 49-50); bathos returns the sentinel `GitState(hash=\"unknown\", ...)` (bathos/src/bathos/git.py:15, 38). The swap silently changes metadata.json's sentinel from null to the string 'unknown'. (c) FAILURE-MODE regression: proteinsmc guards with `shutil.which(\"git\")` (io.py:33) and `timeout=5` (io.py:45); bathos/git.py:19-37 has neither and catches only CalledProcessError/FileNotFoundError \u2014 a hung git (index.lock, network mount) would hang the run indefinitely instead of returning None after 5s. Also: bathos is not a declared proteinsmc dependency (pyproject.toml:10-24). 'corrected_action: None' is wrong.",
      "evidence": "proteinsmc/src/proteinsmc/oed/tracking.py:296-298; proteinsmc/src/proteinsmc/io.py:33,45,85; bathos/src/bathos/git.py:15,19-38"
    },
    {
      "id": "R75",
      "survives": true,
      "objection": "Attack failed. The available trap was the shared word 'loop' (proteinsmc's sampler loops: run_smc_loop/run_mcmc_loop et al., runner.py:57-77), and the clearance did not fall into it. Verified independently: xtrax/src/xtrax/loop/ is exactly 26 .py files, and reading one confirms the domain \u2014 xtrax/src/xtrax/loop/promotion_gate.py:1-8 is the 'Promotion-to-main gate (T2-30, #2181, AC-23)' enforcing per-promotion human approval of evolved code, alongside admission.py, attestation_evidence_gate.py, campaign_approval_gate.py, seed_gate.py etc. This is an autoresearch governance/attestation subsystem, categorically not a sampling loop. proteinsmc has zero references to xtrax anywhere: `rg xtrax proteinsmc/src proteinsmc/pyproject.toml` returns nothing. OUT OF SCOPE is correct. Only weakness worth noting: the clearance's evidence was a directory listing alone (structural); I supplied the missing semantic read and it confirms rather than contradicts.",
      "evidence": "xtrax/src/xtrax/loop/promotion_gate.py:1-8"
    },
    {
      "id": "R76",
      "survives": true,
      "objection": "Attack failed after widening the negative, which was the shape-3 target. Re-ran `rg 'split_key_for_sampler|split_key_batched|key_management'` across ALL seven repos (proteinsmc, asr, aminx, xtrax, proxide, prolix, bathos) rather than just proteinsmc/src: the only hits are the file's own definitions (proteinsmc/src/proteinsmc/utils/key_management.py:17,55 plus its docstring examples at :42,:73), its dedicated test file (proteinsmc/tests/utils/test_key_management.py, ~20 hits), and one docs table row (proteinsmc/docs/TESTING_COVERAGE_ASSESSMENT.md:365). Checked the symbol names, not just the module name, so a `from proteinsmc.utils import split_key_batched` style import would have been caught; utils/__init__.py re-exports 20+ names from sibling modules but does not re-export key_management (no `.key_management` line in its import block). Second-physical-copy check: asr/vendor/proteinsmc is a separate git checkout and does carry src/proteinsmc/utils/key_management.py, but has no importer of it either. The delete is loud, not silent (its test file fails immediately). Two additions the row should carry: tests/utils/test_key_management.py and the docs:365 coverage row go with it, and the vendored asr copy is untouched by a fix in the main repo.",
      "evidence": "proteinsmc/src/proteinsmc/utils/key_management.py:17,55; proteinsmc/src/proteinsmc/utils/__init__.py:1-52"
    },
    {
      "id": "R80",
      "survives": false,
      "downgraded_to": "EXECUTABLE_WITH_CHANGES (bug confirmed; 'corrected_action: None' is wrong)",
      "objection": "The defect itself survives attack \u2014 verified at runner.py:54-79 (6 registry entries), :303 (`final_state = None`), :304 (`if config.sampler_type == \"smc\":`), :315 (the sole `run_fn(...)` call, inside that branch). But the clearance is wrong on two things it asserted. (1) SECOND PHYSICAL COPY, unmentioned: asr/vendor/proteinsmc is a separate git checkout and its src/proteinsmc/runner.py carries the identical registry and the identical guarded dispatch at the same lines (:54 SAMPLER_REGISTRY, :57-77 the six run_fn entries, :176/:183 the validate-then-lookup, :256 `run_fn = sampler_def[\"run_fn\"]`, :304 `if config.sampler_type == \"smc\":`, :315 the single call). A fix landed only in the main repo leaves asr's vendored runner silently broken in exactly the same way. (2) SEVERITY IS INFERENCE: 'live and hit by any caller selecting a non-smc sampler_type' \u2014 no in-repo production caller can select one. The only src caller of run_experiment is proteinsmc/src/proteinsmc/oed/experiment.py:111, and the config it passes comes from convert_design_to_config, whose return annotation and constructor are SMCConfig (experiment.py:41, :60); `rg sampler_type src/proteinsmc/oed/` returns ZERO hits. Every other run_experiment reference in the repo is a test (tests/test_runner.py:52,61,108,169,210; tests/test_runner_jax_inputs.py:69), and those patch SAMPLER_REGISTRY['smc'] with a Mock. Reading '6 registry entries' as '6 exercised dispatch paths' is the structural-as-semantic move.",
      "evidence": "asr/vendor/proteinsmc/src/proteinsmc/runner.py:304,315; proteinsmc/src/proteinsmc/oed/experiment.py:41,60,111"
    },
    {
      "id": "R81",
      "survives": false,
      "downgraded_to": "REFUTED",
      "objection": "The narrow negative is true \u2014 `rg 'class Proposal'` over aminx/src, proteinsmc/src and xtrax/src returns zero, and `decoder_proposal` appears nowhere \u2014 but it was run narrower than the claim it supports ('aminx already has exactly the building blocks this new function's signature needs'). A case-insensitive search for 'proposal' finds aminx ALREADY implements proposal machinery, which the clearance never looked at: aminx/src/aminx/potts/sampling.py:102-166 is 'Discrete Langevin Monte Carlo (DLMC) with Zanella locally-balanced proposals', computing the forward proposal distribution q(s'|x_i) at :144 and the REVERSE proposal probability q(x_i|s') at :149-159 for the MH ratio; aminx/src/aminx/ebm/langevin.py:177-209 is 'One Metropolis-Hastings-corrected Langevin proposal/accept step'. That is the prior art defining what a proposal is in aminx, and it is a density, not a shape. Against it, the clearance's cited destination is a shared word: SamplerFn (aminx/src/aminx/types/protocols.py:69-80) returns `tuple[ProteinSequence, Logits, DecodingOrder]` and no proposal log-density \u2014 the one quantity an SMC/MH proposal is defined by and the one thing potts/sampling.py bothers to compute. 'proposal-shaped tuple' and 'decoder_proposal would just be a thin wrapper' are therefore unsupported; a Proposal usable for importance weighting needs SamplerFn AND a scoring path (ScoreFn, protocols.py:~84), not a wrapper. Adding a fresh `Proposal` type beside potts/ebm's existing conventions reproduces exactly the third-dialect failure the same pass flags in R82.",
      "evidence": "aminx/src/aminx/potts/sampling.py:144,149-159; aminx/src/aminx/types/protocols.py:69-80"
    },
    {
      "id": "R82",
      "survives": false,
      "downgraded_to": "REFUTED",
      "objection": "The row's one piece of claimed 'direct, independent confirmation' is contradicted by the destination's own source. The clearance says prolix 'maintains a distinct, domain-specific AxisSpec dialect and adapts it at the xtrax boundary rather than using xtrax's AxisSpec directly', inferring that from an aliased import (xtrax_adapter.py:17 `AxisSpec as XtraxAxisSpec`) plus a converter (:34). Read the surrounding files: prolix/src/prolix/tiling/planner.py:6-8 documents that planning was moved to `prolix.tiling.xtrax_adapter.plan_axes_with_xtrax` under the tag **XR-KILL-FORK**, planner.py:126 imports and delegates to it, and the adapter's own module docstring (xtrax_adapter.py:1-7) states it 'delegates strategy selection to xtrax joint MemoryBudget planning ... No secondary host demotion loop.' Three prolix API modules route through it (api/ensemble_planner.py:19, api/step_carry.py:11, api/ensemble_dedup.py:27). So prolix kept a legacy public dataclass at the seam while KILLING the rival planner \u2014 the opposite of maintaining a second dialect \u2014 and the file is evidence FOR consolidation, not against it. Second break, self-inflicted: the corrected_action orders 'DELETE the three stands' while the reachability field admits call sites were 'not independently traced'. They are not interchangeable. `chunked_map` has five live callers plus a package export (oed/smc_tree.py:83,144 \u2014 the OED batched runner CLAUDE.md names; utils/memory.py:137,142; utils/pmap_utils.py:51; utils/__init__.py:31), so deleting it breaks live OED code. `distribute` (utils/pmap_utils.py:19) is exported (utils/__init__.py:52,85) with zero internal callers. padding.py has NO importer anywhere in src/ or tests/ \u2014 SEQ_BUCKETS is used only inside its own file (padding.py:41,44) \u2014 i.e. an already-dead 142-line module. Three different reachability statuses collapsed into one verb.",
      "evidence": "prolix/src/prolix/tiling/planner.py:6-8,126; prolix/src/prolix/tiling/xtrax_adapter.py:1-7; proteinsmc/src/proteinsmc/oed/smc_tree.py:83,144"
    },
    {
      "id": "R78",
      "survives": false,
      "downgraded_to": "REFUTED (the clearance's own corroborating evidence is itself a shared-word error)",
      "objection": "R78 corroborates B.3.16 with proxide's ccd_chem_comp_types.proto, calling it 'real nucleotide-adjacent content'. Read it: proxide/crates/proxide-core/proto/ccd_chem_comp_types.proto:4-9 is 'a minimal, bundled subset of the official PDB Chemical Component Dictionary (CCD): just the id -> chem_comp.type mapping needed to classify a residue name as a peptide/nucleotide/other component, regardless of a source file's raw ATOM/HETATM flag. Deliberately excludes atom/bond geometry.' Its message fields are `id` ('ALA','MSE','HIP') and `chem_comp_type` ('L-PEPTIDE LINKING','NON-POLYMER') (:16-17). That is polymer-class labelling of structural residue names \u2014 no codon table, no genetic code, no nucleotide alphabet, no translation. proteinsmc's nucleotide/codon content is genetic-code machinery: utils/translation.py, utils/constants.py (CODON_TO_RES_CHAR, CODON_INT_TO_RES_INT_JAX, ECOLI_CODON_FREQ_JAX \u2014 see utils/__init__.py:12-27), scoring/cai.py. The word 'nucleotide' is shared; the thing is not. This is B.7 shape 1 committed inside the pass meant to catch it. The remaining proxide hits are force-field assets (src/proxide/assets/charmm/charmm36_cgenff.xml, openmm_bundled/charmm36.xml), also not codon content. Second break: the file count is not reproducible. The stated 'naive `grep -rln 'nucleotide|codon' src/proteinsmc` (excluding tests) returns exactly 13 files' \u2014 I get 15 (models/__init__.py, models/sampler_base.py, models/smc.py, models/types.py, oed/experiment.py, runner.py, sampling/initialization_factory.py, scoring/cai.py, types.py, utils/__init__.py, utils/constants.py, utils/initiate.py, utils/metrics.py, utils/mutation.py, utils/translation.py); union with `rg -l sequence_type src/` (8 files) is 16 distinct. Neither 13 nor 19 is reproducible, and the clearance used its own unreproducible 13 to declare the unverified 19 'structurally consistent' \u2014 filling a gap with inference, which it then admits ('I did not independently enumerate all 19').",
      "evidence": "proxide/crates/proxide-core/proto/ccd_chem_comp_types.proto:4-9,16-17; proteinsmc/src/proteinsmc/utils/__init__.py:12-27"
    }
  ]
}