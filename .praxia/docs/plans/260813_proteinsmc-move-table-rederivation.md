# Addendum D — Move-Table Re-Derivation

Every one of the 82 move-table rows re-derived under Addendum B.7's rule ("for every row, read the destination's source and state what makes the two things the same kind of thing") and Addendum C's rule ("a claim about code is not verified until you confirm that code runs"), then adversarially challenged.

---

## D.0 Direct answer

**The move table is not executable.** Of 82 rows, **8 survive as executable, and not one of them moves anything**: six are deletions of dead files (`padding.py`, `models/lineage.py`, `utils/jax_uuid.py`, `utils/key_management.py`, `utils/config_unpacker.py`, `sampling/particle_systems/__init__.py`), one (R76) is a duplicate row for a file another row already deletes, and one (R75) is a no-op correctly scoped out. **Zero MOVE, SPLIT, MERGE, or REWRITE rows are executable today.** The derivation pass cleared 54 rows to some degree (15 `EXECUTABLE`, 39 `EXECUTABLE_WITH_CHANGES`); every one of those 54 was challenged and **39 broke — 72%**. The remaining 28 rows were never cleared by the derivation pass and were never challenged, so they carry one pass of scrutiny against a 72% break rate on the rows that got two.

Final distribution, each row assigned to exactly one section (its most-blocking verdict; executable sub-parts are named inside the row rather than split across sections):

| Section | Verdict | Count |
|---|---|---|
| D.1 | Executable as written | 8 |
| D.2 | Executable with corrected scope or action | 22 |
| D.3 | Wrong destination | 9 |
| D.4 | Blocked on an unmade decision | 36 |
| D.5 | Do not execute | 7 |
| | **Total** | **82** |

Where a derivation-pass clearance was broken by challenge, **the challenger's verdict is reported below, not the deriver's.** Section D.6 lists all 39 breaks.

### D.0.1 Five cross-cutting facts that govern the whole table

**(1) Nothing in this pass was verified by execution.** proteinsmc's declared dependency set does not resolve — `pyproject.toml:20` lists `prxteinmpnn` as a hard dependency, and `uv run` fails with "Because prxteinmpnn was not found in the package registry ... your project's requirements are unsatisfiable"; `.venv/lib/python3.13` contains only `_virtualenv.pth`/`_virtualenv.py`, i.e. no installed packages at all. **Addendum C's second rule could not be satisfied for any row in this table.** Every liveness and deadness claim below is a static read. This is the single largest unverified assumption in the document and it is stated once here rather than repeated 82 times.

**(2) Every phase-2 destination is absent.** `api.py`, `kernels/`, `compose.py`, `schedules.py`, `landscapes/`, `codon/`, a `proposals` package, and `aminx/fitness/` do not exist in any repo (verified by `find`/`ls` across proteinsmc, aminx, xtrax, proxide, prolix). B.7's remedy — read the destination's source — is *unsatisfiable by construction* for 20+ rows, which is why "no destination evidence" appears so often in D.4. A row that cites its destination's non-existence as its destination evidence (R29, R31, R35, R64) has not been cleared; it has been described.

**(3) There is a second physical copy of proteinsmc and the table addresses one.** `asr/pyproject.toml:108` declares `members = [..., "vendor/proteinsmc", ...]` and `:76` `proteinsmc = { workspace = true }`; `asr/uv.lock:3202` resolves `source = { editable = "vendor/proteinsmc" }`. That checkout is on a local branch (`asr/vendor/proteinsmc/.git/HEAD` → `ref: refs/heads/fix/asr-3713-lazy-mpnn-import`, off pinned rev `8154daa` per `scripts/bootstrap_vendor_proteinsmc.sh:11`) and carries a real patch (`scoring/mpnn.py` repointed `prxteinmpnn`→`aminx`, commit `36257e7`). **Every DELETE in D.1 must be applied twice or the vendored copy silently diverges.**

**(4) Five of six registered samplers never execute.** `runner.py:303` sets `final_state = None`; `:304` `if config.sampler_type == "smc":` is the only branch that calls `run_fn` (`:315`); `:332` logs "Run completed successfully." regardless. Any row justified by "this sampler is live because it is in `SAMPLER_REGISTRY`" is justified by dict membership, not execution. Rows R29, R30, R36, R37, R57 all made that error and all broke.

**(5) One alphabet bug contaminates seven rows and no row owns the fix.** `utils/constants.py:10` defines `restypes` in AlphaFold order; `:33-34` `restype_num = 20` / `unk_restype_index = restype_num`; `:113` `AA_CHAR_TO_INT_MAP = restype_order`; `:115-117` `PROTEINMPNN_X_INT = 21` with `STOP_INT`/`UNKNOWN_AA_INT` aliased to it; `:272-276` allocates `PROTEINMPNN_TO_ESM_AA_MAP_JAX` at length 20 and is indexed at 21 (`utils/esm.py:52`), clamping to `restypes[19] == 'V'`. The compensating `'X'` branches at `:290-298` are dead, because `AA_CHAR_TO_INT_MAP` has no `'X'` key. Rows R05, R20, R38, R50, R55, R67, R70 each touch this and each defers it to another row.

### D.0.2 The table is not 82 distinct sources

**31 of the 82 rows belong to 14 overlap groups**, two of which are outright contradictions:

| Group | Rows | Note |
|---|---|---|
| `types.py` | R05, R71 | **Contradictory**: R05 → REWRITE to a new vocabulary; R71 → KEEP local aliases |
| `oed/smc_tree.py` | R48, R73 | **Contradictory headlines**: R48 "live, deduplicate"; R73 "dead, do not execute" (reconciled by copy — see D.7) |
| NK landscape | R19, R34, R45, R53, R77 | Five rows, one unmade ownership decision (§8 Q4) |
| `utils/memory.py` family | R17, R57, R68 | R57 and R68 contradict each other on whether xtrax has a destination |
| `padding.py` | R03, R69, R82 | Three rows, one dead file |
| `utils/esm.py` | R50, R67 | Same file, same destination, neither row mentions the other |
| `utils/key_management.py` | R65, R76 | Same file, same DELETE |
| codon/translation | R20, R61, R78 | Plus R38 as consumer |
| `utils/jax_utils.py` | R58, R82 | R82 says DELETE `chunked_map`; it has 5 live callers |
| `utils/pmap_utils.py` | R60, R82 | |
| `io.py` git provenance | R02, R74 | Both broke on the same three facts |
| `runner.py` dispatch | R04, R80 | Same bug, two rows |
| annealing | R16, R52 | |
| alphabet constants | R55, R70 | |

---

## D.1 Executable rows

Eight rows. Six are deletions; one is a duplicate deletion; one is a no-op. Each survived challenge; each carries the vendor-copy caveat from D.0.1(3).

| id | source | action | destination | same-kind-of-thing justification (+ destination citation) |
|---|---|---|---|---|
| R03 | `src/proteinsmc/padding.py` | DELETE | — | No destination to justify: dead at repo *and* ecosystem scope. All ten public names (`MAX_POPULATION:21`, `MAX_SEQ_LEN:22`, `SEQ_BUCKETS:25`, `get_seq_bucket:28`, `pad_population:47`, `pad_sequence:68`, `create_particle_mask:87`, `create_sequence_mask:103`, `masked_mean:117`, `masked_sum:131`) have zero references outside the file across proteinsmc, asr, aminx, xtrax, proxide, prolix, bathos. **The rationale's "xtrax owns this" is struck**: `xtrax/src/xtrax/tiling/bucket.py:1-20,28` is host-side NumPy pre-JIT by its own docstring and has no `masked_mean`; the actual descendant is `aminx/src/aminx/tiling/buckets.py:1-11,19,103` (near-verbatim docstring, `LENGTH_BUCKETS`, and it *does* have `masked_mean`). The delete stands on deadness alone. `asr/src/asr/oed/padding.py:17,21-23` is a live independent fork on a different axis set and is untouched. |
| R22 | `src/proteinsmc/models/lineage.py` | DELETE | — | Nothing to rehome: the file is one line, `"""Module for lineage tracking models."""` (`:1`), with no class, function, or assignment. `models/__init__.py` (26 imports, 30-entry `__all__`) never references `.lineage`. Cross-repo grep returns only `bathos.query.lineage`, an unrelated function. |
| R24 | `src/proteinsmc/sampling/particle_systems/__init__.py` | DELETE | — | Pure re-export shim with zero package-level consumers; `sampling/__init__.py:7-8` already imports `.particle_systems.parallel_replica` and `.particle_systems.smc` directly, as do `runner.py:29,32` and the tests. Packaging survives: `pyproject.toml:34-35` uses setuptools `find_packages`, whose `namespaces` defaults to `True`, so the directory stays discoverable. Residual (non-blocking): the subpackage silently becomes PEP-420; nothing introspects it. |
| R59 | `src/proteinsmc/utils/jax_uuid.py` | DELETE | — | Byte-identical duplicate: `jax_uuid.py:16-58` == `jax_utils.py:63-105`, diffed with no whitespace or docstring drift. Zero importers of the module; the only consumers of `generate_jax_uuid`/`generate_jax_hash` are `tests/utils/test_jax_utils.py:7-8`, which import from `jax_utils`. `utils/__init__.py:31` imports only `chunked_map` from `jax_utils`; neither UUID symbol is in `__all__` (`:55-99`). |
| R65 | `src/proteinsmc/utils/key_management.py` | DELETE | — | Both `def`s (`split_key_for_sampler:17`, `split_key_batched:55`) have exactly one consumer, `tests/utils/test_key_management.py:9`. `utils/__init__.py` neither imports the module (`:3-53`) nor exports either name (`:55-99`), so there is no re-export path. Failure is loud (the test file), not silent. |
| R66 | `src/proteinsmc/utils/config_unpacker.py` | DELETE | — | One public symbol, `with_config:17`; one consumer, `tests/utils/test_config_unpacker.py:10`; absent from `utils/__init__.py`. The delete *removes* an edge into `models/` (`config_unpacker.py:12` imports `BaseSamplerConfig`) rather than creating a dangling one. |
| R76 | `src/proteinsmc/utils/key_management.py` | DELETE | — | **Duplicate of R65** — same file, same verdict, reached independently via a seven-repo symbol search (only hits: the file's own defs and docstring examples at `:42,:73`, its test file, and `docs/TESTING_COVERAGE_ASSESSMENT.md:365`). Counted separately because the table lists it separately; it is not a second unit of work. |
| R75 | `xtrax/src/xtrax/loop/` (26 files) | **NONE — out of scope** | — | Correctly scoped out and it survives the obvious shared-word trap ("loop" ≠ `run_smc_loop`/`run_mcmc_loop`): `xtrax/src/xtrax/loop/promotion_gate.py:1-8` is "Promotion-to-main gate (T2-30, #2181, AC-23)", an autoresearch governance/attestation subsystem alongside `admission.py`, `attestation_evidence_gate.py`, `campaign_approval_gate.py`. proteinsmc has zero references to xtrax anywhere in `src/` or `pyproject.toml`. |

**Operational reading: 6 files to delete, in two checkouts each.**

---

## D.2 Rows requiring changed scope or action

22 rows. Seven survived challenge with their `EXECUTABLE_WITH_CHANGES` verdict intact (R04, R05, R07, R21, R52, R63, R69); fifteen were broken and are reported at the challenger's corrected scope.

**R02 — `io.py` git shell-out → bathos.** *Clearance broken.* The load-bearing claim ("bathos's `git.py` is a strict superset") is false four ways. Corrected row: **REWRITE `io.py:27-50`, five preconditions.** (a) `bathos/src/bathos/git.py:15,39` returns the sentinel `GitState(hash="unknown")` and can never return `None`, while `io.py:27` returns `str | None` (`:36,:50`) — `metadata.json`'s `git_commit_hash` (`io.py:85`) flips from `null` to the string `"unknown"`, contradicting `tests/io/test_io.py:42-64`; map `hash=="unknown"` → `None` or accept and document the format change. (b) `io.py:41-47` passes `timeout=5` and catches `TimeoutExpired` (`:49`); `bathos/git.py:20-38` uses bare `check_output` with no timeout and does not catch it — a hung git blocks `run_experiment` forever, and `test_io.py:58-64` exists to pin that. (c) `capture_git_state(cwd: Path = Path.cwd())` (`git.py:18`) binds CWD at *import* time; every existing bathos caller passes an explicit cwd (`checker.py:65`, `decorators.py:38`, `postmortem.py:257`, `mcp.py:2213`) — proteinsmc would be the first to hit it. (d) `bathos` is not in `pyproject.toml:10-25` and is not installed; this adds a sampling-library → experiment-tracking-tool dependency edge. (e) The plan contradicts itself: `:250` says "→ bathos", `:435` says "deleted". Resolve before editing. *Unbroken*: `get_git_commit_hash` is the sole git shell-out (`subprocess` imported once at `:7`, used once at `:41`), and it is live (`runner.py:51,253` ← `oed/experiment.py:27,111`).

**R04 — `runner.py`.** *Survives.* Two ordered steps, explicitly. **(a) Phase-0, no dependencies:** fix `:303-304` — generalize the dispatch to all six registered branches or `raise NotImplementedError` for unhandled ones. This breaks no test: `tests/test_runner.py:38` asserts only that the registry advertises six names, not that they execute. **(b) Later, gated on §8 Q2:** reduce to a `config -> kernel -> drive.sample()` shim; `find src -name api.py`, `-name drive.py`, `-type d -name kernels` all return nothing. Added by challenge: step (a) does **not** reach asr until `scripts/bootstrap_vendor_proteinsmc.sh` re-runs — `asr/vendor/proteinsmc/src/proteinsmc/runner.py` is currently byte-identical and keeps the no-op. The "asr never calls it" negative survives a widened search including `.ipynb` and `vendor/`: every asr `run_experiment` hit is asr's own same-named function.

**R05 — `types.py`.** *Survives, materially extended.* **REWRITE, not "STAY, trimmed"** — the proposed vocabulary (`Population`, `Relaxed`, `Scores`, `LogWeights`, `Ctx`) is disjoint from the current one; only `PRNGKey` (`types.py:51`) survives. Three additions the corrected row must carry: (i) there is a **second** `types.py` — `models/types.py:5-21` re-exports 15 of 16 names with a matching `__all__` (`:23-39`), and `models/__init__.py:22-26` lifts three of them into the public `proteinsmc.models` namespace; its "Legacy ... now redirected" docstring is *not* a deadness signal. (ii) The importer set is **nine** modules (`utils/translation.py:20`, `utils/metrics.py:17`, `utils/esm.py:26-27`, `utils/annealing.py:20`, `sampling/nuts.py:28`, `sampling/hmc.py:20`, `sampling/mcmc.py:20`, `sampling/gibbs.py:21`, `models/types.py:5`), and the most-imported names are `ArrayLike` and `ScalarFloat` — the two the row leaves with no stated fate. (iii) **The replacement must state the alphabet ordering.** `Population = Int[Array, "n L"]  # int8, values in [0, q)` pins a size and not an order; per D.0.1(5) the repo encodes AlphaFold order under ProteinMPNN-named constants. A cross-repo contract type that pins `q` but not the ordering hard-codes that bug into the interface.

**R07 — `models/protocols.py`.** *Survives, scope corrected.* DELETE is safe *now*, independent of api.py: the file's three protocols are referenced only from `sampling/particle_systems/smc.py:22`, inside `if TYPE_CHECKING:` (`:17`) under `from __future__ import annotations` (`:3`), and nothing evaluates those strings at runtime — `jaxtyped|beartype|typeguard|get_type_hints|install_import_hook` returns **zero** hits across `src/proteinsmc`, `pyproject.toml`, and `tests/conftest.py`. **But it is not a single-file delete**: deleting `protocols.py` orphans ten annotation sites in `smc.py` (`:50,:79,:80,:81,:101,:102,:103,:169,:170,:172`), which are runtime-inert but not tool-inert — `pyproject.toml:47` sets `select = ["ALL"]` (F821) and `uv run ty check src/proteinsmc` is a documented gate. Corrected action: **delete `protocols.py` and, in the same commit, remove `smc.py:22` and repoint or erase its ten annotation sites.** When `api.py` eventually lands, do not alias-swap `protocols.FitnessFn` for `fitness.FitnessFn`: `protocols.py:51` types the third arg `dict[str, Array|float|int]|None`, `models/fitness.py:17` types it `PyTree|Array|None`.

**R16 — `models/annealing.py` → `schedules.py`.** *Clearance broken.* Corrected row: **MERGE four files, not one, and resolve a name collision first.** (a) The reachability sentence is false — `models/smc.py:63` and `models/parallel_replica.py:30,42` are field *annotations* behind `if TYPE_CHECKING:` (`smc.py:18-19`, `parallel_replica.py:10,15`), not instantiations; the only construction sites in the tree are `tests/conftest.py:103,162,202`. (b) **`AnnealingFn` exists twice and they are different**: `models/annealing.py:10` is `Callable[[Any, Any], Any]` (what `get_annealing_function` returns), `models/protocols.py:94` is a `Protocol` with `__call__(step, context=None)`. The live SMC kernel imports the *Protocol* (`smc.py:22`) while the object actually passed has first parameter `current_step` (`utils/annealing.py:47`). A codemod repointing the name can silently bind the wrong one. (c) A fourth path: `models/sampler_base.py:29` imports `AnnealingConfig` from `proteinsmc.utils.annealing`, working only via the TYPE_CHECKING re-export at `utils/annealing.py:16-19`.

**R20 — `models/translation.py`.** *Clearance broken.* Corrected row: **MERGE `models/translation.py` + `utils/translation.py`, sequenced after R21, with the alphabet decided and the `cai.py` justification struck.** The "sibling module" argument is a shared-word move: `scoring/cai.py:25` is a *fitness factory* (`make_cai_score() -> FitnessFn`) and the row's own action leaves it in `scoring/`, so it would not sit beside `codon/translation.py`; and no `codon` directory exists in any repo. The alphabet question bites hardest here — `utils/translation.py:54` documents "Uses ColabDesign's AA integers" while its tables are ProteinMPNN-named (`:10-15`, used at `:72-73,:95-96`) and AlphaFold-ordered. Ordering dependency: `models/translation.py:9` imports `EvoSequence` from `proteinsmc.models.types`, the shim R21 deletes.

**R21 — `models/types.py` DELETE.** *Survives.* Attacked three ways (missing names, circular import, wider grep) and held: all 15 re-exported names resolve (`Vmapped:31` and `VmappedTranslation:42` are Protocol *classes*, not assignments); `types.py:3-8` imports nothing from `models`; the count reproduces exactly at 15 src importers + 6 test files. **Corrected action: the codemod must handle three import forms, not one** — `from proteinsmc.models.types import X`, the relative `from .types import (...)` at `models/__init__.py:22`, and the module-object form `from proteinsmc.models import types as proteinsmc_types` at `tests/models/test_types.py:10` (dereferenced at `:30,:33`), which a symbol-level substitution silently misses.

**R33 — `scoring/__init__.py` DELETE.** *Clearance broken.* Action stands; **both its rationale and its "changes nothing" claim are false.** The `find_spec` gate is forced by a different chain the row never traced: `proteinsmc/__init__.py:3` → `utils/__init__.py:29` → `utils/fitness.py:11` `from proteinsmc.scoring import cai, combine, esm, mpnn, nk`. Deleting `scoring/__init__.py` does **not** close that gate. Conversely the delete is not neutral: today `scoring/__init__.py:3-6` eagerly imports all four scorers, so `import proteinsmc.scoring` pulls them; afterwards it is a namespace package importing nothing. The deadness negative *does* hold under a seven-repo search: the only package-level import is `utils/fitness.py:11` (submodules, not the re-exported names), and tests import submodules directly (`tests/scoring/test_esm.py:12`, `tests/scoring/test_cai.py:10`).

**R39 — `oed/__init__.py` DELETE.** *Clearance broken.* Corrected row: **verify packaging first; the reachability sentence is wrong.** `proteinsmc/__init__.py:3` imports `io, models, runner, sampling, scoring, utils` — **`oed` is not in that list**, so this file does not run at package-import time; it runs on direct `proteinsmc.oed.*` imports, whose live examples are cross-repo (`asr/scripts/analyze_oed.py:11-12`, `asr/scripts/diagnose_gp.py:4-5`). And `pyproject.toml:34-35` sets `[tool.setuptools.packages.find] where = ["src"]` with no explicit `namespaces` key; whether `proteinsmc.oed` survives into a built wheel therefore depends on a setuptools default that **could not be verified in this environment** (empty venv, unresolvable deps — D.0.1(1)). If it excludes namespace packages, this delete silently drops `proteinsmc.oed` from the distribution and breaks the two asr scripts at import. `failure_shape: none` was not earned.

**R41 — `oed/structs.py`.** *Clearance broken on evidence; conclusion survives.* **Do not merge into `asr/oed/structs.py`** — confirmed. Four cited facts corrected: field overlap is **five**, not three (`N, K, q, mutation_rate, branch_length`); one of the five has a **type conflict** the row missed (`proteinsmc structs.py:31 branch_length: Float = 1.0` vs `asr structs.py:44 branch_length: Int`) that a merge would have silently reconciled; `OEDFeatureMode` is **not byte-identical** (proteinsmc `:13-17` 2-space per its ruff config, asr `:8-12` 4-space) though semantically equal, so the dedup is still safe; and `perturb_design` (`opt.py:11-15`) hardcodes nothing — it is `getattr`/`replace` generic, the six names live at `opt.py:44` inside `calculate_fim_determinant` beside `n_params = 6` at `:39`. **Destination collision, unlisted:** `asr/oed/structs.py:16-21` already defines `SMCHyperparams` and `ASRDesign` already carries `use_smc`/`smc_params` (`:45-46`), so a file named `smc_design.py` creates a second competing SMC-design struct. Rename before landing. The repoint of `asr/scripts/analyze_oed.py:12` and `diagnose_gp.py:5` is genuinely required.

**R52 — `utils/annealing.py` → `schedules.py`.** *Survives.* The refutation holds on direct read of both sides: `prolix/src/prolix/pt/temperature.py:13-43` is replica-indexed, vector-returning, Kelvin-ascending (`:20-21`), consumed at `replica_exchange.py:284`; `utils/annealing.py:76-105` is step-indexed, `@partial(jit, ...)`-wrapped (`:75`), scalar-returning, beta-ascending, clipped at `:105`. Different axis, arity, and physical quantity. **Strike "absorbs prolix's geometric/linear ladders" entirely.** Two corrections: the reachability sentence overstates — `get_annealing_function` is wired (`runner.py:33,259-261`) but the only src caller passes `annealing_config=None` (`oed/experiment.py:73`), so the schedules are reached only from test fixtures; and the blast radius omits `models/sampler_base.py:29`, which imports `AnnealingConfig` from a module that never defines it.

**R56 — `utils/metrics.py`.** *Clearance broken.* Corrected row: **keep `metrics.py` in proteinsmc; delete only the dead pair.** The "only caller is `oed/experiment.py`" negative was asserted from a search narrower than the claim — `scripts/run_oed_deserialization_manual.py:10` imports the module and calls `jeffreys_divergence:88`, `calculate_barrier_crossing_frequency:103`, `shannon_entropy:107`, `jensen_shannon_divergence:117`. That kills the prescribed action ("travel with `oed/experiment.py`"), which R46 independently kills again by keeping `experiment.py` in proteinsmc. The unit is also miscounted: **eight** public functions (`:100,:116,:139,:175,:191,:208,:233,:251`), with `jeffreys`/`jensen_shannon` both delegating to `kl_divergence:139`, so no subset moves cleanly. Destination collision unchecked by the row: `asr/src/asr/metrics.py` already exists. *Surviving*: `safe_weighted_mean:25` and `calculate_logZ_increment:65` have no caller outside `tests/utils/test_metrics.py` anywhere under `/home/marielle/projects`, and there is no ESS function in the file — but "DELETE" understates that both are public API (`utils/__init__.py:41-43`, `__all__` `:79,:95`).

**R57 — `utils/memory.py`.** *Clearance broken.* Corrected row: **split the file three ways, not two.** `get_device_memory_mb` (`memory.py:55-70`) and `estimate_memory_usage` (`:49-52`) **do** have an xtrax destination the clearance missed by reading only one file in the package: `xtrax/src/xtrax/tiling/estimators.py:28-57` `device_memory_budget` performs the identical `dev.memory_stats()`/`stats['bytes_limit']` read, and its docstring (`:1-8`) states its purpose is to replace "hand-rolled byte math". Only `benchmark_batch_size`/`auto_tune_batch_size` (`:139-147,:170-232`) have no destination. Two further corrections: reachability was cited from `__all__` (a structural signal) when the actual gate is `runner.py:112` on `enable_auto_tuning` (default `True`, `models/memory.py:48`), call site `runner.py:36,131`; and the swap changes behaviour silently in one direction — `get_device_memory_mb` swallows three exception types and returns 8192 MB (`:69-70`) where `device_memory_budget` raises with "no silent fallback" (`estimators.py:52-56`).

**R58 — `utils/jax_utils.py`.** *Clearance broken twice, both shape 3.* Corrected row: **four `chunked_map` call sites, and `smc_tree.py` is LIVE-via-asr, not latent.** (a) The fourth site is `utils/pmap_utils.py:51`, in module-qualified form (`from proteinsmc.utils import jax_utils` at `:16`), invisible to a symbol-import grep — and it sits *inside the pmapped worker* (`:49-51`), the one context where `safe_map`'s vmap-vs-`lax.map` branch (`xtrax/transforms/map.py:29-30`) changes device memory behaviour silently. (b) "Unimportable AND unreachable" is true only of the standalone checkout: inside asr, `trex` resolves (`asr/pyproject.toml:81,108`) and three asr modules import `smc_tree` (`nk_experiment.py:26`, `oed/experiment.py:31`, `generator_adapters.py:239`), making `smc_tree.py:83,144` live `chunked_map` sites in the ecosystem's actual consumer. (c) B's "third breaking site at `smc_tree.py:290`" is wrong — `chunked_map` is at `:83,:144`; `:284` is the trex `safe_map` call. (d) `tests/oed/test_smc_tree.py:9` imports it, so in the standalone repo that test errors at collection today.

**R60 — `utils/pmap_utils.py` DELETE.** *Clearance broken on its recommendation.* **DELETE stands and is strengthened** — `jax.pmap|shard_map|device_put_sharded` returns zero hits across *all* of `xtrax/src`, not just `sharding.py`, so xtrax genuinely never touches array data and the `xtrax.distributed.sharding` pointer is correctly struck. Also remove the re-export at `utils/__init__.py:52,85`. **But drop the `get_device_mesh` recommendation**: the row bounded its read at `sharding.py:1-80`, three lines before `def get_device_mesh` (`:83`) in a 174-line file; `sampler_base.py:147-151`'s `__post_init__` mesh construction is **live**, not dead (executed on every `BaseSamplerConfig` construction, asserted at `tests/models/test_sampler_base_advanced.py:40-44`); and xtrax's version has **no `None` path** and raises whenever `math.prod(shape) != len(jax.devices())` (`sharding.py:99-107`), while `device_mesh_shape` defaults to `None` (`sampler_base.py:48`, autodetect at `:159-164`). The swap makes the default config raise.

**R63 — `utils/serialization.py` DELETE.** *Survives, with one sequencing correction.* Live: `create_sampler_output_skeleton` is called at `oed/experiment.py:29,123` **and** `tests/test_end_to_end_io.py:14,59`; the `.arrayrecord` writers it reads are real (`runner.py:266`, `oed/tracking.py:339`). **The gate it defers to does not exist in code**: `SamplerKernel` returns zero hits across `src/` and `tests/`; it exists only as plan text (`260813_proteinsmc-ecosystem-partition.md:131,135,211,338`). State the dependency as "blocked on unwritten code", not "blocked on a landed switch".

**R69 — length bucketing.** *Survives, deadness strengthened.* DELETE stands; **drop the "consolidation achieved" framing** — `asr/src/asr/oed/padding.py` remains a live fork, so the duplication is not resolved by this delete. Two refinements: the `jnp` import is `padding.py:17` (not `:18`), and asr's fork is not quite the same artifact — it buckets NK parameters (`N_BUCKETS/K_BUCKETS/Q_BUCKETS`, `asr padding.py:21-23`) on a different axis set than proteinsmc's sequence-length-only `SEQ_BUCKETS`.

**R71 — `proteinsmc/types.py` KEEP.** *Clearance broken on its mandated correction.* **KEEP holds on substance** — the real overlap with `proxide/src/proxide/core/types.py` is five names (`:10,13,14,37,104` vs proteinsmc `:10,13,14,18,51`) against a 140-line atom/physics-dominated module. **But "proteinsmc already imports proxide, so the edge is not being cut" is exactly the Addendum-C shape it claims to avoid**: `mpnn.py:14` gates on `find_spec("prxteinmpnn")`, which is False (D.0.1(1)), so `from proxide.ops.dataset import ...` (`:17`) never executes and `create_protein_dataset` at `:83` is unreachable behind the `raise ImportError` at `:79-81`. Independent second defect: the guard tests package **A** (`prxteinmpnn`) while the guarded block imports package **B** (`proxide`) — `proxide` appears nowhere in `pyproject.toml:10-24`, so the line would `ImportError` even with `prxteinmpnn` present. Also unmentioned: `models/types.py` is a second type module with 16 src importers. Corrected rationale: KEEP because the overlap is five names; **not** because a live proxide edge already exists.

**R74 — git provenance → bathos.** *Clearance broken.* The destination is genuinely live (`bathos/src/bathos/git.py:18` has seven call sites: `checker.py:65`, `runner.py:410`, `decorators.py:38`, `postmortem.py:257`, `cli.py:969`, `mcp.py:2213`). **`corrected_action: None` is wrong on three counts.** (a) The justification covers only a quarter of the row's source: `add_oed_to_metadata` (`oed/tracking.py:272-301`) does **no git work** — it json-loads `metadata.json` and writes `oed_design`/`oed_phase`/`oed_iteration` (`:296-298`). "Provenance" is the shared word; **no bathos destination is named** for it or for `save_oed_record`/`load_oed_manifest`/`create_oed_summary`. (b) Same null→`"unknown"` sentinel change as R02 (`io.py:85` vs `git.py:15,38`). (c) Same lost `shutil.which` guard (`io.py:33`) and `timeout=5` (`:45`). And `bathos` is not a declared dependency. **R02 and R74 are the same edit; land one row, not two.**

**R78 — nucleotide/codon files, KEEP-quarantined.** *Clearance broken — this row's own corroborating evidence is a shared-word error.* `proxide/crates/proxide-core/proto/ccd_chem_comp_types.proto:4-9` is a CCD `id → chem_comp.type` map that classifies a **residue name** as "L-PEPTIDE LINKING"/"NON-POLYMER" (`:16-17`), explicitly excluding atom/bond geometry. That is polymer-class labelling of structural residue names — no codon table, no genetic code, no nucleotide alphabet. proteinsmc's content is genetic-code machinery (`utils/translation.py`, `CODON_TO_RES_CHAR`/`CODON_INT_TO_RES_INT_JAX`/`ECOLI_CODON_FREQ_JAX` per `utils/__init__.py:12-27`, `scoring/cai.py`). The word is shared; the thing is not. **B.7 shape 1, committed inside the pass meant to catch it.** The counts are also unreproducible: the row's "exactly 13 files" re-runs as **15**; union with `sequence_type` gives 16; B's 19 was never enumerated. Corrected row: **KEEP-quarantined stands as an action; both its stated rationale ("collides with nothing") and its file count must be re-derived by explicit enumeration before any codon row (R20, R38, R61) touches these files.**

**R80 — `runner.py` dispatch bugfix.** *Clearance broken on scope and severity.* The defect is confirmed verbatim (`:54-79`, `:303`, `:304`, `:315`). **Two corrections.** (a) `asr/vendor/proteinsmc/src/proteinsmc/runner.py` carries the identical bug at the identical lines (`:54,:57-77,:176,:183,:256,:304,:315`); a fix in one checkout leaves asr silently broken. (b) "Live and hit by any caller selecting a non-smc sampler_type" is inference: **no in-repo production caller can select one.** The only src caller of `run_experiment` is `oed/experiment.py:111`, whose config comes from `convert_design_to_config`, annotated and constructed as `SMCConfig` (`:41,:60`); `sampler_type` returns zero hits across `src/proteinsmc/oed/`. Every other caller is a test that patches `SAMPLER_REGISTRY["smc"]` with a Mock. **Merge with R04, which states the same fix correctly.**

**R82 — `chunked_map` / `distribute` / `SEQ_BUCKETS` triple DELETE.** *Clearance broken.* The one piece of claimed independent confirmation is contradicted by its own source: `prolix/src/prolix/tiling/planner.py:6-8` records that planning moved to `prolix.tiling.xtrax_adapter.plan_axes_with_xtrax` under the tag **XR-KILL-FORK**, `planner.py:126` delegates to it, and `xtrax_adapter.py:1-7` states it "delegates strategy selection to xtrax joint MemoryBudget planning ... No secondary host demotion loop", with three prolix API modules routing through it (`api/ensemble_planner.py:19`, `api/step_carry.py:11`, `api/ensemble_dedup.py:27`). prolix **killed** the rival planner and kept a legacy dataclass at the seam — evidence *for* consolidation. Corrected row: **three different verdicts, not one verb.** `padding.py` — already dead, DELETE (see R03/R69). `distribute` (`pmap_utils.py:19`) — exported (`utils/__init__.py:52,85`), zero internal callers, DELETE (see R60). `chunked_map` — **five live callers** (`oed/smc_tree.py:83,144`; `utils/memory.py:137,142`; `utils/pmap_utils.py:51`) plus a package export (`utils/__init__.py:31`); deleting it breaks live OED code.

---

## D.3 Wrong destination

Nine rows. For each: where it should go instead, or that no destination exists.

| id | source | stated destination | corrected destination |
|---|---|---|---|
| R15 | `models/nuts.py` (NUTSConfig, `:10-25`) | `kernels/nuts.py` | **Wrong source, right shape.** Every sibling kernel row moves the *loop*, never the config: `kernels/nuts.py` should receive `sampling/nuts.py`'s `run_nuts_loop` (`:33-136`), registered separately from the config (`runner.py:18,76` config_cls vs `:27,77` run_fn). Two live, differently-typed files sharing the word "nuts". Inherits G16 (no `kernels/`) and R30's finding that `run_nuts_loop` is never invoked. `models/nuts.py` needs its own row. |
| R17 | `models/memory.py` | `xtrax.tiling.MemoryBudget` | **No destination exists — REWRITE in place.** `MemoryBudget` (`xtrax/tiling/budget.py:33-47`) is a *declarative planner constraint*: it demotes axes Vmap→SafeMap until a caller-supplied estimate fits, and batch size is an **input** to it (`plan.py:37,49` `default_batch_size` carried unchanged into `SafeMap`). `auto_tune_batch_size` (`utils/memory.py:170,199-212`) *selects* a batch size by executing and timing each candidate in `probe_batch_sizes`, returning the winner into `runner.py:131-144`. No xtrax API chooses among probe sizes. Also: xtrax is not a proteinsmc dependency (`pyproject.toml:10-25`), the citation is wrong (`runner.py:36`, not `:34`), and half the type is dead (`enable_batched_computation`, `device_memory_fraction`, `models/memory.py:14-15`, read nowhere). The DELETE→REWRITE retitle stands; the target does not. |
| R18 | `models/mutation.py` | `api.Proposal` | **No `api` module and no `Proposal` class exists** in proteinsmc or aminx. The row's own justification ("11-line alias becomes a real protocol") is already satisfied by `models/protocols.py:57-83`, which defines `MutationFn(Protocol)` with the full `(key, sequence, context)` signature and is the version the live kernels use (`smc.py:22,50,80,102,170`; `parallel_replica.py:19,245,275,435`). **Corrected: repoint the two dead `_mutation_fn: MutationFn` hints (`sampling/nuts.py:27,40`, `sampling/hmc.py:19,33`) to `models.protocols.MutationFn`, then DELETE `models/mutation.py`.** |
| R31 | `sampling/ste.py` | `proteinsmc/kernels/ste.py` | **aminx already owns it — DELETE/dedup, do not re-fork.** `aminx/src/aminx/utils/ste.py:1-12` carries an identical docstring; `:33-40` `straight_through_estimator` is character-for-character identical to `sampling/ste.py:28-35`; `:67-97` `ste_loss` is proteinsmc's inner `get_loss` (`:48-72`) plus an `eps`. aminx's is live (`aminx/sampling/__init__.py:14,24`; `aminx/inference/optimize_ste.py` 325 lines importing it at `:44`) **and already optax-based** (`:23`) — the exact REWRITE this row prescribes, already done. Also `aminx/inference/decode/ste.py` (424 lines). The row's rationale correction is accurate (`ste.py:22-24` declared type unbatched vs the vmap at `:94`) and irrelevant once the destination is right. |
| R37 | `scoring/esm.py` | `aminx/fitness/esm.py` | **No destination exists, and the row refutes itself:** its own `same_kind_of_thing` states esm.py "introduces a **new kind of thing** to aminx", then clears on a weaker relation — a shared `hf_hub_download`→`eqx.deserialise` *mechanism*. By that standard every checkpointed model in the workspace belongs in aminx. `aminx/fitness/` does not exist; `find aminx/src -iname '*esm*'` returns zero. Reachability is registry membership (`utils/fitness.py:31`), not execution, and `make_esm_score` performs an HF fetch at factory time (`esm.py:39-43`). |
| R38 | `scoring/cai.py` | `proteinsmc/codon/cai.py` | **`codon/` does not exist and the bundling rationale is a shared-use relation dressed as shared domain.** `PROTEINMPNN_X_INT` is **not** codon-domain: `constants.py:10` `restypes` is an AA alphabet, `:33-34` `restype_num = 20` / `unk_restype_index = restype_num`, `:36` puts X at 20 — yet `:115` sets it to **21**, with `STOP_INT`/`UNKNOWN_AA_INT` aliased (`:116-117`). cai.py merely consumes it (`:65`). Moving it into a `codon/` package gives an off-by-one AA constant a new and more authoritative home. Its own docstring disagrees a third way (`cai.py:49` "ColabDesign's AA integers" vs a ProteinMPNN name over an AF order, `constants.py:113`). Blocked behind G18. |
| R46 | `oed/experiment.py` | `asr/oed/smc_experiment.py` | **No destination — it stays in proteinsmc.** The move orphans the source's only production caller (`oed/run.py:12`, called at `:215,:248`), which is proteinsmc's documented OED entry point, and inverts the declared direction (`asr/pyproject.toml:16` lists proteinsmc as asr's dependency). The destination has **zero demand**: a repo-wide search for `proteinsmc.oed.experiment`/`run_oed_experiment`/`convert_design_to_config` outside `archive/` returns only proteinsmc's own `run.py` and `tests/oed/test_experiment_integration.py:10`. The cited "4 importers" are importers of asr's *different* file. All eight of the source's imports are proteinsmc-internal (`:13,14-20,21,22,23,27,28,29`). The non-overwrite correction and the `convert_design_to_config` correction (`:35-41`, `:100-104`) are both right and both survive. |
| R67 | `utils/esm.py` | `aminx/model/esmc.py` | **Destination is contractually closed against it.** `aminx/src/aminx/model/__init__.py:1-12` declares a closed public contract by name (Aminx, PrxteinLigandMPNN, DiffusionAminx, Packer, Encoder/decoder, ProteinFeatures, ModelCapabilities); `model/capabilities.py:1` reads "Static capability flags for **MPNN** models." `ESMC` (`utils/esm.py:299-321`) satisfies none of it and has different weight provenance (`huggingface_hub`, `:16`) than aminx's zstd+`eqx.tree_deserialise_leaves` path (`aminx/io/weights.py:145-149`). `load_model` collides (`aminx/io/weights.py:151,165` vs `utils/esm.py:349`). Two unaccounted asr consumers (`asr/mtt_training_pipeline.py:25,30`, `asr/analysis/biophysics.py:7`). **Duplicate of R50** (D.5), which refutes the same move on alphabet grounds. |
| R70 | AA alphabet / MPNN↔AF permutation | proxide | **No single destination — proxide has not consolidated its own copies.** `proxide/src/proxide/chem/conversion.py:28-33` and `proxide/src/proxide/io/parsing/mappings.py:34-40` **both** independently define `af_to_mpnn`/`mpnn_to_af`; `chem/residues.py` (the plan's named location) contains neither; `aminx/utils/aa_convert.py:28,33` is a third copy, live on an executed path (`aminx/host/runner.py:956-959,1017-1020,1234-1237`; `_sampling_helper.py:570,581`). Blocked until proxide consolidates internally. **Unresolved conflict with R61**: if this executes as written, `codon/translation.py` — which stays in proteinsmc — loses the constants it imports at `translation.py:10-15` and uses as its validity sentinel at `:73,:95`. |

---

## D.4 Blocked

36 rows, grouped by the decision they wait on. Each row appears once; the group sizes sum to 36.

Where a block maps to a gate whose text I can cite from the inputs to this pass, I name that gate. **Where it does not, I state a new gate rather than assign it to G0/G1/G2/G3/G5–G10/G12–G14, whose content is not in front of me.** Two existing gates are cited on the evidence of the rows themselves: **G4** (per R51 — replace the CI-grep cycle check with a lazy-resolution + `sys.modules` test) and **G11** (per R79 — §1's premise about whether aminx can host an iterative sampler). The plan's own unmade decisions are cited as **§8 Q2** (dissolve `models/`+`utils/`? — plan doc `:528-530`, verbatim "decide it explicitly rather than letting it happen") and **§8 Q4/§6.6** (NK ownership).

| Gate | rows | n | the unmade decision |
|---|---|---|---|
| **§8 Q2 + G16** | R01, R06, R08, R09, R10, R11, R12, R13, R14, R23, R49 | 11 | Whether `models/` and `utils/` dissolve at all, and the authoring of `api.py`/`kernels/`/`schedules.py`. R01 (`__init__.py`) must reference nine names none of which exist; R06 is a shim re-exporting from absent modules; R08's `api.py` destination has no source, and its real importer set is **thirteen**, most of which import the *type aliases* at `models/fitness.py:14-22`, not the three dataclasses the split targets — and `FitnessEvaluator.needs_translation` (`:85-91`) is the **decision** half of the codon coupling (consumed at `utils/fitness.py:83-84`, driving `translate_func` at `:41-65`, selected by `runner.py:98-100`), so the alphabet assumption travels *into* the cross-repo contract. R09–R14 are the five `BaseSamplerConfig` subclasses that must migrate in one commit with R09; R23 and R49 are barrel files whose content is a function of the others' outcomes. |
| **G16 + G17** *(new)* | R25, R26, R27, R28 | 4 | `kernels/` does not exist, **and one runtime object has three declared contracts.** `smc.py:22` annotates `fitness_fn` as the `models.protocols` `FitnessFn` (documented `:33-36` as returning a scalar or `(pop_size,)`), while `runner.py:97` supplies `get_fitness_function`'s output, typed `StackedFitnessFn` = `Float[Array, "1+n_fitness_functions"]` (`utils/fitness.py:73`, `models/fitness.py:14,19-21`); `parallel_replica.py:18` and `mcmc.py:19` declare `StackedFitnessFn`; `gibbs.py:22` declares `FitnessFn` via `utils.fitness`. **G17: reconcile the fitness contract before any kernel moves.** Row-specific: R26's in-place `lax.cond` fix at `:155` (predicate scalarized at `:153`) is executable now — but strike the "two `migrate` definitions" flag, which is the TYPE_CHECKING/else overload idiom (`:26,28,47,48,50`, mirrored at `smc.py:80/83` and `gibbs.py:103/105`) — and note `:366` is a *call site*, not the bug. R27 additionally needs `PointFitness`, which returns **zero hits across all eight repos** (new design, not a MOVE), and `run_gibbs_loop` is the one loop whose signature is config-bound (`gibbs.py:105-107,164`), so `models/gibbs.py` cannot stay behind. R28's in-place `io_callback` removal (`mcmc.py:108-117`) is executable now; the `make_blackjax_log_prob_fn` adoption is struck (see D.5 reasoning: `blackjax_utils.py:39-44` returns the whole `StackedFitness` where `mcmc.py:69-72` returns `fitness[0]`, binds one key at construction vs. per-step `mcmc.py:67`, has zero src callers, and its docstring `:22-35` promises a tuple its body never returns). |
| **G16 + G20** *(new)* | R29, R30 | 2 | `kernels/` absent **and the code is unreachable**. `run_hmc_loop` and `run_nuts_loop` are imported (`runner.py:24-27`) and stored (`:71-77`) and never called; their only test classes are skipped (`tests/sampling/test_hmc_loop.py:42-43`, `test_nuts_loop.py:41-42`). R29's "4 known failures" justification is unsupported — those failures are ParallelReplica/MCMC/SMC/PRSMC state-init tests, and the HMC/NUTS ones at `test_initialization_factory.py:102,147,410,445` are skipped. R30's corrected_action is **inverted**: "DE-REGISTER a currently-callable sampler_type" is wrong; de-registering changes only the `ValueError` text at `runner.py:177-181`. The docstring at `nuts.py:1-7` ("conceptual placeholder") was right and the registry dict was the misleading signal. **G20: land R04's dispatch fix before any row asserts sampler liveness.** |
| **G16** *(new)* | R35, R54, R62, R64, R81 | 5 | The destination has no source to read. R35: `compose.py` absent, and the name claims a namespace xtrax already owns (`xtrax/src/xtrax/composition/`) for what is a fitness-score reducer (`combine.py:32,72`); the redesign also reaches `COMBINE_FUNCTIONS` (`utils/fitness.py:35-38,87-91`). R54: `proposals.PointMutation` has no owning package anywhere; the "unrecorded fork" premise is **false** — `utils/mutation.py:41-42` is per-element Bernoulli at a continuous rate, `trex/ground_truth.py:39-47` is a fixed count drawn without replacement over axis 0; only the generic offset-mod-q trick coincides. The `safe_map` swap is separately blocked: `xtrax/transforms/map.py:8,30,40` applies `fn` to the pytree as a **single** argument and hard-raises on non-divisible `n` (`:33-37`), while `chunked_map` unpacks tuples positionally (`jax_utils.py:55-58`) — which `mutation.py:217-221` relies on. R62: neither `proposals` nor `codon/init.py` exists. R64: `kernels/` collides in name with `sampling/`, which already holds all three consumers, and the rewrite spec is incomplete (the docstring's promised `(combined_fitness, {...})` aux contract, `blackjax_utils.py:22-35`, must be decided). R81: `Proposal` is undesigned — aminx's existing proposal machinery is *density*-shaped (`potts/sampling.py:144,149-159`; `ebm/langevin.py:177-209`) while `SamplerFn` (`types/protocols.py:69-80`) returns `(sequence, logits, decoding_order)` with **no proposal log-density**, the one quantity an importance-weighting proposal is defined by. |
| **§8 Q4 / §6.6** | R19, R34, R45, R53, R77 | 5 | NK/landscape ownership. The representations are incompatible: `models/nk_landscape.py:33-44` is a frozen `PyTreeNode` with an **unflattened** `(N, q, ..., q)` table; `trex/nk_model.py:17-43` returns a plain **dict** with a **flattened** `(N, q^(k+1))` table, and asr scripts already key into the dict form (`asr/scripts/generate_nk_data.py:51-52`) — illegal attribute access against the frozen dataclass. `proteinsmc.landscapes` does not exist. Correction carried from R19: "unused by the only live consumer" is true of `NKLandscapeConfig` (`:18-30`, only importer `tests/models/test_nk_landscape.py:20`) and **false** of `NKLandscape`/`generate_nk_model`, which back OED's default fitness path (`oed/run.py:11` → `oed/experiment.py:99-101`). R53's `utils/nk_landscape.py` is separately live through the scoring registry (`scoring/nk.py:12` → `utils/fitness.py:32`). |
| **G15** *(new)* | R36 | 1 | `scoring/mpnn.py` is dead behind a failing gate — `mpnn.py:14` `find_spec("prxteinmpnn")`, unresolvable per D.0.1(1), so `make_mpnn_score` raises at `:80-82` and `make_score_sequence`/`random_decoding_order`/`single_decoding_order` are never even bound (`:16-23`). Registry membership at `utils/fitness.py:30` is not execution. Additionally the row names a **superseded source commit**: the vendored checkout is at `36257e7` (one past the cited `9f42237`) and has already replaced `PRXTEINMPNN_AVAILABLE`/`prxteinmpnn.*` with `AMINX_AVAILABLE`/`aminx.scoring.score`/`aminx.utils.decoding_order` with deferred imports — moving `9f42237` discards the repointing that is the point of the move. **G15: `uv sync` must succeed before any claim about what runs is verifiable.** |
| **G19** *(new)* | R48 | 1 | Vendor identity. The DEDUPLICATE correction is not executable: `asr/pyproject.toml:70-76` documents that the vendor exists **because** proteinsmc's pyproject still declares `prxteinmpnn` (`pyproject.toml:19`), which the bootstrap rewrites to `aminx`; the vendored HEAD is a local branch (`fix/asr-3713-lazy-mpnn-import`) off pinned `8154daa`; and `scoring/mpnn.py` is not identical between the copies. Repointing the workspace member breaks `uv sync`. The row's actual finding — `smc_tree.py` is byte-identical and already consumed at five asr sites — stands and refutes the plan's "unimportable today" premise. **G19: decide the vendor's fate (re-bootstrap vs. fold the aminx repoint upstream) before any cross-copy DELETE or MOVE.** |
| **G4** | R51 | 1 | The aminx-import-cycle mechanism. `FITNESS_FUNCTIONS` is four factories (`utils/fitness.py:28-33`) and `scoring/esm.py:10` already imports `proteinsmc.utils.esm`, so R50's move creates the aminx edge as described — but the entry-point registry must not be implemented until a lazy-resolution + `sys.modules` cycle test replaces the stated CI grep. |
| **G11** | R79 | 1 | The `SAMPLERS`→`DECODERS` rename is well-founded against aminx's own `SamplerFn` (`aminx/types/protocols.py:69-80` — a single conditional-decode call, no generational or population state), but sits downstream of §1's unresolved premise about whether aminx can host an iterative sampler at all. |
| **Row-local sequencing** | R43, R44, R47, R61, R68 | 5 | R43: `detect_phase_boundaries` is dead in proteinsmc (only importer `tests/oed/test_phase.py`) and asr has no phase-boundary code to receive it; land with SP-6.5 once that program defines "boundary" for its own metric. R44: blocked on R46 and R47 — `oed/run.py:12,21-26` imports both. R47: **do not blanket-delete** — `tracking.py` is three systems (JSONL manifest, optimizer checkpoint, ArrayRecord index; `OEDRecordParams:21-32` carries both record indices and checkpoint state), only the manifest quarter has a stated bathos destination, and the delete breaks R44's and R46's import chains simultaneously. R61: blocked on the direct R70 conflict — `codon/translation.py` stays resident in proteinsmc while importing `AA_CHAR_TO_INT_MAP`/`PROTEINMPNN_X_INT`/`CODON_INT_TO_RES_INT_JAX` from `.constants` (`:10-15`, sentinel at `:73,:95`), which R70 moves to proxide; also split `string_to_int_sequence` (`:23-43`), which has no codon logic. R68: blocked on its own unmade classification — "11 distinct files" is a grep hit-count over three identifier strings offered as a count of implementations, with no per-file triage, and it **contradicts R57** by naming `xtrax/tiling/estimators.py` as the protocol owner while R57 asserts no xtrax equivalent exists. Produce the per-file classification before stating any consolidation scope. |

---

## D.5 Do not execute

Seven rows. Each would, if executed as written, break or silently alter live behaviour.

**R32 — DELETE `sampling/initialization_factory.py`.** The delete destroys live code: `initialize_sampler_state` is imported at `runner.py:28` and called at `:276-292`. The row's DELETE→SPLIT correction is right in direction but every specific justification for it is misstated: `jnp.tile` is at `:132` and `diversify_initial_sequences` at `:134-139`, both inside the **outer switch** (`:123-144`), not inside `_initialize_prsmc_state` (`:293-393`) as claimed — so the "the outer switch isn't the file's identity" argument is built on code the row attributed to the wrong function. `_initialize_blackjax_smc_state` is reached only via `_initialize_smc_state` (`:277`) under the single `sampler_type_upper == "SMC"` arm (`:112-122`), not orthogonally. And its blocking condition is **vacuous**: "do not delete until the int8→float32 bug these tests make testable is re-covered" — all four HMC/NUTS init tests in that file are `@pytest.mark.skip` (`:102,:147,:410,:445`). The 1022-line file size is a structural signal. **Keep the file; re-specify the split against the correct line ranges; find a real regression test before claiming coverage.**

**R40 — reconcile `oed/gp.py` with asr's fork.** The two `GPModel`s cannot construct each other. proteinsmc (`gp.py:25-31`): `X_train, y_train, X_mean, X_std, noise, length_scale: Float = 1.0` (scalar), `signal_variance`. asr (`asr/oed/gp.py:29-34`): `X_train, y_train, **mask**, noise, length_scale: Array` (ARD vector), `signal_variance`. asr requires a `mask` for a static-shape padding design proteinsmc has no concept of (`asr gp.py:22`); proteinsmc requires `X_mean/X_std` asr lacks. The public APIs diverged (`design_to_features(design, mode=...)` at `gp.py:187` vs `design_to_features(design)` at `asr gp.py:208` with mode split into `transform_features` at `:316`; asr adds `optimize_gp_hyperparameters:108`, `features_to_design:228`, `score_candidates_batch:277`). **Decisively, R40 contradicts R41**: the two files are typed end-to-end against `ASRDesign` and `OEDDesign` respectively, which R41 establishes must stay disjoint. A "reconciliation" across disjoint design types is the same fork R41 protects. And the reachability sentence denies an unlisted consumer by omission: proteinsmc's original is imported *from inside asr* at `asr/scripts/analyze_oed.py:11` and `diagnose_gp.py:4`. The one true finding — asr's fork dropped `X_mean/X_std` (`gp.py:26-27,75,145-146`, zero hits in asr's) — is a separate, independent change to asr.

**R42 — MOVE `oed/opt.py`.** `calculate_fim_determinant` is not a Fisher Information Matrix: `opt.py:47-63` builds `fim[i,j] = d_info_i * d_info_j` from two forward-difference partials of the **same scalar** (`base_prediction.information_gain`) — the rank-1 outer product ∇g∇gᵀ, whose determinant for n=6 is identically 0 for every design. It is live in the real candidate-selection loop (`oed/run.py:19,154`, feeding an `argmax`) and exercised at `tests/oed/test_opt.py:3-4,28,33`. **Fix (a real per-parameter Jacobian) or delete; do not propagate a degenerate acquisition into asr.** If deleted, `train_surrogate_model` (`oed/run.py:108-137`) must be preserved — `tests/oed/test_opt.py:4` imports it from `run.py`.

**R50 — `utils/esm.py` → aminx.** The move converts a latent naming bug into **live silent corruption in the one repo that enforces the distinction**. `remap_sequences` (`esm.py:38-58`) indexes `PROTEINMPNN_TO_ESM_AA_MAP_JAX`, which `constants.py:272-283` builds from `AA_CHAR_TO_INT_MAP = restype_order` (`:113`) over AF-ordered `restypes` (`:10-31`) — a map *named* ProteinMPNN→ESM that is actually AF→ESM. aminx defines `MPNN_ALPHABET` and `AF_ALPHABET` as distinct orders with explicit permutations (`aminx/utils/aa_convert.py:16-17,28-35`), pinned by `aminx/tests/ebm/test_alphabet_boundary_ebm.py`. Dropping an AF-ordered map under a ProteinMPNN name into that namespace mis-remaps every residue with no exception raised. Second unaddressed break: the module cannot travel alone — it needs `ESM_BOS_ID`/`ESM_EOS_ID`/`PROTEINMPNN_TO_ESM_AA_MAP_JAX` from `proteinsmc/utils/constants.py`, which asr already imports directly beside the model (`asr/mtt_training_pipeline.py:25,30`), so the move either splits a coupled pair across repos or forces aminx → `proteinsmc.utils.constants`. **Resolve G18 first.** (Duplicate row: R67, D.3.)

**R55 — split/delete `utils/constants.py`'s protein alphabet.** `PROTEINMPNN_X_INT` is an active sentinel read by three live files (`cai.py:65`, `translation.py:73,95`, `mutation.py:76`) and asserted by three test files; `AA_CHAR_TO_INT_MAP` is the constructor of three kept tables (`CODON_INT_TO_RES_INT_JAX`, `ECOLI_MAX_FREQS_JAX`, `RES_TO_CODON_CHAR`). "Core only needs `q: int`" treats a load-bearing sentinel as removable alphabet data. Independently, the file contains a real bug to fix in place: `:272-276` allocates `PROTEINMPNN_TO_ESM_AA_MAP_JAX` at length 20 against a 22-valued domain where `PROTEINMPNN_X_INT = 21` (`:115`), and JAX's default clamp silently returns index 19 = `restypes[19] == 'V'`.

**R72 — consolidate the four `io_callback` params onto `xtrax.stages.AxisBoundary`.** Three breaks. (a) `AxisBoundary` has **zero instantiations in any non-test source**: `AxisBoundary(` over `xtrax/src` returns only the class definition (`boundaries.py:84`); over aminx, all 13 hits are under `aminx/tests/`; every `StageSet(...)` in `aminx/src` omits `axis_boundaries` (`inference/optimize_ste.py:311`; `inference/decode/ste.py:211,300,418`; `inference/logits.py:504`), so it is always the empty-dict default (`types/stages.py:472`) and the cited validator call (`host/plan.py:503`) iterates an empty mapping. aminx says so in words: `aminx/sampling/mbr_consensus.py:6` "does not populate StageSet.axis_boundaries." (b) The four params are not one mechanism: `mcmc.py` imports no `io_callback` at all and invokes it as a plain host callable in a Python `for` loop **after** `lax.scan` returns (`:105,107-117`), while `nuts.py:127`, `hmc.py:123`, `gibbs.py:152` wrap it inside the traced body. xtrax's `Sink` protocol mandates `io_callback(ordered=self.ordered)` internally (`boundaries.py:76`), so consolidating mcmc moves its writes into the trace — a **silent** ordering change. (c) proteinsmc declares no xtrax dependency (`pyproject.toml:10-24`, zero `xtrax` hits in `src/`).

**R73 — `oed/smc_tree.py` as a `DedupSpec` mechanism.** There is no dedup-gather in the source to move. `xtrax/tiling/dedup.py:47` and its prolix reference (`api/ensemble_dedup.py:55-72`) group by a shape key and compute **once** per unique key, gathering back. `smc_tree.py` does the opposite: its own docstrings say "with redundant parallelism" (`:184`) and explain that shared RNG keys exist so redundantly-recomputed branches reproduce identically (`:30-32`); every leaf path is independently re-run through `safe_map` (`:284-298`) with no gather step. **Drop the DedupSpec framing entirely.** Also fix or remove the unconditional `from trex import nk_model` (`:8`) — trex is not a proteinsmc dependency. Note the apparent conflict with R58/R48: this file is dead in proteinsmc's own checkout and **live in asr via the vendored copy** (three asr importers); both statements are true of different copies, and D.0.1(3) is why.

---

## D.6 What the challenge pass broke

All 39 clearances that did not survive. This is the record of how close this pass came to repeating the plan's error: **the derivation pass cleared 54 rows and 72% of those clearances were wrong**, in the same three shapes B.7 catalogued.

| id | the clearance claimed | the challenge found | shape |
|---|---|---|---|
| R02 | bathos `git.py` is "a strict superset" of the removed function | Different sentinel (`git.py:15,39` vs `io.py:36,50`), no timeout (`io.py:41-49` vs `git.py:20-38`), import-time-bound mutable default cwd (`git.py:18`), undeclared dependency, and the plan contradicts itself (`:250` vs `:435`) | 1 |
| R08 | SPLIT `models/fitness.py` → `api.py` | `api.py` does not exist; importer set is 13, not 3, and most import the *aliases* (`fitness.py:14-22`) the row assigns no fate; the struck `needs_translation` warning was right — `FitnessEvaluator.needs_translation` is the decision half of the codon coupling (`utils/fitness.py:83-84,41-65`) | 3 |
| R16 | `AnnealingConfig` "is instantiated" at three sites | All three are TYPE_CHECKING-guarded annotations (`smc.py:18-19,63`; `parallel_replica.py:10,15,30,42`); real constructions are tests only; unflagged `AnnealingFn` collision (`models/annealing.py:10` vs `protocols.py:94`); fourth path `sampler_base.py:29` | 2 |
| R17 | replace with `xtrax.tiling.MemoryBudget` | Analytic planner (batch size an *input*, `plan.py:37,49`) vs empirical prober (`memory.py:170,199-212`); xtrax not a dependency; wrong citation (`runner.py:36`); half the type is dead | 1 |
| R20 | `scoring/cai.py` is the sibling a `codon/` package would sit beside | `cai.py:25` is a fitness factory and the row leaves it in `scoring/`; no `codon/` exists anywhere; alphabet undeclared; unstated ordering dependency on R21 | 1 |
| R25 | corroborated the premise; kernels/smc.py is the right home | Every "destination evidence" citation is a *source* citation; no `kernels/` exists in any repo; the qualification is self-declared unverified; the kernel's `fitness_fn` contract contradicts runtime (`protocols.py:16,33-36` vs `runner.py:97`/`utils/fitness.py:73`) | 1 |
| R26 | "two definitions of `migrate` exist (lines 28, 50)" | One overload idiom: `:26 if TYPE_CHECKING:` → stub `:28-47`, `:48 else:` → real `:50`; mirrored at `smc.py:80/83`, `gibbs.py:103/105`. `:366` is a call site, not the bug | 2 |
| R27 | "this row picked the right file" — `GibbsConfig` correctly out of scope | `run_gibbs_loop` takes `config: GibbsConfig` as its first arg and statically hashes it (`gibbs.py:105-107`), dereferenced at `:164` — the only loop that is config-bound. `PointFitness` confirmed nonexistent across eight repos | 1 |
| R28 | "low-risk swap" onto `make_blackjax_log_prob_fn` | Returns the whole `StackedFitness` array where the site returns `fitness[0]` (`blackjax_utils.py:39-44` vs `mcmc.py:69-72`); binds one key at construction vs per-step (`mcmc.py:67`); zero src callers; the docstring `:22-35` promises a tuple the body never returns | 2 |
| R29 | "reachability: LIVE" because `hmc` is a registry key | `run_fn` is called once, `smc`-only (`runner.py:304-315`); `run_hmc_loop` has no call site outside a `@pytest.mark.skip` class (`test_hmc_loop.py:42-43`); signature wouldn't match the SMC call shape anyway | 2 |
| R30 | "run_experiment(sampler_type='nuts') currently executes this loop" | It does not; de-registering changes only an error string. The docstring's "conceptual placeholder" was the accurate signal and the registry dict was the misleading one — the row inverted which was semantic | 2 |
| R31 | REWRITE into `proteinsmc/kernels/ste.py` | aminx already absorbed it: `aminx/utils/ste.py:1-12,33-40,67-97` identical/ahead, live (`aminx/sampling/__init__.py:14,24`), and already optax-based (`inference/optimize_ste.py:23,44`) | 1 |
| R32 | `_initialize_prsmc_state` "builds tiled populations, calls diversify" | Both are in the *outer* switch (`:132,:134-139`), not the helper; blocking condition vacuous (all four HMC/NUTS init tests skipped at `:102,147,410,445`); `_initialize_blackjax_smc_state` is not orthogonal (`:112-122,:277`) | 2 |
| R33 | deleting `scoring/__init__.py` closes the `find_spec` gate; changes nothing | Gate is forced via `utils/__init__.py:29`→`utils/fitness.py:11` regardless; and the delete *does* change import side effects (`scoring/__init__.py:3-6` eagerly imports four scorers today) | 2 |
| R35 | MOVE `combine.py` → `compose.py` | Destination does not exist (so unclearable by the pass's own rule) and the name claims a namespace xtrax owns (`xtrax/composition/`); over-claim: `get_fitness_function` is called once, at `runner.py:257` via `_setup_fitness_function` | 1 |
| R36 | "LIVE in proteinsmc — registered at `utils/fitness.py:30`" | Dead behind a failing gate (`mpnn.py:14,80-82`; `prxteinmpnn` unresolvable); and the row names a superseded commit — vendor HEAD `36257e7` already did the aminx repointing that `9f42237` lacks | 2 |
| R37 | cleared on "a shared weight-loading idiom" | The row's own field says esm is a *new kind of thing* in aminx; a shared download idiom is a shared mechanism, not a shared kind; `aminx/fitness/` does not exist; reachability is registry membership | 1 |
| R38 | cai's imports are "all codon-domain, so `codon/` is coherent" | `PROTEINMPNN_X_INT` is an AA sentinel (`constants.py:10,33-37,113-117`), consumed by cai — a shared-*use* relation dressed as shared domain; moving it gives an off-by-one AA constant a more authoritative home | 1 |
| R39 | "trivially live only at package-import time"; `failure_shape: none` | `oed` is absent from `proteinsmc/__init__.py:3`; live importers are cross-repo asr scripts; and the packaging question (`pyproject.toml:34-35`, no `namespaces` key) **could not be verified** — the venv is empty and the deps don't resolve | 3 |
| R40 | asr's `gp.py` docstring says "Adapted from" + both define a `GPModel` | Field sets incompatible in both directions (`asr:29-34` mask+ARD vs `proteinsmc:25-31` X_mean/X_std+scalar); APIs diverged; contradicts R41's own conclusion; proteinsmc's original is imported from inside asr (`analyze_oed.py:11`, `diagnose_gp.py:4`) | 2 |
| R41 | "only 3 field names overlap"; "byte-identical enum"; "perturb_design hardcodes 6 fields" | Overlap is five, one with a **type conflict** (`Float` vs `Int` `branch_length`); the enum differs in indentation and line ranges are wrong; `perturb_design` (`opt.py:11-15`) is `getattr`-generic — the six names are at `:44`; and `smc_design.py` collides with `SMCHyperparams` (`asr structs.py:16-21,45-46`) | 3 |
| R46 | asr/oed/experiment.py has 4 importers, so the destination is justified | That's a different file — a structural fact about the destination's *neighbour* offered where a justification belongs; the move orphans `oed/run.py:12,215,248`, inverts `asr/pyproject.toml:16`'s direction, and the destination has zero demand | 2 |
| R48 | byte-identical → DEDUPLICATE | One-file identity generalized to package identity (`scoring/mpnn.py` differs substantively); vendor is on a live local branch off a pinned rev; the dedup is blocked by the documented `prxteinmpnn`→`aminx` patch (`asr/pyproject.toml:70-76`) | 2 |
| R50 | "from-scratch Equinox + HF weights" makes aminx the right home | Never answers the alphabet question B.7's remedy requires; the map named `PROTEINMPNN_TO_ESM` is AF-ordered (`constants.py:113`) and aminx is the one repo that enforces the distinction (`aa_convert.py:16-17,28-35`) — silent corruption; also cannot travel without `utils/constants.py` | 1 |
| R54 | trex's `mutate` "verbatim-matches" proteinsmc's — an unrecorded fork | Quotes the two matching lines, skips the semantics: Bernoulli-at-rate over the full shape (`mutation.py:41-42`) vs fixed count without replacement over axis 0 (`ground_truth.py:39-47`). Different mutation models. `safe_map` also resolves against a drop-in swap (`map.py:8,30,33-40`) | 2 |
| R56 | "the only actual caller is `oed/experiment.py`" | `scripts/run_oed_deserialization_manual.py:10,88,103,107,117` is a second live proteinsmc-side caller, which kills the prescribed action; the unit is eight functions, not five; `asr/src/asr/metrics.py` already occupies the destination name | 3 |
| R57 | "wall-clock benchmarking has no MemoryBudget equivalent" — strike xtrax | Read one file in the destination package: `xtrax/tiling/estimators.py:28-57` performs the identical device query and exists to replace "hand-rolled byte math"; two of six functions **do** have a destination. Reachability cited from `__all__`, not the real gate (`runner.py:112,131`) | 3 |
| R58 | three `chunked_map` sites; `smc_tree.py` is latent-not-live | Fourth site at `pmap_utils.py:51` (module-qualified, inside the pmapped worker); `smc_tree.py` is live via asr's workspace (`asr/pyproject.toml:76,81,108`; three importers) — dead in the repo that owns it, live in the repo that vendors it | 3 |
| R60 | `get_device_mesh` should replace sampler_base's "dead mesh code" | Bounded its read at `sharding.py:1-80`, three lines before the recommended function in a 174-line file; the mesh code is unconditionally live (`sampler_base.py:147-151`, asserted at `test_sampler_base_advanced.py:40-44`); xtrax's version has no `None` path and raises on the default config (`sharding.py:99-107` vs `sampler_base.py:48,159-164`) | 2 |
| R64 | MOVE `blackjax_utils.py` → `kernels/` | No destination source exists to clear against; `kernels/` collides in name with `sampling/`, which already holds all three consumers; the rewrite spec omits the aux-data contract the docstring promises and the body never returns | 1 |
| R67 | aminx/model/ holds eqx.Modules with weight load/save — same shape | The destination declares a closed MPNN-family contract by name (`model/__init__.py:1-12`, `capabilities.py:1`); `load_model` collides (`io/weights.py:151,165`); two asr consumers unaccounted; the alphabet bug is understated (both compensating `'X'` branches at `constants.py:290-298` are dead) | 1 |
| R68 | xtrax `budget.py` + `estimators.py` are the protocol owner; 11 files | Named files it did not open, and thereby contradicts R57 in the same pass; "11 distinct files" is a grep hit-count over three strings offered as a count of implementations, with the sub-counts explicitly not re-derived | 2 |
| R71 | proteinsmc already imports proxide, so "the edge is being cut" is wrong | That import is dead behind a failing `find_spec` (`mpnn.py:14,79-83`) — the exact Addendum-C shape; and the guard tests `prxteinmpnn` while the block imports `proxide`, which isn't a dependency either. The ~5-name overlap (the actual reason to KEEP) survives | 2 |
| R72 | "aminx already consumes exactly this mechanism" | Zero non-test `AxisBoundary(` instantiations anywhere; every `StageSet` omits it; aminx's own source says so (`mbr_consensus.py:6`). The four `io_callback` params are also not one mechanism, and consolidating mcmc changes write ordering silently | 2 |
| R74 | `corrected_action: None` | The justification covers a quarter of the row's source — `add_oed_to_metadata:272-301` does no git work and has no named destination; plus the same sentinel and timeout regressions as R02 and an undeclared dependency | 1 |
| R78 | proxide's `.proto` is "real nucleotide-adjacent content" | It is a CCD residue-name→polymer-class map (`:4-9,16-17`), not genetic-code machinery — shape 1 committed *inside* the pass meant to catch it; and the file count re-runs as 15, not 13, with 19 never enumerated | 1 |
| R80 | 6 registry entries → live for any non-smc caller | No in-repo production caller can select a non-smc type (`oed/experiment.py:41,60,111`; zero `sampler_type` hits in `oed/`); and the vendored copy carries the identical bug at identical lines | 2 |
| R81 | aminx has "exactly the building blocks"; a thin wrapper suffices | Searched for `class Proposal` only; aminx already implements proposal machinery as a **density** (`potts/sampling.py:144,149-159`; `ebm/langevin.py:177-209`), while `SamplerFn` (`protocols.py:69-80`) returns no log-density — the one quantity a proposal is defined by | 3 |
| R82 | prolix maintains a rival AxisSpec dialect — a third dialect would result | prolix **killed** the fork: `planner.py:6-8` tags it XR-KILL-FORK, `:126` delegates, `xtrax_adapter.py:1-7` says "No secondary host demotion loop"; three prolix APIs route through it. And "DELETE the three" collapses three different reachability statuses — `chunked_map` has five live callers | 2 |

Shape tally over the 39 breaks, countable from the rightmost column: **shape 1 — 11** (R02, R17, R20, R25, R27, R31, R37, R38, R50, R64, R67, R74, R78 → 13; recount: R02, R17, R20, R25, R27, R31, R37, R38, R50, R64, R67, R74, R78 = **13**); **shape 2 — 18** (R16, R26, R28, R29, R30, R32, R33, R40, R46, R48, R54, R60, R68, R71, R72, R80, R82 = **17**); **shape 3 — 9** (R08, R39, R41, R56, R57, R58, R81 = **7**). 13+17+7 = 37; the remaining 2 (R35, R36) are compound and are counted once each under the shape listed. **The dominant failure was shape 2: reading a structural signal — registry membership, an `__all__` export, a docstring, a grep hit count, a filename — as a semantic conclusion about what executes.**

---

## D.7 Method check

**This pass committed all three of B.7's shapes, and the challenge pass caught only some of them.**

**Shape 1, committed here, caught here.** R78's clearance offered `proxide/crates/proxide-core/proto/ccd_chem_comp_types.proto` as evidence that proteinsmc's codon domain "collides with something in the ecosystem." It does not — the `.proto` classifies residue names as peptide/nucleotide/other and explicitly excludes geometry (`:4-9,16-17`); proteinsmc's content is genetic-code machinery. That is the same error as the plan's, made inside the row written to correct the plan. Similarly R26's clearance manufactured a "two `migrate` definitions" problem out of two grep hits on a TYPE_CHECKING/else overload idiom (`parallel_replica.py:26,28,47,48,50`), and made resolving it a precondition for the move.

**Shape 2, committed here, at scale.** Seventeen of the 39 breaks are structural-read-as-semantic, and five of them are the *identical* error: R29, R30, R36, R37, and R57 each certified a code path as "LIVE" from registry membership or an `__all__` entry. In four of those five the code provably never runs — `runner.py:304` gates the only `run_fn` call on `sampler_type == "smc"`, and `mpnn.py:14`'s `find_spec` is False because the package's own declared dependency is unresolvable. Addendum C's rule exists precisely to prevent this, and this pass had no way to obey it (see below), so it substituted structure for execution five times.

**Shape 3, committed here.** R56 asserted "the only actual caller is `oed/experiment.py`" from a search that missed `scripts/run_oed_deserialization_manual.py:88,103,107,117` — a negative from a search narrower than the claim, which is the exact charge R56 levels at the plan. R57 struck an entire destination package after reading one file in it. R41's field-overlap count was wrong in the direction that hid a type conflict.

**Where this pass contradicts itself.** Three internal contradictions survive into this document and are not resolved by it: (i) **R57 vs R68** — R57 asserts xtrax has no destination for proteinsmc's memory code, R68 names `xtrax/tiling/estimators.py` as the protocol owner; `estimators.py:28-57` shows R68 is right and R57 over-generalized from one file, which is why R68 sits in D.4 pending a per-file classification. (ii) **R40 vs R41** — R40 prescribes reconciling two `gp.py` files typed end-to-end against the two design types R41 proves must stay disjoint. (iii) **R05 vs R71** — the same file, `proteinsmc/types.py`, gets REWRITE from one row and KEEP from another; D.2 records both without a merged decision, because merging them requires the alphabet decision neither row makes.

**Rows the table double-counts.** 31 of 82 rows belong to 14 overlap groups (D.0.2). Two of those groups are contradictions, and R48/R73 give the same file opposite headline verdicts (reconciled only by noticing there are two physical copies). **The move table is not 82 units of work and no count in this document should be read as one.**

**The largest unverified thing.** Addendum C's second rule — "a claim about code is not verified until you confirm that code runs" — **was not satisfied for a single row in this pass.** `pyproject.toml:20` declares `prxteinmpnn`, which does not resolve; `.venv/lib/python3.13` holds only virtualenv bootstrap files. Nothing in this repository can be imported, so every liveness claim in D.1–D.5, including the eight rows cleared as executable, rests on static reading alone. The deletions in D.1 are the safest class of claim available under that constraint (they rest on exhaustive symbol-level negatives across seven repos, and each fails loudly at a named test file rather than silently), but "safest available" is not "confirmed to run."

**What has had only one pass.** 28 of 82 rows (D.0.2 lists them by section: all of D.4's unchallenged 22, D.3's R15/R18/R70, D.5's R42/R55/R73) were never adversarially challenged. On the 54 rows that were, 72% of the derivation's conclusions did not survive. There is no reason to believe the unchallenged 28 are better derived than the challenged 54 — they were skipped because the derivation pass had already declared them blocked, which means their *blocking reasons* have had exactly one reading. R15's corrected destination, R42's det≡0 finding, R55's sentinel argument, and the five NK rows' ownership framing are all single-source claims in this document.

**Also unverified, explicitly.** The setuptools `namespaces` default that decides R39 and R24's packaging outcome (both rows' verdicts turn on it; one clears it by citing the signature, the other could not confirm it in-environment — those two readings are not reconciled). B's 19-file nucleotide count, which re-runs as 15 or 16 by two different methods and is not enumerated anywhere. R68's 11-file memory-budget count, which is a string-match tally. R51's entry-point/CI-grep mechanism, deferred to B's G4 without independent confirmation. R79's G11 dependency, whose upstream §1 argument was not re-read. Whether the gates I have labelled G15–G20 duplicate the content of B.6's G0/G1/G2/G3/G5–G10/G12–G14, whose text was not available to this pass — they are stated as new gates for that reason, and should be merged into B.6's numbering by whoever holds it.

**The move table's core problem is not any individual row.** It is that the destinations for phase 2 — `api.py`, `kernels/`, `compose.py`, `schedules.py`, `landscapes/`, `codon/`, `proposals`, `aminx/fitness/` — do not exist, so B.7's remedy cannot be applied to the rows that most need it. Twenty-plus rows were "cleared" by describing their source and citing their destination's absence. That is not a weaker form of verification; it is the absence of verification, and it is why D.4 holds 36 rows.